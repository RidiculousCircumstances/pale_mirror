# Continuity Ledger

## Goal (success criteria)
- Implement Pale Mirror Frontier v3 from `docs/frontier-v3-contract.md`, `docs/frontier-v3-execution-semantics.md` and `docs/frontier-v3-implementation-plan.md` as a greenfield Java event-driven simulation with production two-way Minecraft materialization.
- Deliver an autonomous 1024×1024 world with 12 settlements and one distributed hive, exact actor/object identities and resource quantities/custody, continuous HOT/COLD shared rules/history without equal physical fidelity, bounded COLD knowledge, cross-scene causality and no protected zones. Legitimate physical consequences are accounted within bounded work; unloaded aftermath and confirmed/in-flight/ambiguous recovery obey the execution-semantics contract, never blanket immediate-world or WAL-atomicity promises.
- Deliver through `docs/engineering-agent-protocol.md`: the supervisor only engineers/documents/reviews; one `gpt-5.6-terra` high subagent implements approved bounded orders. Prove continuity, lasting player causality and no systematic mode-switch advantage; preserve inherited WIP and human acceptance boundaries.
- Cut production over to v3 and remove the v2 runtime only after deterministic, full-scope, performance, restart/recovery, real-world and player-comprehension gates pass.

## Constraints/Assumptions
- Pale Mirror is the independent nested repository `pale-mirror/`; the outer repository owns pack/deployment artifacts. Inspect and commit them separately.
- Java 21, Minecraft 1.21.1 and NeoForge 21.1.248 remain the pinned development platform.
- `pale-mirror-frontier` will be a pure-Java module under `io.farfrontier.palemirror.frontier.v3` with no dependency on `pale-mirror-domain`, `frontier.reference`, Python, Minecraft or NeoForge.
- V3 is fresh-world-only. V2 remains frozen and isolated until the v3 cutover gate; there is no shared state, save migration or runtime fallback.
- Every v3 format or immutable projection change recreates every affected V3 world (disposable, local or deployed): the active route-patrol cut accepts only snapshot schema 126 and persistence-envelope v41 state/WAL inputs. Older snapshot/WAL bytes fail closed before hydration or replay; reset is an explicit deployment operation, never an in-process migration.
- One resident is one canonical person and one HOT Villager; one bioform is one canonical creature and one HOT graybox Zombie. Minecraft resource quantities are exact one-for-one units, while fungible vanilla stacks may split and merge under canonical lots, allocations and custody accounts.
- No player, settlement, hive, ownership class or operation boundary is a safe zone from legitimate physical effects. Every actual consequence must be observed and reconciled.
- Desired-state materialization never silently overwrites an unknown/player change. Physical effects and post-impact accounting are a separate boundary.
- No simulation, materialization or audit path force-loads chunks. Start at most one visible client on `DISPLAY=:0` and run only relevant gates.

## Key decisions
- 2026-09-05 user amendment:25-percent restart acceleration is advisory, not a blocking floor; accept retained22.52 percent for this optimization and do not chase the gap with native runs. Preserve original reports and all correctness/recovery,3x feedback and2.5x provider requirements. Future measured regressions still require review.
- 2026-09-05 managed delivery accepted: engineer alone owns normative docs/ledger and acceptance; Terra high alone owns implementation under versioned work orders. Safe previous-writer handoff and Gate A/B/C review precede write grants, expensive tests and advancement. Protocol: `docs/engineering-agent-protocol.md`.
- The v3 product promise is one seamless autonomous Minecraft world with shared rules, identities and history, not identical HOT/COLD physical fidelity. Leaving, returning and restarting do not themselves create resources, reset work or undo confirmed consequences.
- Accepted execution-semantics correction: `docs/frontier-v3-execution-semantics.md` is normative for independent presentation/physical activity, bounded COLD knowledge, causal aftermath, cross-front interaction, recoverable confirmation and exact-versus-calibrated comparison. It adds mandatory exits to existing F0 slices, not a new wave or implementation-completion claim. At the next safe F0.VA measurement boundary, align family descriptors, test comparators and unproved gaps before the next slice; preserve WIP, acceleration and the ordered monorepo/provider gate. Changed assertion meaning invalidates only affected cached acceptance.
- Canonical mutation is one ordered server-thread lane of typed commands, deterministic events and persisted due actions; there is no indivisible daily phase.
- One simulation unit is one running server tick. Zero-player server time advances; stopped-server time pauses; sleep advances through an explicit command; `/time` alone does not.
- Fixed-point arithmetic, checked rounding and keyed/counter RNG make v3 deterministic from its own input stream without Python numerical parity.
- Exact-person migration is one bounded Transit journey: its adjacent-position cursor and destination-bed reservation are canonical; COLD advances the same journey in short steps, while the forthcoming naturally loaded HOT executor may only take/release that identity and cursor, never create a second movement state.
- HOT scenes are persisted atomic physical-execution leases over an already-existing canonical process, never process owners. Presentation demand and natural physical interaction eligibility are independent inputs; neither creates, starts or resets work, and COLD resumes only after the affected physical custody safely closes. Every duration-bearing process has paired registered COLD/HOT drivers over one authoritative cursor; acquire, observed checkpoints and release update that process and lease atomically. Atomic effects, distributed frontiers and ambient presence keep their distinct intent/frontier/custody boundaries.
- `docs/frontier-v3-seamless-foundation.md` is the mandatory implementation handoff for the current correction programme. F0 must close replica/custody separation, deferred physical aftermath, fungible resource custody, bounded operation fronts with local navigation, fenced restart recovery, failure taxonomy, observer neutrality and first-visibility readiness before MAT-004 or any new materialization breadth resumes.
- `docs/frontier-v3-integration-feedback-foundation.md` defines mandatory slice F0.V between completed F0.0 and resumed F0.1. It pauses but preserves the active harvest WIP while adding a measured internal Process/Scene SDK: process-agnostic HOT/COLD contract and scenario layers, a production-backed harvest adapter, one second existing-process conformance proof, deterministic test-only crash boundaries, one-build/one-client restart execution and bounded failure bundles. The second proof opens no gameplay breadth and closes no later F0/MAT slice; no later F0 or MAT breadth may bypass the F0.V exit gate.
- `docs/frontier-v3-accelerated-verification-loop.md` is mandatory substage F0.VA inside F0.V. After the current process reaches a safe boundary, expensive matrix closure pauses while the agent adds exact lifecycle barriers, one persistent client per assigned matrix, content-addressed nonfinal evidence with conservative dependency selection, an executable test pyramid, four isolated GitHub Actions correctness workers with serialized same-host timing, and opaque immutable development-fixture clones. Final evidence bypasses cached passes and fixture images. F0.VA targets 3x aggregate median feedback on three representative edit classes and 2.5x wall-clock speed for the same four-worker correctness matrix while retaining restart correctness and the user-accepted measured benefit (25 percent advisory).
- After all locally verifiable F0.VA gates are green, `docs/frontier-v3-implementation-plan.md` requires a history-preserving, non-squashed monorepo migration before original F0.V resumes. The current workspace root remains the root, Pale Mirror remains at `pale-mirror/`, both old HEADs must remain ancestors, and the verified `main` is pushed without force to `git@github.com:RidiculousCircumstances/pale_mirror.git`. That remote then owns the actual four-worker provider proof; neither pre-migration history nor recovery bundles may be discarded.
- The internal SDK has six explicit execution archetypes: duration work, coordinated traversal, composite operation, atomic intent, spatial frontier and ambient custody. A versioned descriptor declares production vocabulary/provider bindings and hard work/state/locality bounds. Natural demand from any number of players aggregates to one lease per exact process/front and cannot duplicate, accelerate or prematurely drain it. F0.4, not F0.V, owns the full composite-operation proof: one parent retains purpose/roster/resources/result while deterministic disjoint children join, cancel, partially succeed/fail and return or transfer every allocation before parent completion.
- `docs/frontier-v3-terra-implementation-brief.md` is the mandatory executable handoff for the GPT-5.6 Terra continuation agent. It operationalizes F0 without changing its source-of-truth order: the ledger names the current slice; the architecture map, contract, foundation brief and audit own their respective facts.
- The accepted terrain-aware movement contract treats the flat graybox as one provider, not the spatial model. Typed surface/body/port/transport positions, semantic oriented facility ports and one bounded immutable 3D capability topology own movement; COLD and HOT advance the same retained edge/cursor, while loaded obstruction/damage changes topology through typed evidence. Pedestrian/bioform and future rail graphs are distinct, and Create may only implement the rail physical provider.
- V3 persistence is checksummed snapshots plus ordered WAL. Player custody and non-replayable physical effects are durable before acknowledgement/execution and recover by postcondition inspection, never blind replay.
- Buildings/organs have stable IDs, semantic block parts and domain state. Infection uses a sparse 4×4 surface field; territory and actor positions use separate resolutions; baseline plus bounded sparse deltas retain physical aftermath.
- Human and hive AI use event-triggered utility selection, durable HTN-like processes and bounded HOT local goals. The two seed nests belong to one hive economy; hive expansion may consume only fresh bounded territorial beliefs from local Ganglia/Scouts, never the global infection field. The hive keeps one durable asymmetric doctrine (`CONSOLIDATE`, `EXPAND`, `INTERDICT`) derived only from exact reserves and fresh local facts; it gates operation kinds rather than cloning settlement policy.
- F0.4 must make decision ownership first-class before generalized combat: each settlement and the whole Hivemind have exactly one canonical `DecisionAuthority` scope over perceived knowledge, doctrine, commitments, reconsideration epoch, stable policy ID/version and bounded trace without duplicating objective/task/resource owners. Each coordinated operation retains one `TacticalPlan` within its existing operation/front ownership. Common closed strategic, tactical and individual-behaviour policy protocols dispatch through stable descriptors to asymmetric implementations; generic operation/scene code never branches on a concrete unit or tactic, while scenes, entity brains and movement providers only execute traceable bounded directives. Every order names one authority/epoch, every local directive names one plan/front/current lease, and stale epochs fail closed.
- The accepted hive physiology contract is `docs/frontier-v3-hive-physiology.md`: infection substrate, destructible dual-capacity hiveroot and attached organs are distinct; the whole intelligence is the Hivemind, local integration uses `GANGLION`, territory uses connected stationary `RELAY`, and every coordinated remote expedition requires an exact mobile `OVERSEER`. Exact inactive bioforms occupy explicit cocoon slots and never wake because a chunk loaded. Purposeful Sentinels must physically sense and deliver observations. Biomass, carcasses, adaptations, grafts and payload charge are conserved visible processes, and controller loss degrades rather than deletes subordinates. Schema 112/envelope 24 own independently tagged chassis/mutation/assignment plus lifecycle, exact task-mobilization custody, ruleset-defined Overseer command capacity, retained original Scout sighting, HiveTaskAssembly topology, exact physical assembly-blockage evidence, independently persisted route-engagement command authority and one atomic same-roster departure payload that starts its exact assault; `ActorLocation` and ambient lease remain the sole vitality/body/HOT-custody authorities. Cocoon-retained or task-committed forms are not eligible for unrelated COLD patrol, route-engagement or assault selection.
- The accepted human capability contract is `docs/frontier-v3-human-capabilities.md`: one exact resident separately owns condition, trainable skills, profession/employment, exclusive current assignment, exact equipment and work/tactical organization. The first composition is small exact defence, patrol/escort, engineering/recovery and medical/evacuation units, never cohorts or permanent mob classes. Combat functions are derived rather than permanent classes; mobilization has explicit civilian opportunity cost, and leader/equipment loss degrades the same people instead of replacing them. Existing `ResidentRole` is provisional bootstrap affinity requiring an explicit stable-tag/schema migration before broader Wave-4 role expansion.
- `docs/frontier-v3-create-direction.md` is the accepted post-graybox industrial direction, not a current cutover gate. Create will be a NeoForge physical provider over unchanged v3 authority. Transport scope is rail only—long lines, exact freight trains, stations/yards, switches/signals, bridges/tunnels, loading and repair; road freight and aeronautics are excluded. The future physical scope is factories, farms, weapons, turrets, extraction, energy, metallurgy/materials, warehousing/rail logistics and construction/engineering. Current graybox work must retain provider-neutral facility ports, rail topology, exact train/cargo leases and observed consequences so this remains an incremental replacement rather than a second economy.
- A settlement assault is a distinct bounded hive operation rooted in one fresh Scout settlement sighting and fresh local territorial evidence. It retains its own attackers, target, lifecycle and HOT/COLD scene cause; it never uses a route operation, cargo carrier or hidden global query as a substitute.
- Exact resource units, containers and cargo are the economy. Canonical resource lots and exact allocations own provenance and claims; transient slot bindings and player/world carriers own observed physical custody. Ordinary stack split/merge and partial transfer must conserve quantity without inventing permanent per-stack identity, hidden aggregate stock or duplicate placement. Meaningful player supply may require tens or hundreds of stacks.
- An insolvent account rejects new debits, reservations and work admission, but may still receive an exact zero-sum payment. Debt recovery or recapitalization must never be blocked by the debtor's status or compensate it by minting money.
- Durable replica realization is distinct from current physical custody. A container may remain a known realized replica while unloaded without forcing its economy into HOT execution; loaded ownership is a renewable fenced lease. Recognized loss/theft/damage becomes a domain disruption, ambiguous local evidence isolates only that asset, and only canonical/persistence corruption quarantines the frontier.
- Evidence levels remain separate: architecture, automated correctness, real-world continuity and unbriefed player comprehension.
- Frontier v3 causal changes use the smallest relevant checked-in pilot scenario: read-only fixture, ordinary player action, terminal domain assertion and PMV3 trace; visible claims add a semantic camera/frame check, while durable/recovery changes add a declared restart split.
- A pilot visual frame is a local test-only barrier, not a player action: `clean` is the default and closes incidental screens, hides generic UI/chat, settles rendering and waits for exact capture acknowledgement before the next action. `player` is explicit for UI evidence; ordinary HUD toggles are prohibited.
- The active continuity ledger is capped at 1000 lines; Java sources are capped at 1000 lines by the Gradle guardrail. Both limits are ratchets, not permission for mixed-responsibility coordinators.
## State

### Done
- Full inherited history is preserved verbatim in `docs/archive/CONTINUITY_2026-09-05_engineer_handoff.md`. Its old Now/Next entries are historical, not current instructions.
- User relayed the previous writer's explicit handoff on 2026-09-05: current21 terminal, no further file changes/tasks. Main verified PID1979855 absent and no conflicting F0.V runner. Nested HEAD `0ac6f97a695d636ae928313de7a807d1f3ea05a1`, root HEAD `891a4dfe1a613857124775696973d065cc9399cb`; all inherited WIP and root untracked `.f0v-baseline/` preserved.
- Protocol linked from AGENTS, implementation plan, Terra brief, seamless foundation and accelerated-verification brief. Engineer owns docs/acceptance; sole executor is `/root/terra_executor`, gpt-5.6-terra high. Updated active goal matches accepted bounded execution semantics.
- Current21 `build/f0va-current21/feedback-timing.json`, run `9f75476b-7f1f-47d7-b516-19fe8272db4a`: terminal ok, 18 lanes, aggregate speedup 5.065026x against 3x; process_contract 5.012120x, scenario_semantics 5.113490x, lifecycle_restart 5.070469x. This is iterative-feedback evidence only.
- Current21 prepared source-content SHA `d835013a9e4cad29f75c21a737f4204e9fb43fad6c3deb7aea48d666ac22c9b6` /1531 inputs; artifact `41ac2b1fe122d03361976f5614c7ef95052e5a94188297a3ff770a8ccdd82d53`. Verification-plan source SHA `9f3ccb6865a496ab80c2f3d2b719de548cd3297c5b988db43a64f6a61b6717cc` /2016 inputs belongs to a DIFFERENT namespace, not drift. Core governance links changed only after measurement completion; next preparation requires new inputs.
- Current21 persistent native `d96f13b0-64cd-4fc3-8403-ca73d08bbd7c` at `build/f0va-current21/persistent-native.json`: one client, four servers, three worlds, 28 journal barriers. Final stop additionally proves exact save/owned exit/port closure operationally. No missing-save defect or 30-event journal expansion is accepted.
- Terra Phase A read-only packet maps six capabilities: VA.1/.4 local native proof; VA.2 cache keys and SUCCESS terminal references; VA.3 executed T0–T3/selector proof, not T4; VA.5 workflow exists but actual provider evidence absent; VA.6 historical bootstrap r2/r3 and consumer r1/r2 proof is development-only, not current final acceptance.
- Current21 baseline T0/T1 tests actually executed; candidate lifecycle_restart-2 reuses same-key T0/T1/T2 SUCCESS entries with source/artifact/classpath/dependency identity and no fixture, while T3 executes. Cache and prepared namespaces remain distinct.
- Docs diff check and `./gradlew guardrails` passed after core linkage and again after ledger/archive/review-record changes (34 tasks,16 s each). Root diff check passes; nested199 dirty/untracked entries and root `.f0v-baseline/` preserved. PM-HANDOFF-01 Gate C ACCEPTED: adoption/docs only, not inherited implementation. No source implementation by engineer.

### Now
- Active order `docs/work-orders/PM-F0VA-PERSISTENT-CRASH-01.md` revision2 IMPLEMENTING after fresh Gate A ACK by replacement /root/terra_crash_executor (gpt-5.6-terra high). Old /root/terra_executor is COMPLETED with all grants CLOSED. Replacement alone owns listed Node/Java source writes and exact focused Node tests/diff checks; main owns ALL Gradle. Transfer preserves204dirty/untracked entries and outer .f0v-baseline/ only; no native/test process inherited. Node22/22 baseline passes but complete Java/proof-release and size/style fixes remain. No escalation/approval waits. Full/native/provider grants closed. Await coherent implementation and freeze before main focused Java/Gate B review.
- In-progress review findings are recorded in that order: incomplete Java arm must be a negative, legacy resume must not bypass crash proof, and main pure API probe reproduced accepted mismatched resolved revision/authority epoch, omitted authority epoch and unknown boundary. No Gate B acceptance. Also recorded actual CI adoption requirements for retained diagnostic projections, portable/local build identity and serialized scenario hashes; all13current lanes have zero frames.
- Last order `docs/work-orders/PM-F0VA-PERSISTENT-PLAN-01.md` revision1 ACCEPTED by main Gate C. Four allowed compiler/preflight files changed; source tuple d8d307f8…b02ed4. Main focused28/28, full Node144/144 and critical gate71tasks/1m11s PASS;290/290GameTests at2026-09-05 16:23:22.644+05, session22719 terminal exit0. Exact assignment compiler handles1/2worlds, all13current lanes/5crash windows and trusted caller identities; crash arms are metadata, executable halves validate. Real CI consumes preflight but still launches clients per lane. Executor idle/all grants closed; no native matrix/provider/deployment run.
- Last order `docs/work-orders/PM-F0VA-CI-COMPOSITION-01.md` revision 1 ACCEPTED by main Gate C. Eight allowed harness files changed: schema-2 strict projection evidence, shared HOT/COLD and distinct-arrival comparison, separate serial/parallel admission, timing identity binding. Main full Node137/137 and exact critical Gradle gate pass (71 tasks,1m12s;290/290GameTests at2026-09-05 15:59:43.304+05). Session34561 terminal exit0; JAR41ac2b…82d53 unchanged. Only generated core-game-test output regenerated; no native matrix/provider run. Executor idle, all grants closed pending next order.
- Last order `docs/work-orders/PM-F0VA-CI-BOOTSTRAP-01.md` revision1 ACCEPTED by Gate C: three job forms bootstrap private Gradle homes before unchanged offline preparation, worker admission first. Regression red11/12 before fix then green with adverse mutations; main full Node130/130 and Terra guardrails check67tasks/16s pass. Exactly2workflows+ci-matrix.test.mjs changed. No actual bootstrap/native/remote run. Executor idle; grants closed pending next order. Earlier critical gate passes290/290GameTests,71tasks, artifact41ac2b…82d53.
- Last order `docs/work-orders/PM-F0VA-TIMING-POLICY-01.md` revision1 ACCEPTED by engineer Gate C. Terra changed only timing.mjs/timing.test.mjs: version2 advisory target, exact medians/targetMet, strict identity/invalid-sample/nonpositive-benefit rejection. Focused tests/diff checks and Terra `guardrails check` pass (67 tasks,17 s). Its RCON mock was denied loopback listen EPERM; main ran identical full Node suite in allowed environment:129/129 pass, none skipped. Executor idle; write/test grant closed pending next approved order. No native/preparation ran.
- Current mandatory slice F0.VA inside F0.V. F0.1 harvest and MAT-004 patrol M0 WIP remain paused/preserved. Open: local four-worker evidence, actual provider >=2.5x and current critical/package/final evidence. Restart-benefit target gap is no longer a blocker. Aggregate5.07x cannot replace correctness/provider gates.
- Original `build/frontier-v3-scenarios/f0v-timing-smoke-persistent-disconnect.json` failed the former25-percent policy at22.52% (medians69373.518 ->53748.945 ms), six individually successful samples on older artifact ecfe42…ff4e. User accepts that measured benefit; old failure files stay unchanged. No rerun to reach25 percent is authorized or needed. This is not current-source correctness acceptance.
- No commit, migration, push, deployment, live-server reset or v2 removal is authorized by the current verification order. Historical live-world facts are in the archive; discover current config/processes before any approved operation.

### Next
- Finish the active bounded expected-server-crash protocol and independently review Gate B before full verification. Then issue actual assigned-matrix runner integration and narrow native proof orders. Arm/causal crash observation/exact owned JVM exit/client connection-loss acknowledgement/port closure must not alias normal disconnect or terminal success; recovery requires exact replacement ready and same living client. Scoped implementation/focused tests are granted; full/native execution is not. Complete F0.VA.1 and provider proof remain open.
- After deterministic CI blockers, consider one fresh V3-AUD-052 arrival_checkpoint_one corrective native proof. Generic demanded-candidate selector is implemented and its focused GameTest passes; this is not F0.1 breadth. Full original matrix remains paused; actual provider proof is still required after migration.
- Finish local F0.VA gates, then separate verified non-squashed monorepo/push order to `git@github.com:RidiculousCircumstances/pale_mirror.git`, then actual four-worker provider/pinned-host proof. UNCONFIRMED_EXTERNAL does not waive this sequence.
- After F0.VA acceptance resume original F0.V native/crash matrix; after F0.V acceptance resume preserved F0.1, then F0.2–F0.6. No MAT breadth before F0 closure; remaining waves and human M3 gates remain required.
- Execution-semantics alignment: F0.V family comparator/knowledge/custody descriptors; F0.1 shared partial-work handoff; F0.2 independent physical eligibility/knowledge/aftermath; F0.3 observer-free transfers/conservation; F0.4 cross-front navigation/decision authority; F0.5 save-window confirmation/fencing; F0.6 calibration/rapid-switch neutrality. These are unproved family exits, not closed by timing tests.

## Open questions
- The checkout still has no configured Git remote and provider evidence remains `UNCONFIRMED_EXTERNAL`. The destination and resolution are now fixed: after local F0.VA closure, migrate both histories without squash, push verified `main` to `git@github.com:RidiculousCircumstances/pale_mirror.git`, then run the actual four-worker and pinned-host gate. SSH/provider availability remains operationally unconfirmed until that step executes.
- Future Create integration still requires explicit selection/pinning of compatible Create add-ons, exact rail topology, recipes, energy ratios and industrial balance after graybox validation; road freight and aeronautics are not open scope in the accepted first phase.
- Product decision needed before the next finance slice: should an operational settlement grant its local company a bounded interest-free working-capital credit facility with fixed daily repayment, where a missed payment blocks new work but does not cancel confirmed wages/shipments, and recovery is only exact repayment or recapitalization? No credit/debt implementation is authorized until this rule is accepted or replaced.
- Exact farm growth/yield timing, infection/territory tuning and final HOT actor budgets remain profile calibration work; product validation still needs real-display, clean-room and cooperative-player v3 audits. The accepted per-owner lane rule now permits one strategic objective plus one facility harvest objective. The restart runner has complete graceful and abrupt two-server same-world evidence: both pilots pass the `GROWING` field assertion after durable save or exact-JVM crash/restart; an entirely absent confirmed field is rebuilt only from complete baseline, while partial/foreign state remains visible conflict.
- Context-card player-visible evidence now exists for one V3 field board and the new quarantine card; a local physical-board frame now proves the new compact/occluded grammar. Future UX review should verify other object types (hive organ, damaged structure and route) and judge broader world navigation, information density and landmark placement with unbriefed players.
- The operation-local caption/camera grammar has narrow evidence for one HOT caravan, but broader product validation remains open: unbriefed players must still judge navigation, information density, ambient crowding and the visual grammar for other operation types (combat, hive assaults, damaged structures and field work).
- The defender scenario proves that the derived nameplate is genuinely rendered,
  not merely stored as an entity name. It is still narrow correctness evidence:
  the vanilla-scale label is not yet the accepted large, bright tactical visual
  grammar for unbriefed players. Establish that reusable visual layer before
  claiming broader human-combat comprehension.
## Working set
- `docs/engineering-agent-protocol.md`, `docs/work-orders/TEMPLATE.md`, `docs/work-orders/PM-HANDOFF-01.md`, `docs/work-orders/PM-F0VA-VERIFY-01.md`.
- `architecture.yml`; v3 contract, execution-semantics, implementation plan, seamless foundation, integration-feedback foundation, accelerated-verification loop, Terra brief.
- Inherited Java/test-pilot WIP and exact current21 timing/native/prepared/tier evidence; historical failure output must not be overwritten.
- `docs/archive/CONTINUITY_2026-09-05_engineer_handoff.md` preserves all transferred ledger facts, not a second active ledger.
