# PM-F0VA-VERIFY-01: reconcile current F0.VA evidence and restart timing

Specification revision: 1. Parent slice: F0.VA/F0.V reference timing.
Risk: verification-only of inherited critical-code WIP; no source edit grant.
Engineer: /root. Executor: /root/terra_executor, gpt-5.6-terra high.
Specification is READY for Phase A review; active grants are recorded only in
`CONTINUITY.md`. Phase B is not granted by this document alone.

## Superseding user decision, 2026-09-05

The user accepts the22.52-percent benefit and does not want effort spent on the
gap to25 percent. Phase B preparation/native grant was REVOKED before either
started. Only diff check and guardrails ran and passed. Phase A inventory is
accepted as scoped evidence; AC-5's hard25-percent criterion below is historical
and superseded, not falsely reported passed. The former run's failure files
remain unchanged. Current-source correctness/recovery and other F0.VA gates
remain open. Follow-up `PM-F0VA-TIMING-POLICY-01.md` aligns the executable
comparison policy without another native benchmark. The remaining sections
retain the original order for audit, not active launch authority.

## Prerequisites and baseline

- PM-HANDOFF-01 accepted, previous writer acknowledgement and no active
  conflicting writer/measurement. Preserve both Git histories and all WIP.
- Engineer finishes governance links before preparation. The current21 timing
  must reach terminal output first; keep both its success and failure evidence.
- Read AGENTS, engineering protocol, current F0.VA/F0.V briefs and execution
  semantics. Do not reinterpret a historical report as current final evidence.

## Outcome

Record precisely which inherited F0.VA exits are proved, then measure the
existing graceful-restart reference at one new prepared identity. This order
does not close F0.VA, run the full original harvest/crash matrix, migrate the
repositories or introduce new gameplay/tooling.

## Engineer decisions already made

The current final-stop path waits for an identity-bound durable save signal,
owned server exit and actual port closure before writing the successful native
manifest. Its omission from the common barrier journal is not evidence of lost
save confirmation. Do not change terminal-state meaning or require 30 rather
than 28 events under this order. Record this as a limited evidence projection,
not a manufactured mandatory runtime defect.

Prepared `sourceContent` and verification-plan `source` use different input
inventories. Compare each only within its own namespace. A mismatch between
those two hashes is not drift. Existing executed baseline T0/T1 tests count for
their asserted boundaries; cached candidate evidence is assessed by its exact
key and referenced terminal proof, not by an outdated ledger summary.

## Phase A: bounded evidence review

No fresh expensive run yet. Submit:

- current21 terminal timing result and all three workflow comparisons, exact
  environment/source/build identities and any failure; no partial median claim;
- six-capability/exit matrix with code, executed/cached proof paths and missing
  physical/provider evidence; explicit same-key legal candidate reuse check;
- family-to-requirement alignment required by execution semantics: exact versus
  calibrated comparison, custody/knowledge capabilities and owning future F0
  slice for every unproved guarantee. Keep this an inventory, not new behavior;
- baseline for the original restart reference, including the retained 22.52%
  failed measurement and why rerunning the current implementation is justified.

Engineer reviews Phase A before authorizing Phase B. The supervisor updates
normative docs/ledger; executor submits facts rather than writing them.

## Phase B: authorized commands and execution scope

After the explicit VERIFYING grant, run from the nested source repository:

```bash
git diff --check
./gradlew guardrails
node tools/frontier-v3-test-pilot/src/prepare-native-build.mjs --output=build/pm-f0va-verify-01-r1/prepared-build.json
```

Confirm the output path is absent, preparation succeeds and source/launch
identity is stable. Reserve the one visible DISPLAY=:0 and the runner's exact
disposable world/port scope after reading its current preflight. Then run:

```bash
DISPLAY=:0 node tools/frontier-v3-test-pilot/src/run-f0v-timing.mjs tools/frontier-v3-test-pilot/scenarios/disposable-lite-smoke-graceful-restart.json build/pm-f0va-verify-01-r1/prepared-build.json build/pm-f0va-verify-01-r1/graceful-timing.json
```

The command owns three baseline and three candidate samples. Do not start a
second runner, background build or provider job concurrently. Do not overwrite
an existing report, reuse a mutable successful world, relax timeout/assertions,
change the reference, or amend source/docs during the measurement.

## Acceptance matrix

| ID | Required evidence |
| --- | --- |
| AC-1 | Full six-capability matrix distinguishes implementation, local proof, timing and actual GitHub-provider evidence; no invented closure. |
| AC-2 | Prepared and verification source namespaces matched correctly; legal candidate reuse and executed native evidence have exact references. |
| AC-3 | Execution-semantics family gap map assigns all missing guarantees to the existing F0 slices without premature implementation. |
| AC-4 | Docs guardrails pass; native preparation produces one exact new identity. |
| AC-5 | Six successful same-reference restart samples, exact two-server same-world recovery and candidate client-JVM reuse; comparison meets the original >=25% median requirement. |

If AC-5 fails, submit the retained phase timings and failure bundle. That is a
useful diagnosis, not acceptance: no automatic retry loop or source fix. Engineer
defines a narrow correction order with a regression before the next native run.
The aggregate >=3x and actual provider >=2.5x gates remain independently required.

## Phase A evidence reviewed on 2026-09-05

Current21 feedback is terminal `ok`: 18 samples, 5.065026x aggregate; three class
ratios are 5.012120x, 5.113490x and 5.070469x. Prepared and plan-source namespaces
are correctly distinguished. Candidate lifecycle_restart-2 reused SUCCESS
T0/T1/T2 under matching plan/source/artifact/classpaths/dependency manifest,
with null fixture identity; native T3 executed. Current21 baseline T0–T3 all
executed. This establishes scoped iterative-feedback evidence, not fresh T4.

Six capabilities: VA.1/.4 use current21 persistent-native evidence; VA.2/.3
have executed policy tests and exact-key reuse/selector evidence; VA.5 lacks
actual provider proof; VA.6 bootstrap r2/r3 and consumer r1/r2 are historical
development evidence, not a final/current-identity gate.

The original restart reference has six individually successful historical
samples but failed its aggregate target at22.52%, artifact ecfe42…ff4e. Current21
uses artifact41ac2b…82d53, but an artifact change alone does not establish a
causal fix. Additional review traced the original runner's identity-bound
disconnect/save/exit and persistent recovery path, but the old report lacks
source-content inventory, so no change-based performance fix is claimed.
Historical phase medians show the candidate needs about1719 ms less total
time to reach25%; phase intervals overlap and must not be summed as disjoint.

Engineer decision: allow exactly one new six-sample Phase B acceptance
measurement at a freshly prepared current identity, not a retry asserting a
known fix. All old samples completed correctly; the failure was the numeric
floor. Keep that failure visible, all assertions and the threshold unchanged.
If the new comparison fails, stop and diagnose its retained phases before any
further native attempt. Do not report the new run as proof of a causal fix.
The concrete family gap inventory is `PM-F0VA-SEMANTICS-GAPS.md`; its future
descriptor/family corrections remain outside this verification-only order.

## Scope and stop rules

Allowed writes are generated outputs of the approved tools in their documented
build/test-runtime paths. No source, tests, schema, scripts, configuration,
workflow, docs or ledger changes by the executor under this order. No commits,
push, deployment, migration, live-server reset or v2 removal. Unexpected current
writers, unavailable display/permissions, changed input identity or requirement
contradiction stops the affected phase and is reported to the engineer.
