# PM-F0VA-NATIVE-QUAL-01: qualify the accepted persistent runner

Specification revision: 1. Parent: F0.VA persistent-runner adoption.
Risk: native verification of accepted critical-code WIP; no source edit grant.
Engineer: `/root`. Operator: `/root/terra_crash_completion`, Terra high.
State: ACCEPTED — revision15 pair and unchanged three-world benchmark.
All source/full/native/benchmark/external grants CLOSED.

## Three-world terminal acceptance

Result build/pm-f0va-native-qual-01-r15/three-world-benchmark.json OK104.21s,
SHA03e8c7859beb455f1a077d0518f3338bfc5cdba3c666e3680832077262e9d250.
Main inspected3worldkeys/4serverruns/client3939157/4terminal results/28events,
including exact graceful disconnect/save/reconnect, and finalStop3942574
durableSave/portClosed. Main verified recorded four server/client PIDs absent
and four owned ports closed. Terra reports runner cleanup, no failure bundle,
both diff checks PASS/34guardrails18s and dirty210/1; successful worlds cleaned
by existing runner. Source61ba53d8/JAR e4eefc3d unchanged before/after per actual
requirePreparedF0vBuild. Main independently compared embedded build with r15
prepared JSON: exact equality. Local lifecycle hash1791daa5 is sha256Json(build),
not portable assigned-worker hashaf4e33c9; differing namespaces are not drift.
This order is ACCEPTED for persistent-runner correctness, not a measured new
speedup, provider/F0.VA closure or harvest/M2/M3 acceptance. Next is read-only
local exit audit, not automatic migration or original F0.V execution.

## Revision15 acceptance and benchmark grant

Main inspected prepared/plan/result and subsidiary manifest. Result
build/pm-f0va-native-qual-01-r15/worker-3.json is OK175.47s, exact2lanes,
plan f9227169967028ddf04b7dd5b320097d01485db510d1d82e7957b1f057ea3fe2,
build af4e33c966548ca61cbb12d4c13fdd9bed389a42a9cff45611ce276c8aee1fa7,
source61ba53d8b73095aed18db3a8cfaf012fe03ad4cd8c86fa37777de117c16d2e72.
Manifest build/frontier-v3-ci-shards/worker-3-b9c9699f-0123-4c7b-8652-2b987106971d/persistent-matrix.json
proves2worldkeys/4serverruns/one client3918740/36events/3terminal results.
Crash epoch0 prefix4 binds release boundary, exact job/payload/revision2425,
four observations and same-world ready successor epoch1. Both declared original
lane projections are retained; HARVESTING/complete=false proves this declared
recovery checkpoint, NOT completed harvest or M2/M3. FinalStop names server
3925196 with durableSave/portClosed true. Main checked all four server PIDs and
client absent, four ports closed. Terra reports all runner cleanup, diff checks
PASS and dirty210/1; successful disposable worlds removed by existing runner,
not retained like failed worlds. Pair ACCEPTED; failed r1–14 remain failed.

Terra exclusively runs the unchanged3world benchmark from nested repository:

```bash
DISPLAY=:0 XAUTHORITY=<rediscovered-current-authority> \
FRONTIER_V3_PILOT_WORKER_ID=local-f0va FRONTIER_V3_PILOT_PORT=25641 \
FRONTIER_V3_PREPARED_BUILD_IDENTITY=build/pm-f0va-native-qual-01-r15/prepared-build.json \
node tools/frontier-v3-test-pilot/src/run-f0va-persistent-matrix.mjs \
  build/pm-f0va-native-qual-01-r15/three-world-benchmark.json
```

Before launch run both current-doc diff checks/guardrails, revalidate the SAME
prepared source/artifact/classpath identity, verify exclusive output and actual
process/disk/display/ports. Governance work-order/ledger updates are outside
PREPARED_SOURCE_PREFIXES; no identity-bearing source/scenario/contract change.
If any fingerprint differs STOP; do not silently reprepare or substitute input.
One client DISPLAY=:0; ports25641/42; existing unchanged scenario/world lifecycle.
No worker3 rerun, source/helper/debug edits, clean, live/deploy/commit/migration/
push. First failure preserves owned evidence and proves cleanup, no retry.
Require terminal OK,3independent worldkeys,1client throughout expected4server
runs, final durable stop/owned exit/closed ports and no identity drift/failure
bundle. This is local persistent-runner qualification, not four-provider proof,
original F0.V or human/product acceptance. Main reviews before next authority.

## Revision15 operator amendment

Final-coverage Gate C accepts exact runtime assertion coverage distinct from
terminal result records and rejects same-length result substitution, with genuine
worker3 compiler/reader composition. Terra exclusively operates the unchanged
worker3 abrupt release-boundary plus graceful pair using new exclusively created
build/pm-f0va-native-qual-01-r15/ for all prepared/plan/result paths below.
Current-doc both diff checks/guardrails precede fresh identity. Verify13-lane
plan/exact worker3 pair/JARe4eefc3d; rediscover process/disk/display/XAUTHORITY
and closed ports. One visible client DISPLAY=:0, ports25631/32, two independent
disposable worlds. No source/scenario/helper/debug/export edits, clean or retry.
First failure retains worlds/bundle/logs and proves owned cleanup, then read-only
diagnosis. No benchmark/live/deploy/commit/push/migration. Main accepts terminal
pair before separate benchmark. Historical failed measurements remain failed;
all unchanged parent safety/acceptance/exact commands apply with r15 paths.

## Revision14 terminal review

Failed173.21s; no accepted lanes, crash receipt remains unaccepted. Result
build/pm-f0va-native-qual-01-r14/worker-3.json binds plan7b71f674/build71049eee.
Bundle build/frontier-v3-ci-shards/worker-3-98afce72-5f9a-431d-8798-f2375a2953c8/persistent-matrix.failure/bundle.json.
Main inspected final event36 assertionCount4 and stack; validator passes earlier
checks but expects3 non-crash segments, whereas the producer validates/counts
all4runtime segments. Distinct terminal-result coverage remains3. Main verified
PIDs3868208/3868806/3871107/3872380/3875094/3869230/3869245 absent and four ports
closed. Both worlds retained per Terra. No retry/benchmark; bounded composition
correction must retain terminal-domain and crash authentication requirements.

## Revision14 operator amendment

Canonical-release Gate C accepts order-independent successor construction with
strict authentication and genuine final-reader regressions. Terra exclusively
operates the exact worker3 abrupt release-boundary plus graceful pair under
new exclusively created build/pm-f0va-native-qual-01-r14/ for all prepared,
plan and result paths below. Current-doc diff checks/guardrails precede fresh
identity; verify13-lane plan, exact worker3 pair and JARe4eefc3d. Rediscover
process/disk/display/XAUTHORITY and closed ports; one visible client DISPLAY=:0,
ports25631/32, two independent disposable worlds. No source/scenario/helper/
debug/export edits, clean or retry. First failure stops, retains worlds/bundle/
logs, proves owned cleanup, then read-only diagnosis. Benchmark/live/deploy/
commit/push/migration remain closed. Main reviews actual terminal pair before
separate benchmark; historical failed measurements stay failed. All unchanged
parent safety, acceptance and exact command rules apply with r14 output paths.

## Revision13 terminal review

Result build/pm-f0va-native-qual-01-r13/worker-3.json failed161.89s with no
accepted lanes. Plan44e1364f/buildc29ea2cc bind the attempt. Bundle:
build/frontier-v3-ci-shards/worker-3-59dc7d52-585d-402f-a304-f515ee74ca4a/persistent-matrix.failure/bundle.json.
Main inspected36 lifecycle events: abrupt prefix4/arm/fire/owned exit/recovery,
graceful handoff/recovery, same client JVM3823647 and four terminal assertions.
Final matrix validator rejects the release as foreign or stale; whole-pair
acceptance and crash receipt remain absent. Terra traced equal successor fields
with differing object insertion orders through clone/hash/JSON comparison.
Main verified six recorded server/client PIDs absent and all four owned ports
closed. Both worlds retained per Terra. No retry or benchmark; bounded Node
canonical-release correction must retain strict authentication and regressions.

## Revision13 operator amendment

Assigned-terminal Gate C accepts genuine compiled-lane terminal/COLD validation
with direct negative-reader and membership/offset regressions. Terra exclusively
operates the same exact worker3 abrupt release-boundary plus graceful pair,
using exclusively created build/pm-f0va-native-qual-01-r13/ for every preparation,
plan and shard path below. Both current-doc repo diff checks/guardrails first,
then fresh identity; verify13-lane plan/worker3 assignment and JARe4eefc3d.
Rediscover processes/disk/display/XAUTHORITY and closed ports; one visible client
DISPLAY=:0, ports25631/32, two independent disposable worlds. No source/scenario/
debug/export/helper edits during measurement. First failure stops, retains
worlds/bundles/logs and proves owned cleanup; then read-only causal review.
No module clean/retry/benchmark/live/deploy/commit/push/migration. Main accepts
terminal pair before separate benchmark. Unchanged safety/acceptance rules apply;
all historical grants closed and failed runs stay failed.

## Revision12 terminal review

Result build/pm-f0va-native-qual-01-r12/worker-3.json failed172.98s, no accepted
lanes. Bundle build/frontier-v3-ci-shards/worker-3-e891f537-055a-469d-a7a1-62858e2df12c/persistent-matrix.failure/bundle.json.
Main inspected35lifecycle events: abrupt prefix4, owned crash, same-client
reconnect/recovery2actions, graceful world pre3/recovery2actions and final normal
disconnect, across four server runs/two world keys/client JVM3751808. Terra
reports exact release marker2424. Terminal original-lane validator then throws
TypeError reading f0vTerminalProjections from absent lane.scenario (line651).
Compiled workerPlan.lanes intentionally retain segments rather than scenario.
Crash receipt remains null; terminal projections/whole pair not accepted.
Main verified PIDs3751277/3753378/3754712/3757282/3751777/3751808 absent and all
four owned ports closed; first world present, Terra confirmed both retained.
Plan c3cf7206/build426a5356 bind this attempt. Assigned-terminal order addresses
actual compiler→validator composition; no blind retry/benchmark/source grant here.

## Revision12 operator amendment

Reconnect-departure revision3 Gate C supplies common authenticated graceful and
abrupt cleanup, including repeated cross-world regression. Terra exclusively
operates the exact worker3 abrupt release-boundary plus graceful pair, using
new exclusive build/pm-f0va-native-qual-01-r12/ for every prepare/plan/shard path
in exact execution below. Both current-doc repo diff checks and guardrails precede
fresh identity; verify13-lane plan, unchanged worker3 pair and JARe4eefc3d.
Rediscover actual process/disk/display authority and closed ports; report changed
XAUTHORITY. One visible client DISPLAY=:0, ports25631/32, two independent worlds.
No source/scenario/debug/export/helper changes during measurement. First failure
stops, preserves bundles/worlds/logs and verifies owned cleanup; then read-only
causal review. No blind retry, module clean, benchmark/live/deploy/commit/push/
migration. Main accepts terminal pair before separate benchmark. All unchanged
parent acceptance/safety rules apply; historical grants remain closed.

## Revision11 terminal review

Result build/pm-f0va-native-qual-01-r11/worker-3.json failed106.43s, no terminal
completed lanes. Bundle build/frontier-v3-ci-shards/worker-3-fd920e44-ab58-4277-9d2b-5373fc985187/persistent-matrix.failure/bundle.json.
Main inspected marker2423 at01:24:45 and lifecycle events14–20: same-client
reconnect, two recovery action checkpoints, segment completion, normal disconnect,
durable save and closed port. Event21 starts the next independent world.
Failure01:25:24 is its graceful inter-world reconnect cleanup, not the earlier
abrupt reconnect. Overall crash receipt is null and pair unaccepted; preserve
this partial evidence without claiming F0.VA closure.
Main verified recorded server/wrapper PIDs absent, four owned ports closed,
first retained world present and saves01:25:07/01:25:32. Terra also verified
client JVM/runner cleanup. Plan4e1162f0/buildc03a28cd bind this terminal attempt.
Cause: valid graceful predecessor lacks expected-crash claim, so bounded null
cleanup still rejects it. Reconnect-departure revision3 addresses both valid
transition kinds; no native retry or benchmark is granted.

## Revision11 operator amendment

Reconnect-departure revision2 Gate C accepts authenticated synchronous cleanup
classification with normal/exceptional and independent retirement regressions.
Terra exclusively operates one worker3 abrupt release-boundary plus graceful
pair, substituting exclusively created build/pm-f0va-native-qual-01-r11/ for
every preparation/plan/shard path in exact execution below. First both repository
diff checks and guardrails; then fresh identity using existing checked-in tools.
Verify exact13-lane plan, unchanged worker3 pair and JAR408df7fe identity.

Rediscover process/disk/display authority and closed owned ports before launch;
report changed XAUTHORITY. At most one visible client DISPLAY=:0, ports25631/32,
two independent disposable worlds, unchanged crash/protocol/scenario semantics.
No prior debug/export/one-off inputs or source edits during measurement.
First failure stops, preserves bundle/world/logs and proves owned cleanup,
followed only by read-only causal review. No automatic retry, module clean,
benchmark, live/deploy/commit/migration/push. Report fresh identities and terminal
crash/recovery/domain evidence. Main accepts pair before separate benchmark.
Historical grants below remain closed; unchanged safety/acceptance rules apply.

## Revision10 terminal review

Worker result build/pm-f0va-native-qual-01-r10/worker-3.json failed79.73s;
no completed lanes. Main inspected the actual result and client stack:
persistent_reconnect_lost at01:00:53 during ConnectScreen.startConnecting.
Terra reports exact release marker2433 and authenticated expected loss, followed
by recovery-server readiness; no recovery receipt or graceful success.
Bundle: build/frontier-v3-ci-shards/worker-3-cbf43862-94ee-4f89-be57-bbb568096d31/persistent-matrix.failure/bundle.json.
Main verified PIDs3639910/3642265/3640445/3640458 absent and all four owned ports
closed. Logical plan8fd05b8a/buildc16e77cb remain bound in terminal result.
Terra traced NeoForge logout's null connection after the old player disappears.
Revision2 of PM-F0VA-NATIVE-RECONNECT-DEPARTURE-01 supersedes its overstrict null
rule; no blind retry, timing run or claim of native recovery is authorized.

## Revision10 operator amendment

PM-F0VA-NATIVE-RECONNECT-DEPARTURE-01 Gate C accepts exact predecessor-departure
classification. Terra operates one exact worker3 abrupt release-boundary plus
graceful pair using exclusively created build/pm-f0va-native-qual-01-r10/ for
every preparation/plan/shard output below. First run both docs diff checks and
guardrails, then fresh identity preparation with existing checked-in tools.
Verify exact13-lane plan/worker3 assignment and new JAR088e2f7a identity.

Rediscover real process/disk/display authority and closed ports before launch.
Report changed XAUTHORITY before visible client launch. Terra exclusively owns
ports25631/25632, at most one client DISPLAY=:0, two independent disposable
worlds and unchanged persistent-client/crash protocol. No debug/export/one-off
diagnostic helper inputs. Source/scenarios/acceptance frozen during attempt.

First failure stops: preserve bundle/world/logs and prove owned cleanup; then
read-only cause review. No automatic retry, module clean, benchmark, live
service/deployment, commit, migration or push. Report fresh source/build/plan,
run/PID identities and exact terminal crash/recovery/domain assertions.
All unchanged parent safety/acceptance rules remain binding; main accepts
terminal pair before any separately granted benchmark.

## Revision9 terminal review

Worker result `build/pm-f0va-native-qual-01-r9/worker-3.json` failed77.87s,
no completed lanes. Bundle:
`build/frontier-v3-ci-shards/worker-3-be1f7dac-dc63-45e0-9cbb-6c730375aeeb/persistent-matrix.failure/bundle.json`.
Main verified exact release crash marker revision2425 at00:35:49 and lifecycle
sequence1–13: same completed prefix1–4, armed loss, controller fire, owned server
exit, expected client loss, closed port, recovery server ready. The client fails
at00:36:01 with persistent_reconnect_lost inside ConnectScreen.startConnecting.
Recovery connection/receipt never completed; process.crash is null in terminal
bundle, so do not claim full native recovery or graceful-lane success.
Main confirmed both server PIDs3578632/3580748 and client JVM3579102/wrapper3579071
absent, all four owned ports closed, retained world present and all-dimensions
save00:36:07. Terra reviews reconnect classification read-only; no retry/source
edit/benchmark is granted. Source6850d4b2, builda5474477, plan031058af remain
bound in prepared identity/result and retained lifecycle.

## Revision9 operator amendment

`PM-F0VA-NATIVE-HANDOFF-WITNESS-01` Gate C supplies the regression-proved
Prepared/Handoff release-witness correction. Terra operates one exact worker3
pair, substituting exclusively created `build/pm-f0va-native-qual-01-r9/` in
every preparation/plan/shard path below. First run both docs diff checks and
guardrails, then prepare fresh identity using existing tools. Verify exact
13-lane plan and worker3's abrupt release-boundary plus graceful assignment.

Rediscover actual processes, disk, display authority and closed ports before
launch; report changed XAUTHORITY before client launch. Terra exclusively owns
ports25631/25632 and at most one visible client on DISPLAY=:0. Two independent
disposable worlds, existing persistent client and unchanged crash semantics.
No prior diagnostic logging/export/helper inputs. Source/scenarios remain frozen.
First failed acceptance stops the attempt; retain bundle/world/logs and prove
owned cleanup, then read-only causal review. No automatic retry, module clean,
benchmark, live service/deployment, commit, migration or push. Report source,
build/logical-plan/run/PID identities and terminal domain/recovery evidence.
All unchanged parent acceptance and safety rules apply. Main reviews terminal
pair evidence before granting any separate benchmark.

## Revision8 terminal review

Worker result `build/pm-f0va-native-qual-01-r8/worker-3.json` is failed,
342.85seconds, no completed lanes. Retained bundle:
`build/frontier-v3-ci-shards/worker-3-30c70c28-4a3a-4c3d-a5da-9858fecd7eb0/persistent-matrix.failure/bundle.json`.
The preflight no longer blocks launch; real release trace revision2424 at
00:05:31 still lacks the crash marker. Client exited before its crash boundary;
graceful lane and benchmark did not run. All-dimensions save at00:10:14 retained.
Main independently inspected result/log and confirmed recorded PIDs
3502483/3503095/3503737 absent and all four owned ports closed. Terra investigates
the causal mismatch read-only and proposes a bounded correction with regression;
no retry, source edit or new native diagnostic is granted by this record.

## Revision8 operator amendment

`PM-F0VA-NATIVE-MIXIN-PREFLIGHT-01` Gate C corrects the proven false-negative
preflight; typed crash identity/ambiguity correction remains unchanged.
Terra operates the exact worker3 pair below using exclusively created output
root `build/pm-f0va-native-qual-01-r8/` in all preparation/plan/shard paths.
Run both documentation diff checks/guardrails before fresh identity preparation.
Verify actual process/disk/display authority and closed qualification ports;
derive launch inputs through existing preparation/runner tools, not the prior
one-off diagnostic helper. No debug-export/logging overrides from diagnosis.
Retain all prior worlds, exports and bundles; no clean/reset or source edits.

The13-lane plan must assign worker3 only the exact abrupt-release and graceful
pair. Terra owns ports25631/25632 and at most one visible client on DISPLAY=:0;
discover/validate current XAUTHORITY and report any changed value before launch.
One qualification attempt; stop at first failure, retain bundle and prove owned
cleanup. Existing runner controls its own process groups/world lifecycle only.
Report fresh source/build/logical-plan/run/PID identities and actual terminal
domain/recovery results. Benchmark, live service, deployment, commit, migration
and push remain ungranted. Main reviews terminal evidence before the next step.
All unchanged parent acceptance and cleanup rules remain binding.

## Revision7 operator amendment

`PM-F0VA-NATIVE-CRASH-IDENTITY-01` Gate C supplies the matching regression and
early applied-mixin preflight. The assigned Terra executor now operates the
same exact worker3 qualification below, substituting output root
`build/pm-f0va-native-qual-01-r7/` in every preparation/plan/shard path. This is
an explicit run grant, not source-write authority. First run documentation
diff/guardrails, inspect both repository states, actual display authority,
process ownership and ports; prepare fresh identity and verify exact assignment
before launch. If the recorded display authority has changed, report the
discovered exact replacement before client launch. No module clean or world
reset. Only the existing runner may create its new task-owned disposable world.

Terra owns qualification ports25631/25632 and at most one visible client on
DISPLAY=:0; no main competing heavy run. Preserve revision6 world and all
earlier bundles. Stop on the first failure, retain bundle/logs and confirm
owned cleanup; diagnose read-only without retry or source changes. Report
terminal results with source/build/plan/run/PID identities and remaining gaps.
Main verifies the actual result before granting the separate three-world
benchmark. The benchmark is NOT authorized by this revision7 grant.

Historical operator lines below describe revisions1–6. All unchanged safety,
acceptance and command semantics remain binding for revision7.

## Outcome and non-goals

Prove that the accepted assigned-runner source works against real NeoForge
server/client processes before repository migration. First execute the exact
`worker-3` assignment: its abrupt
`release_durable_before_cold_resumption` lane and graceful-restart lane must use
two independent disposable worlds and one persistent visible client. Then run
the unchanged local persistent benchmark with three independent worlds and one
client. This is local F0.VA qualification, not four-worker/provider proof and
not original F0.V closure.

Do not edit source, tests, scripts, workflows, configuration or scenarios. Do
not touch the live `far-frontier-v3-live.service`, its world, deployment or
hosted artifacts. Do not commit, migrate or push. The accepted 22.52-percent
restart benefit is not rerun; 25 percent remains advisory.

## Discovered ownership and inputs

- Repository HEAD before launch:
  `0ac6f97a695d636ae928313de7a807d1f3ea05a1`; inherited dirty WIP is retained.
- Root repository HEAD:
  `891a4dfe1a613857124775696973d065cc9399cb`; inherited
  `.f0v-baseline/` is retained.
- Visible display: `DISPLAY=:0`, exact current authority
  `/run/user/1000/.mutter-Xwaylandauth.AZ4VT3`; `xdpyinfo` must pass immediately
  before each client-bearing run.
- Assigned qualification owns ports `25631/25632`; benchmark owns
  `25641/25642`. All four must be closed before launch and after cleanup.
- Retained outputs are under `build/pm-f0va-native-qual-01/`. Every output uses
  exclusive creation; an existing path stops the order rather than being
  overwritten.
- The already running production server and artifact hosts are unrelated and
  remain untouched. At most one qualification client and one qualification
  server may exist at a time.

## Exact execution

From the nested repository, first run the post-documentation gate and prepare
one current immutable input:

```bash
git diff --check
./gradlew guardrails
node tools/frontier-v3-test-pilot/src/prepare-native-build.mjs \
  --output=build/pm-f0va-native-qual-01/prepared-build.json
node tools/frontier-v3-test-pilot/src/write-ci-matrix-plan.mjs \
  --prepared=build/pm-f0va-native-qual-01/prepared-build.json \
  --output=build/pm-f0va-native-qual-01/ci-plan.json
```

Verify the prepared source/artifact/classpath identity, exact 13-lane plan and
that `worker-3` owns only the declared abrupt and graceful lanes. Then run:

```bash
DISPLAY=:0 XAUTHORITY=/run/user/1000/.mutter-Xwaylandauth.AZ4VT3 \
FRONTIER_V3_PILOT_WORKER_ID=worker-3 FRONTIER_V3_PILOT_PORT=25631 \
node tools/frontier-v3-test-pilot/src/run-ci-matrix-shard.mjs \
  --plan=build/pm-f0va-native-qual-01/ci-plan.json \
  --prepared=build/pm-f0va-native-qual-01/prepared-build.json \
  --worker=worker-3 --measurement=correctness-1 \
  --output=build/pm-f0va-native-qual-01/worker-3.json
```

Only after its terminal evidence and cleanup are accepted, execute the
unchanged three-world benchmark with the same prepared identity:

```bash
DISPLAY=:0 XAUTHORITY=/run/user/1000/.mutter-Xwaylandauth.AZ4VT3 \
FRONTIER_V3_PILOT_WORKER_ID=local-f0va FRONTIER_V3_PILOT_PORT=25641 \
FRONTIER_V3_PREPARED_BUILD_IDENTITY=build/pm-f0va-native-qual-01/prepared-build.json \
node tools/frontier-v3-test-pilot/src/run-f0va-persistent-matrix.mjs \
  build/pm-f0va-native-qual-01/three-world-benchmark.json
```

## Acceptance

1. Prepared identity is current before and after both runs; exactly one normal
   packaged JAR exists and its hash is retained.
2. Worker result is terminal `ok`, exact worker/measurement/plan/build hashes
   match, both lanes are complete, and the subsidiary native manifest proves
   two world keys, one non-null invariant client PID, typed abrupt arm/proof/
   release with the same authenticated completed prefix, recovery successor,
   graceful restart and terminal assertions.
3. Benchmark result is terminal `ok` and still proves three independent world
   keys, one client PID through every lifecycle, expected server-run count,
   durable final stop, owned exit and closed ports.
4. No failure bundle, unowned Java/client process, occupied owned port or
   source-content drift remains. Both repository dirty states and HEADs are
   reported separately; generated build evidence is not staged.

## Failure and cleanup policy

The first failed acceptance stops the sequence. Preserve the runner-created
failure bundle, process/log/lifecycle paths and disposable world evidence; do
not retry automatically and do not patch source. The runner must terminate its
owned process groups. The engineer independently checks the recorded PIDs and
ports; only a demonstrably order-owned survivor may be terminated. Never stop
or restart the live service. A reproduced code defect reopens work only through
a new bounded Terra order after review.

## Execution record, revision 1

Documentation gate passed: diff check and 34-task guardrails in 15 seconds.
Fresh preparation succeeded with source-content
`7b62b2b0121950a16f4fc01afde61607ae62d06f9d48e7848d1a870d25ddb285`,
JAR `05117ce161a6288548ebaebb19a21fe56d2a08eaf63096fe26f3c0c77dfd68e7`,
plan `549dd6f28e4f56cb9dbdd1920367d8e68099d3c99fa108cc4df67d82d42e9de3`
and 13 lanes. Worker-3 was exactly the requested two-lane/two-world assignment.

The worker stopped before starting any server or client. The contract's valid
late-bound revision token `observed_at_boundary` was passed unchanged by the
runner, but `prepareFrontierV3PilotServerWorld` accepted only decimal digits and
rejected the otherwise complete crash tuple. Retained evidence:

- failed shard: `build/pm-f0va-native-qual-01/worker-3.json`;
- failure bundle:
  `build/frontier-v3-ci-shards/worker-3-e2907a18-7fd7-4443-a006-119844f9d39d/persistent-matrix.failure/bundle.json`;
- assigned plan and prepared copy remain beside that bundle.

Ports 25631/25632 were closed and no order-owned Java/client process survived.
The production service was untouched. Per stop policy, the three-world
benchmark was not started and there was no automatic retry.

`PM-F0VA-NATIVE-CRASH-REVISION-01` subsequently passed Gate B/C. Revision2 may
repeat the exact execution with the new output root
`build/pm-f0va-native-qual-01-r2/`; all other display, port, ownership,
acceptance and stop rules remain unchanged.

## Execution record, revision 2

Fresh source `0cb18ada8514ceef9f1ac322b740443ba2b018829d99702d7f67b3d1cd7e72b2`,
plan `c1fae105e3ed2da4b5dcce62fd114e76f6cffce191867d8daf997c10bbf2a670`
and build identity
`6eb5e38aae562262ae3685e84f60498ac55d1f669c0a4d6504c063d8ee815d12`
were prepared. Revision2 passed the former Gradle blocker, prepared and started
the first assigned server, then failed before starting the client while
publishing `SERVER_READY`: the session's already validated normalized runtime
plan no longer reproduced its own `contentSha256`.

Retained failed shard is
`build/pm-f0va-native-qual-01-r2/worker-3.json`; failure bundle is
`build/frontier-v3-ci-shards/worker-3-9a6ef13a-e41f-43ec-9c61-09c62e2eaac3/persistent-matrix.failure/bundle.json`.
It names server PID3090549/run88e9cf07-9e7c-47d6-8547-37b21987f962 and null
client PID. Owned cleanup performed a normal RCON stop/save/exit; ports
25631/25632 are closed and no order-owned process survived. The benchmark was
again not started.

`PM-F0VA-NATIVE-RUNTIME-IDENTITY-01` subsequently passed Gate B/C. Revision3
may repeat the exact execution under `build/pm-f0va-native-qual-01-r3/`; all
other display, port, ownership, acceptance and stop rules remain unchanged.

## Execution record, revision 3

Fresh source `0de9a2b07d0bf4800c2d3d1829a97d6e9a841aa1a581caa38b11dd2522319ba3`,
plan `5cd675fb686a6e7a6b4c873d9e9cdf855193b0784bfff096995d7f56c997ee9d`
and build identity
`865aa4559de420a93ef7590f836c88de33711d3d568e491383e879e8fcb36829`
were prepared. Revision3 passed both previous blockers, started server
PID3116227/run0f53805e-69f9-4599-99d2-83e456bdaa24 and visible client
PID3116711. During initial Quick Play connection setup, Minecraft emitted a
pre-login logout callback. Session control had no bound `activeConnection` but
classified the callback as `unexpected_active_connection_loss`; the client
failed before `PREPARED_CLIENT_READY`.

Retained failed shard is
`build/pm-f0va-native-qual-01-r3/worker-3.json`; failure bundle is
`build/frontier-v3-ci-shards/worker-3-467ea40f-729b-463d-8a76-4392bb2073f4/persistent-matrix.failure/bundle.json`.
Owned client exited1 and server cleanup performed RCON save/exit. Ports
25631/25632 are closed and no order-owned process survived. The benchmark was
not started.

## Execution record, revision 4

After `PM-F0VA-NATIVE-INITIAL-CONNECTION-01` Gate C, fresh preparation retained
source `e8c4af0bcd62b64d8f5d5cb900be93ac82bbc8d5cdbbf9fe087aa0fc859a2a1b`,
JAR `4ea05b2cec716dd53980a57d0016631df3fb1af311a11a774a6a259f52e4ce5a`,
build identity `26624d127b497142b3825ec36dae61f442e8ac9c1f1afd8c8b2a76f7af006f1b`
and plan `83c24f2493d326132215b20daa8f27071333c36942c81584872a7bcda1b747b8`.
The plan contained 13 lanes/four workers and worker-3 again owned only the
declared abrupt release-boundary and graceful lanes.

Revision4 passed all earlier blockers: server PID3156982 and visible client
PID3157491 reached connected fixture readiness, completed actions1–4, acquired
the natural HOT lease and durably emitted the exact
`frontier.scene_lease_released_v2` transition at revision2423. The server did
not emit `PMV3_CRASH_BOUNDARY`, so the client correctly timed out after
300000ms waiting for `expected_loss_armed` and the runner stopped before
claiming a crash.

Retained failed shard is
`build/pm-f0va-native-qual-01-r4/worker-3.json`; failure bundle is
`build/frontier-v3-ci-shards/worker-3-d24d50c0-b1be-40d2-aba0-4d4daf50d016/persistent-matrix.failure/bundle.json`.
It binds session `3221a9e2-b33f-416c-9209-b70bba51cfc2`, run
`5e376b3a-af44-444a-9e46-e25a5893919d` and server run
`5817e4a3-0624-4e09-9a3b-1b6b63bd5d1b`. Cleanup performed RCON stop and a
normal all-dimensions save; ports25631/25632 are closed and no owned process
survived. The benchmark was not started.

The generated server program arguments contain no early pilot-crash mixin
configuration; the current pilot registers it only from `ServerStartedEvent`,
after Mixin bootstrap has already selected launch configurations. The retained
exact release without the marker is the native failure evidence. Revision5 is
blocked on the bounded early-bootstrap correction in
`PM-F0VA-NATIVE-CRASH-MIXIN-BOOTSTRAP-01`.

## Execution record, revision 5

After the early-bootstrap correction passed Gate C, fresh preparation retained
source `8ffd8cf658b77ccbb6f325c5d175d26575d7f5f9f641b879d02f1d60ba7638ae`,
JAR `4ea05b2cec716dd53980a57d0016631df3fb1af311a11a774a6a259f52e4ce5a`,
build identity `4a1f9d38ec5dba2f65fa10070d5fbbb4057f6a1cd7759ee7a88b6bcfbde808ea`
and plan `756be5b206d80b45a578c239e0d63656ee37b0a1dc0bb4f4a2f2b22ccef4b6cd`.
The plan again contained 13 lanes/four workers and the exact worker-3 pair.

Revision5 proved the new early input was genuinely consumed: ModLauncher logged
the adjacent `--mixin.config`/pilot configuration pair and initialized Mixin
before mod construction. It then failed before server readiness and before any
client launch. The configuration declared the broad package
`io.farfrontier.palemirror.internal.frontier.v3`; Mixin consequently owned that
whole package and rejected NeoForge's ordinary direct load of
`FrontierV3PilotFixtureBootstrap` with `IllegalClassLoadError`.

Retained failed shard is
`build/pm-f0va-native-qual-01-r5/worker-3.json`; failure bundle is
`build/frontier-v3-ci-shards/worker-3-f1ec75b7-faa4-4235-bd8c-4ebb9d3fd45d/persistent-matrix.failure/bundle.json`.
It binds run `4ea56364-74ce-4b2e-a7cc-52c51d0aa295`, the retained disposable
world and the complete non-truncated server log. No client PID or completed
server run was claimed. All owned ports are closed, no runner/PMV3 process
survived, the live service was untouched and the benchmark was not started.

Revision6 is blocked on moving only the two pilot mixin classes into a dedicated
mixin namespace without exposing production package-private targets or
weakening package isolation.

## Execution record, revision 6

After namespace Gate C, fresh preparation retained source
`0c67c9a44758a43c392979aa692ac09a0ee3fb8c64cfb9c29bcd41a6ec6e65d3`,
plan `46cf237b0472879d2774c8332825a3c90281d28625458310e8c7034319f01696`
and build identity
`4e0d597eda20984db27f8c69f1e54de716f646112a92a039b1f5b9e2347560bd`.
The exact worker3 assignment started server PID3277899 and client PID3278342.
The retained server log proves early mixin CLI input, ordinary client actions
and release trace revision2425, but no `PMV3_CRASH_BOUNDARY`. Client deadline
300000ms expired waiting for `expected_loss_armed`. No crash was claimed.

Failed shard: `build/pm-f0va-native-qual-01-r6/worker-3.json`.
Bundle:
`build/frontier-v3-ci-shards/worker-3-0a3f3584-e2a7-465f-848e-9f17ab2222f5/persistent-matrix.failure/bundle.json`.
It binds run `10718ae5-8495-4bac-914f-3c99a8c1e14c` and server run
`edc15d5c-59c4-4e56-a31d-8b024d02ccfd`. The log proves normal all-dimensions
save at22:38:17. Independent follow-up found both recorded PIDs absent,
ports25631/25632/25641/25642 closed, and the retained revision6 world present.
The live Java process remains untouched. The benchmark was not started.

Next step is read-only causal review of actual mixin target application and
probe matching. Shared production/pilot mixin package alone is not yet a
proven cause; do not change it or rerun merely on that hypothesis.

Evidence-retention correction: the namespace order's permitted module-wide
Gradle `clean` also removed revision5's disposable world directory. Its root
failure bundle and complete inline log survive, but its `retainedWorld` pointer
is now absent; do not claim that world is retained. Future stale-class cleanup
must be scoped to exact generated pilot class outputs and preserve run worlds.
Revision6's world remains present. Documentation verification after recording
revision6 passed both repository diff checks and34-task guardrails in18seconds.

Independent causal review found a definite matching defect:
`FrontierResourceSiteHarvestSceneSupport.owner` returns the site's settlement;
`FrontierSceneContinuationPlanner.releaseEvents` uses that owner for the real
`SceneLeaseReleased` event. The probe instead compares its armed job ID directly
with `event.subject()`. `FrontierV3DiagnosticTrace.recordScene` reports a
separate job-based diagnostic subject, so the matching trace was not evidence
of a matching event. This mismatch rejects the release even if injection works.
Terra's read-only review must cover all five typed windows and retain exact
job/lease identity; matching any release from that settlement is insufficient.
Actual target application remains unproved, not presumed broken or working.

## Execution record, revision7

Fresh source `59af02f11c96e49acafb4b66d7dc13f632aedd67d186247068619aa1f1341a4b`,
plan `e54f3dbe84c1904a1415917897cd2003d245b4c35603abad9ee6395c852e1c79`,
build `cbce7be4443cfb5b9e77ab1ccd8deaa73266883ae2c4db86e609b28ad27e410d`.
The exact worker3 pair was prepared under `build/pm-f0va-native-qual-01-r7/`.
The server started, but the new preflight reported that the durable crash mixin
was not applied to FrontierStoreTransactionCommitter. No client launched and
no crash/recovery success was claimed. This is a preflight failure; diagnosis
must distinguish actual transformation absence from an applied-info lookup
error before selecting a correction.

Failure bundle:
`build/frontier-v3-ci-shards/worker-3-5c0dcf04-9d88-4023-b5fe-0d75ea84be3b/persistent-matrix.failure/bundle.json`.
It retains the exact error at23:17:50 and all-dimensions save at23:17:57.
Main inspected the inline log and prepared identities, confirmed reported server
PID3383619 absent and ports25631/25632/25641/25642 closed. The bundle has null
clientPid and no completed serverRuns. Terra retains read-only diagnosis only;
no retry, benchmark, source edit or live-service operation is authorized.
