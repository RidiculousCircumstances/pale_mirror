# PM-F0VA-PERSISTENT-RUNNER-01: actual assigned-worker execution

Specification revision:3. Parent:F0.VA. Risk:critical-code (test orchestration/scenario).
Engineer:/root. Sole executor:/root/terra_crash_completion,gpt-5.6-terra high.
Specification READY; Gate A ACK required. Active phase is in CONTINUITY.md.

## Baseline and prerequisites

PM-F0VA-PERSISTENT-PLAN-01 and PM-F0VA-PERSISTENT-CRASH-01 are ACCEPTED.
Previous source/run grants are closed; same executor supplied READ_ONLY design.
No active Gradle/native harness found at this order's preparation. Main retains
all Gradle; unrelated live server is not in scope.

Nested HEAD:0ac6f97a695d636ae928313de7a807d1f3ea05a1,206dirty/untracked entries
before this new order. Outer HEAD:891a4dfe1a613857124775696973d065cc9399cb,
only inherited .f0v-baseline/. Preserve all WIP and both independent histories.
Latest source acceptance: fullNode148/148, critical19859 exit0,290GameTests,
71tasks/2m8s; JAR259e8fde6516539315216616c483a040df856e1b71a052b5019bba549d0746c3.
Acceptance docs guardrails35012 PASS34tasks/16s. Historical current21 is not new
runner/native evidence. Scoped crash-protocol hashes remain in its accepted order.

Runner baseline SHA256 (paths under tools/frontier-v3-test-pilot/src):

```text
run-f0va-persistent-matrix.mjs 89a02ea0c70221b8233c6f4dc50f963a01d2fea228c62bbda371849a9c301c1d
run-persistent-matrix-client.mjs 9d8682fa9e97432436ad5b55646c5f62708a5fc94bf8a0d7e633a8ea81275c9b
run-ci-matrix-shard.mjs 260c088c8272f92f978203562034ecbe04d36ae2f89c60009a22ccd82e0eddf5
persistent-worker-plan.mjs 6c16e8e8119cdcfe8fd4ce16ca17da47da35c75a5ccb291162b80b24f4625337
```

Read AGENTS/ledger, engineering-agent-protocol, accepted design/compiler/crash
orders, architecture.yml harness/causality flow, execution-semantics section6,
accelerated-verification-loop F0.VA.1/.4/.5 and matching release/server skills.

## Outcome and exclusions

Replace the actual shard's per-lane client launches with one invocation of the
existing persistent runner per exact worker assignment. This is executable
adoption covering all13 current lanes and all5 crash boundaries, not another
unused helper or fixed smoke-only demonstration. A worker retains one client
JVM through every assigned ordinary, graceful and abrupt segment.

Preserve the existing benchmark CLI and its >=3-independent-world proof policy.
No gameplay, assertion weakening, provider/workflow changes,
performance-tuning campaign, dependencies/build, Git migration/commit/push,
deployment, live world reset or F0 gameplay breadth. Native qualification is a
separate explicit gate after deterministic/source integration acceptance; do
not claim actual physical continuity or F0.VA closure from this source order.
The sole scenario exception is the exact abrupt departure correction below;
all other contracts/scenarios and canonical implementation remain read-only.

## Contract and owner transitions

### Exact assignment and launch identity

- Schema2 CI plan and accepted compiler own membership/order, original scenario
  hashes, worker/measurement/contract and portable build identity. Runtime
  admission has a distinct versioned kind anchored to that validated compiler
  output and its originating plan; admit exact nonempty1/2world assignments,
  max32segments. No padding/rebalancing or boolean minimum-world escape hatch.
  Benchmark kind/validator still rejects fewer than3 independent worlds.
- Keep the source plan immutable. Retain explicit original lane action offsets
  for inverse restart rebasing. Original scenario/compiled segment hashes are
  distinct from exact serialized runtime bytes after declared port adaptation.
  Pin both and reject drift; Java descriptor hash must equal actual file bytes.
- Verify local prepared inputs against the portable CI hash before launch;
  retain both namespaces, never compare unlike hashes or relax CI validation.
  Fail preflight before native allocation on wrong worker/measurement/scenario.
- Refactor top-level executable runners behind CLI guards with importable,
  injected-I/O test seams consumed by actual CLI call sites. Reuse the existing
  session/server/client owners; no second coordinator, duplicate test-only
  implementation, speculative framework or new process discovery.
- Retain spawned JVM handle/PID BEFORE awaiting server readiness. Signal must
  match that retained PID and fresh serverRunId before publication/use. Never
  overwrite the authority anchor with the signal under validation. Client
  wrapper PID is not native client PID; prove the latter remains one exact JVM.
- Independent lanes get unique disposable worlds; recovery halves share only
  their lane world. Fresh server per required segment, one live pair per worker.

### Closed lifecycle and bounded failure

- Ordinary/graceful paths retain actual action acknowledgements, all required
  assertion evidence, normal disconnect, durable save, owned exit/closed port,
  successor readiness and same-client cleared-state login. Final close remains
  supervisor-authorized only after its authenticated terminal evidence.
- Abrupt path configures only the original admitted server crash probe. Ordinary
  player actions continue until it parks at the exact boundary. Resolve
  observed_at_boundary from that real probe, validate fixed revision/optional
  authority epoch, publish accepted immutable arm, await Java arm ACK, then
  fire the existing controller once against the retained server handle.
- Collect exact controller receipt, owned JVM exit, actual Java same-client loss
  and closed port independently; their arrival order need not be identical.
  Publish accepted release only after full proof and replacement readiness,
  then resume the same client. Recovery halves cannot rearm. Never wait for a
  nonexistent crash-pre terminal result or synthesize normal disconnect/save.
- Exhausting the declared pre-crash actions without a probe is not success or
  an immediate fatal error. Keep the same connection and bounded probe/arm
  processing alive within the existing scenario deadline, without replaying
  actions, emitting terminal success or inventing further player commands.
  Missing probe at deadline still fails. A real earlier parked probe may
  interrupt the action prefix: validate all assertions whose action actually
  completed before that interruption, retain their evidence, and record later
  pre-half assertions as unreached, never passed. This exception requires the
  authenticated exact crash proof, not a timeout or arbitrary missing evidence.
  Every recovery assertion and terminal invariant remains mandatory.
  The fixed restart offset maps declaration coordinates; it does not claim
  every pre-half action executed. Preserve the existing non-replayed recovery
  half (run-isolated-scenario's declared contract). Retain actual completed
  prefix separately; abandon uncompleted pre-half actions on authenticated
  crash, never dynamically replay uncertain effects or fabricate acknowledgements.
- Extend the EXISTING lifecycle journal's closed transitions/repeatability and
  plan-aware matrix evidence validation with typed abrupt semantics. No second
  journal/path owner. Generic transition admission alone cannot prove a crash;
  the exact complete accepted proof remains mandatory before recovery release.
- Unexpected loss, child failure, fatal pilot marker, malformed/stale evidence,
  missing causal input, timeout or cancellation fails the worker with bounded
  attributable evidence. No fresh-client fallback, automatic retry, sleeps-as-
  ACK, timeout increase, unknown-mode fallback or successful partial result.
  Close watches/listeners/streams at ownership end; cleanup can target only
  retained launched handles/worlds. Failed evidence/worlds remain available.
- Reuse scenarioDeadlineMs and existing bounded lifecycle waits; account for
  declared action windows without truncating a later action. Do not turn this
  into a new timing-policy or watchdog framework.

### Declared partial-release setup correction (revision2)

Source review found the original abrupt scenario held the player beside a HOT
harvester but required release followed by recovery with complete:false.
ResourceSiteHarvestSceneExecutor drains after natural completion or lost demand;
the generic release hysteresis additionally requires safe player distance.
Waiting for all64crops would contradict the unchanged terminal invariant.

Only in contracts/resource-site-harvest-f0v.json variants.abrupt_restart, insert
the existing unload_return.actions[3] ordinary departure immediately after the
third action/HOT assertion: visit pale_mirror:frontier_graybox at x0,y65,z0,
settleMs1000. Move restartAfterAction from3 to4. Do not change the first3actions,
the following inspect, after3HOT assertion, terminal assertions/projections,
five crash windows or any other variant. No extra sleep, normal disconnect,
canonical command, runtime demand shortcut or crash-probe modification.

This supplies demand loss while retaining the same client; the real release
probe must still prove the boundary. Earlier probes may park before departure.
Hysteresis runs on server gameTime after demand loss; awaiting its real parked
probe is explicitly permitted above, not a required synthetic12s action ACK.
Regenerate contract/original-scenario/compiled/runtime hashes from actual bytes;
never relabel historical evidence. Main's in-memory declaration-only check
found all13lane identities and four worker memberships unchanged; worker3 still
owns release-boundary and graceful lanes. This calculation is NOT native proof.
The original contract SHA256 is
8d845dc258c3d847dc5a40892f0fc630c5023186010d4426bbc219ffa5045273.

Regression through actual compiler/runner seams must check departure/restart
offset4, after3HOT and unchanged terminal complete:false/all5windows; exercise
release after actions exhaust, early-probe interruption, absent-probe timeout,
and missing evidence for a completed assertion. Existing permitted test files
own these regressions. Do not hardcode acceptance around worker3 or the new
hash; exact validated assignment remains authoritative.

### Diagnostic evidence without cross-segment contamination

- Java SessionControl supplies a local evidence stamp carrying current run,
  session, epoch, segment and action step; actual client adds it before logging.
  It has no canonical/world meaning and cannot alter server-authored values.
  Keep ordinary non-matrix pilot behavior and the1000-line client ratchet.
- Wrapper routes by authenticated stamp, NEVER mutable current plus pipe arrival
  order. Missing/foreign/unknown stamps fail closed for matrix evidence. Setup
  diagnostics are not evidence actions. Delayed legitimate lines retain their
  old segment attribution; never rewrite a sealed immutable terminal record or
  let an old record satisfy the next segment. Required evidence must arrive and
  validate before sealing; late ancillary records may be retained as bounded
  old-segment trace, not promoted into new evidence.
- Retain actual diagnostic values and original action-step correlation, not
  counts alone. Bound records (max1024/segment), bytes per segment/worker,
  unfinished lines and tails with explicit constants and overflow negatives.
  No silent eviction of required evidence; report selected byte caps at Gate A.
- Build one manifest-shaped evidence view per original lane, rebasing recovery
  steps back through the compiler's exact offset. Preserve all assertions,
  terminal/differential/arrival and before/after COLD diagnostics plus bounded
  correlation trace. Use existing diagnosticForAssertion, requireTerminalEvidence,
  requireColdProgress and evidenceFromManifest as applicable. CI extraction
  alone does not replace assertions previously checked by run-f0v-matrix.
- Leave schema2 CI semantic merge/comparators unchanged. All13current assigned
  lanes have zero frames: no new frame breadth or M3 claim. Reject unsupported
  nonempty frame input before launch rather than silently dropping it; preserve
  any existing benchmark behavior.

## Write scope

Exactly these17files; proposed additional helper requires an amendment first.
Under tools/frontier-v3-test-pilot/:

- src/persistent-worker-plan.mjs and test/persistent-worker-plan.test.mjs;
- src/persistent-matrix.mjs and test/persistent-matrix.test.mjs;
- src/lifecycle-barrier.mjs and test/lifecycle-barrier.test.mjs;
- src/run-f0va-persistent-matrix.mjs and new test/run-f0va-persistent-matrix.test.mjs;
- src/run-persistent-matrix-client.mjs and new test/run-persistent-matrix-client.test.mjs;
- src/run-ci-matrix-shard.mjs and test/ci-matrix.test.mjs.
- contracts/resource-site-harvest-f0v.json, only the revision2 correction above.

Under NeoForge's existing internal/frontier/v3/client package:

- main FrontierV3PilotSessionControl.java;
- main FrontierV3TestPilotClient.java, diagnostic stamp/lifecycle caller only;
- test FrontierV3PilotSessionControlTest.java.
- test FrontierV3PilotExpectedCrashTest.java, only arm-quiescence/prefix contract
  alignment and its negative/recovery regressions from revision3 below.

Compiler changes are runtime derivation/offset metadata only; preserve accepted
source admission and the explicit corrected declaration. Existing crash controller/wire, scenario
helpers, f0v-matrix and CI comparators are reused read-only. No changes to Java
canonical/runtime persistence, crash probes, transport provider, dependencies,
configuration, source sets, workflow, normative docs or outer repository.
Engineer alone edits this order/ledger/audit; executor proposes doc facts.

## Acceptance matrix

| ID | Required proof through actual consumed boundary |
| --- | --- |
| AC1 | Runtime admission for all4 exact assignments/13lanes, no duplicated/omitted/padded lanes;1/2world acceptance, benchmark3world rejection, foreign worker/measurement/hash/offset/bytes negatives. |
| AC2 | Import runners without launch side effects; injected actual orchestration runs every assigned lane with exactly one native-client creation per worker, required fresh servers, independent worlds and shared recovery worlds. Real shard invokes it once, no per-lane fallback. |
| AC3 | All5 crash probes exercise armACK-before-fire, full independent causal inputs, successor release and no rearm; at least two sequential crashes in one session. Missing/foreign input and every independent arrival ordering fail/pass appropriately; no normal/save/preterminal synthesis. |
| AC4 | Actual server-ready validation rejects foreign PID without changing retained spawn anchor; early server/client exit, failed reconnect, cancellation and fatal marker produce failed worker evidence and exact bounded cleanup. No actual kill in unit tests. |
| AC5 | Java stamp and Node router agree across segments, sessions and reordered pipes; no stale cross-segment assertion, original action rebasing exact, byte/count/unfinished-line overflow explicit. |
| AC6 | Full real13lane declaration vocabulary passes existing terminal/COLD/differential/arrival extraction from retained values; independently corrupted or missing assertion/value fails. Counts-only or hand-built success projections cannot pass. |
| AC7 | Existing ordinary/graceful/final-close and three-world benchmark tests remain green; focused Java/Node then full Node/critical gate; reviewed source/build identity retained. |
| AC8 | Exact revision2 departure amendment preserves all5boundaries and recovery semantics; original/recovery action offsets, early real-probe interruption, bounded awaiting-probe after action exhaustion and missing completed-assertion evidence have deterministic regressions. Native reachability remains a separate gate. |

Use generated actual plans and injected non-killing effects; fake callbacks
may stand in for I/O, never for the state machine under test. Source-string
checks alone are insufficient. Do not run real clients/servers in unit tests.

Focused Node command, from project root, after explicit IMPLEMENTING:

```bash
node --test tools/frontier-v3-test-pilot/test/persistent-worker-plan.test.mjs tools/frontier-v3-test-pilot/test/persistent-matrix.test.mjs tools/frontier-v3-test-pilot/test/ci-matrix.test.mjs tools/frontier-v3-test-pilot/test/run-f0va-persistent-matrix.test.mjs tools/frontier-v3-test-pilot/test/run-persistent-matrix-client.test.mjs tools/frontier-v3-test-pilot/test/lifecycle-barrier.test.mjs tools/frontier-v3-test-pilot/test/persistent-crash.test.mjs tools/frontier-v3-test-pilot/test/crash-controller.test.mjs
```

Focused Java, main only after source freeze:

```bash
./gradlew :pale-mirror-neoforge:test --tests io.farfrontier.palemirror.internal.frontier.v3.client.FrontierV3PilotSessionControlTest --tests io.farfrontier.palemirror.internal.frontier.v3.client.FrontierV3PilotExpectedCrashTest
```

Full gate, main only after Gate B review:

```bash
npm test --prefix tools/frontier-v3-test-pilot
./gradlew guardrails check :pale-mirror-neoforge:runGameTestServer :pale-mirror-neoforge:build :pale-mirror-neoforge:verifyPackagedJar
```

## Review, native qualification and stop conditions

Gate A ACK: exact16files, current baselines, runtime kind/hash anchoring,
existing journal transitions, diagnostic stamps/caps/sealed handling, injected
tests/commands and any unresolved decision. Then await explicit IMPLEMENTING.
Executor may run only focused Node and diff/style checks after grant; ALL
Gradle and heavy/native resource ownership stay main. No recursive delegation.
Report coherent milestones every5–10minutes, not per-method acknowledgements.
Gate B requires stable actual CLI wiring and AC1–AC6/AC8 mapping before full gate.
Gate C accepts source integration only after AC1–AC8; it does NOT close F0.VA.1.

Next separate native qualification: actual worker3, its abrupt release boundary
and graceful lane,2independent worlds/one client; later unchanged3world benchmark
and remaining local/provider gates. Exact native command, fresh prepared input,
private paths/ports/display and owned-process cleanup grant must be written
after deterministic review, not inferred here. Preserve22.52% accepted benefit;
25% advisory, no timing reruns to chase it. Provider2.5x and feedback3x remain.

Contradictory wire/ownership, unavailable evidence correlation, unexpected file
scope, concurrent writer, repeated unexplained failure or new authority need:
report the concrete cause. No live-server operation, Git mutation, report
rewrite, product-claim weakening or advancing F0 on partial integration.

## Gate A review (revision1, historical)

ACK ACCEPTED,2026-09-05: exact15paths and four runner baseline hashes verified,
both HEADs unchanged,206nested entries/outer .f0v-baseline/ only. Executor has
no unresolved decision or active command. Main initial docs guardrails95642
PASS34tasks/16s. Runtime kind is schema1 frontier-v3-assigned-persistent-matrix,
anchored as above; this is distinct from the unchanged benchmark kind.
Accepted diagnostic limits:1024values/segment,64KiB complete diagnostic line
and unfinished stream fragment/tail,1MiB values+trace/segment,32MiB aggregate
worker. Enforce bytes before retention/parsing and test overflow; limits are
safety bounds, not permission to truncate required evidence.

After main's updated docs gate completes, IMPLEMENTING may be explicitly
granted to the same executor. Implement the coherent actual-runner endpoint
and consumed deterministic seams in this order, then freeze at Gate B with
AC1–AC6 method mapping. Only exact focused8-file Node and diff/style commands;
all Gradle main. No native/full/provider/Git/deployment grant is inherited.

## Revision2 review

Engineer paused revision1 implementation at the confirmed scenario conflict.
Executor acknowledged READ_ONLY, no active command/test, all WIP retained and
no scenario/canonical edits. Preserve the existing approved runner WIP; no rollback.
Focused8-file Node command is unchanged, all Gradle remain main-owned. Main
must pass the documentation gate, accept renewed ACK and explicitly grant
IMPLEMENTING before any further executor writes/tests.

Renewed Gate A ACK ACCEPTED,2026-09-05: exact16files, unchanged diagnostic caps,
AC1–AC8 and non-replayed fixed declaration offsets accepted. Engineer/executor
source reconciliation resolved early-probe and hysteresis objections using
run-isolated-scenario/restartSegments and the actual scene work/demand paths;
no new runtime replay or sleep-as-proof mechanism is required. Main docs gate
26980 PASS34tasks/15s; both diff checks pass, both HEADs unchanged, outer only
inherited .f0v-baseline/. Following the updated-ledger docs gate, engineer may
grant IMPLEMENTING with the same focused8-file Node/diff scope. All Gradle,
full/native/provider/Git/deployment permissions remain withheld from executor.

## Gate B review1 — returned for correction

2026-09-05: executor froze all16files, reported focused8-file Node55/55 PASS in
176ms and diff check PASS. Main Java63695 PASS18tasks/1s:3SessionControl and
10ExpectedCrash tests, no failures/errors/skips, timestamp2026-09-05T15:09:21Z.
These are narrow results, not acceptance. No full/native run was started.

Reviewed frozen hashes:

```text
src/run-f0va-persistent-matrix.mjs 0b896f75a5467052200ff0602894db00898b5a58ebc4263ef980b108d95b1144
src/run-persistent-matrix-client.mjs c4ec8aa5530af8314823910eb81a76404031eabdb0347ec3705e21cadbbb5488
src/run-ci-matrix-shard.mjs e8dacf314ffe5918378efcb5650ecd742a8c13a746134efc66a08e21e5da8544
contracts/resource-site-harvest-f0v.json aed8c1b108b4fb51edb29c41e5917d48ab83ae28516004efb6ee531e67595cc2
```

The source tests do not yet prove the required consumed orchestration. Correct
the following as one coherent packet under the unchanged16file/AC1–AC8 scope;
do not weaken accepted validators or substitute more helper-only tests.

1. **Real assignment admission (AC1).** Main compiled the actual contract for
   all4workers, then passed each result to validatePersistentMatrixPlan: all
   four reject with `persistent matrix segment is malformed`. Compiled
   expectedCrash lacks the trusted laneId required by the accepted descriptor
   and arm. Add derived runtime metadata, not a change to original scenario
   meaning. Actual runner admission must also reconstruct/compare exact content
   from its trusted source plan/compiler; opaque stored hash strings are not
   validation of altered segments/scenarios/offsets. Preserve original,
   compiled and exact port-adapted serialized identities separately. Exercise
   this same admission path for every real worker before native allocation.
2. **Consumed crash proof (AC3).** runExpectedCrashSegment currently forwards
   only loss.detail into proof.clientLoss. The actual Java signal keeps the
   authenticated identity in loss.identity and the segment in loss.suffix;
   accepted proof validation requires those fields, as its existing wire test
   demonstrates. Use the authenticated reader's full result, not invented
   identities. Final plan-aware acceptance must retain/check exact crash
   receipts, not just the existence of generic journal event names. Tests must
   run real arm/publication/controller/proof/release integration with injected
   non-killing I/O, including all5boundaries/two sequential crashes.
3. **Epoch and reached-prefix fidelity (AC3/AC8).** The final evidence checker
   compares result.epoch to its index after filtering crash segments, which
   rejects valid successor epochs. expectedLifecycle also still requires all
   pre-half actionCount acknowledgements even after an early real probe.
   Preserve original epoch coordinates and bind the actual completed prefix
   to authenticated checkpoint/loss evidence. A Promise.race winner alone
   cannot prove that a concurrently published checkpoint never happened or
   authorize omission of its reached assertion. Never fabricate missing ACKs;
   keep unreached assertions explicitly unpassed and all recovery requirements.
4. **Assertions at their owning scope (AC5/AC6/AC7).** Main supplied every
   declared assertion value for the real graceful pre-half; the new verifier
   still rejects it with `terminal semantic invariant is missing: identity`.
   It wrongly applies original full-lane F0.V terminal projections to each
   split half; the old benchmark also has no such F0.V projections. Validate
   every reached half-action assertion at its exact step, then assemble original
   lane evidence with original offsets for applicable terminal/COLD/projection
   consumers. Do not drop nonterminal ordinary assertions (currently only
   terminal assertions are checked), required delayed diagnostics, or legacy
   benchmark checks. Await required values before sealing/publishing success;
   late ancillary trace cannot retroactively repair a sealed missing assertion.
5. **Actual prepared-input propagation (AC1/AC2).** The shard writes and verifies
   localPreparedPath but its new child invocation does not supply that path;
   the runner can rebuild/reprepare or inherit an unrelated environment value.
   Pass the exact verified input through the actual CLI path and forbid a
   preparation fallback for assigned execution. Tests inspect the consumed
   invocation/environment and reject foreign or absent prepared inputs before
   launch, while retaining existing benchmark preparation behavior.
6. **Owned failure cleanup and bounds (AC4/AC5).** startServer catches failures
   only while awaiting readiness; malformed/foreign readiness validation then
   throws outside that catch before the caller receives the spawned handle.
   Ensure that every post-spawn failure cleans that exact handle without an
   outer ownership gap. Probe waits need immediate owned-child/fatal/cancel
   observation and removed listeners on every outcome; pending final-close
   waits must observe fatal cancellation too. Enforce the specified log caps
   in bytes, not string code units. Add actual consumed failure-path tests,
   including foreign-ready PID and failure during pending probe/final-close.
7. **Real orchestration regression (AC2–AC8).** assignedRunnerInvocation is
   unused by the CLI and returns literal clientInvocations:1. Its test cannot
   prove a single JVM launch, crash recovery or cleanup. Refactor the existing
   real orchestration behind an exported injected-I/O seam which the CLI calls;
   do not create a parallel coordinator/test-only implementation. Count actual
   injected spawn/stop effects while executing its state machine over all4
   generated assignments/13lanes, including source-backed evidence vocabulary,
   negative/failure ordering and unchanged benchmark. Additional router tests
   must cover declared count/segment/worker byte caps and split fatal input,
   not just one oversized fragment. Remove unused success-count substitutes.

Executor acknowledged READ_ONLY/no active command after this review. Existing
WIP remains preserved. Resume only by explicit engineer dispatch following the
documentation check. Allowed focused8-file Node/diff commands are unchanged;
all Gradle remain main-owned. Freeze again with concrete consumed code/test
mapping before a new Gate B. This is correction of the existing endpoint,
not another infrastructure order, broader file grant or native proof waiver.

## Gate B review2 — returned for final source correction

2026-09-05: executor froze the renewed packet and reported59/59 focused Node.
Main independently repeated the exact focused8-file command:59/59 PASS in
184.64ms; both repository diff checks PASS. Reviewed core hashes:

```text
src/persistent-worker-plan.mjs 936abc9033cae7af5c1faa262edc66a79569026eb159ab195ea9ffd20a1e6da1
src/persistent-matrix.mjs f07149f555b7deac72dac13c2c9dd0c7a52e17f34cfb4518b6d3893b291cd5b1
src/run-f0va-persistent-matrix.mjs 31ab17202af27c6fdf1edad350ebd1985d3a89b65fc1fcff10065739fe52335f
src/run-persistent-matrix-client.mjs 45297c1f237d27fde2ca6c3df662e7c326511a1f3d15eab4d271c55af512cd2d
src/run-ci-matrix-shard.mjs 902ce5666eb198c995ac6015774b0b1b0e45be2036537dc59c44ca0c3c003e1c
contracts/resource-site-harvest-f0v.json aed8c1b108b4fb51edb29c41e5917d48ab83ae28516004efb6ee531e67595cc2
```

Review1 fixed actual four-worker reconstruction/laneId, loss identity, epoch
coordinates, split assertion ownership, shard prepared-path propagation,
post-spawn readiness cleanup, fatal final-close cancellation and the CLI-owned
outer segment loop. Preserve those changes. Four remaining acceptance gaps are
one final coherent source packet under the unchanged16-file grant:

1. **Assigned preflight still has a preparation fallback (AC1/AC2).** The real
   shard now supplies `FRONTIER_V3_PREPARED_BUILD_IDENTITY`, but the assigned
   runner itself still calls `prepareBuild` when that value is absent. Assigned
   execution must reject absent/empty or foreign prepared identity before any
   native/build allocation. Keep preparation only for the unchanged unassigned
   three-world benchmark. Exercise the actual CLI preflight seam, including
   consumed environment/path and absent/foreign negatives; a truthy object
   passed to `assignedRunnerInvocation` is not this proof.
2. **The real abrupt state machine is still outside the injected regression
   (AC3/AC4/AC8).** `orchestratePersistentSegments` is consumed by the CLI, but
   its test replaces `expected` and `releaseCrash` with fabricated `{id}`
   callbacks. Thus it does not execute arm publication, Java arm ACK ordering,
   controller receipt, full authenticated proof, successor release or the
   bounded probe wait. Move/refactor that existing path behind a consumed
   non-killing injected-I/O seam; run all5 declared boundaries and at least two
   sequential crashes through it. Fakes may implement process/filesystem I/O,
   never replace these transitions.
3. **Crash waiting/prefix attribution is not closed (AC3/AC4/AC8).**
   `waitForExpectedCrashBoundary` sleeps only on `outputAfter`; server exit or a
   server fatal marker does not wake it, so a silent early exit can wait the
   whole scenario deadline. Race that wait against exact child exit, fatal and
   cancellation and close every listener. Both supervisor and client still use
   a raw `Promise.race` for checkpoint versus probe/loss; deterministically
   reconcile already-published authenticated checkpoint evidence so both own
   one identical completed prefix. Add early-exit/fatal/cancel, simultaneous
   checkpoint-boundary and action-exhausted/missing-probe regressions.
4. **Final evidence/identity and bounded diagnostics remain incomplete
   (AC1/AC3/AC5).** Runtime port adaptation replaces `scenarioSha256` while the
   session report keeps the pre-adaptation `contentSha256` and drops the
   compiled-segment hash; retain distinct original, compiled and exact
   serialized-runtime identities and validate them through the Java descriptor
   and final report. Final plan-aware acceptance must retain/revalidate each
   exact crash arm/proof/release, not only generic lifecycle event names.
   Required stamped values can arrive after the filesystem completion/loss
   signal due to pipe ordering; wait boundedly for required reached evidence
   before sealing rather than demoting it immediately to late trace. Add the
   still-missing count, per-segment byte and aggregate-worker byte overflow
   tests in addition to line/fragment tests.

No Java/native/full/provider run is granted by this return. Executor may edit
only the unchanged16 paths, run the same focused8-file Node command and diff
checks, then freeze with an AC1/AC3/AC4/AC5/AC8 consumed-code mapping. Do not
touch timing targets: retained22.52% is accepted and25% remains advisory.
Review2 documentation gate after this amendment: `git diff --check` PASS and
`./gradlew guardrails` PASS34tasks in15s.

## Gate B review3 — returned for causal closure

2026-09-05: executor froze review2 at focused Node62/62 PASS227.56ms and diff
check PASS. No forbidden command ran. Main source review accepts the assigned
missing-input rejection, fatal/exit wakeup direction, router capacity tests and
the extracted outer crash-order function, but cannot accept the endpoint yet.
The remaining gaps are demonstrated at consumed boundaries, not style polish:

- Main's actual-plan in-memory negative added `compiledContentSha256` equal to
  `not-a-hash`; `validatePersistentMatrixPlan` accepted and returned it, while
  the first segment had no `compiledScenarioSha256`. The validator treats the
  field as optional/unvalidated. The final report still has no retained crash
  receipts and `validatePersistentMatrixEvidence` still accepts only generic
  events/results/serverRuns, so it cannot revalidate exact arm/proof/release.
- `executeExpectedCrashProtocol` is consumed, but the all5-boundary test fakes
  arm, ACK, fire and loss and never calls the accepted arm/release validators;
  the two-crash orchestration test still returns fabricated `{id}` proofs.
  `waitForExpectedCrashBoundary` remains unexported/unexercised; its
  `outputAfter` waiter has no close handle and remains retained when exit,
  cancellation or timeout wins.
- The supervisor still decides prefix with `Promise.race` plus a1ms second
  read before publishing the arm. An action checkpoint can be published after
  that read and before Java observes the arm. Java currently acknowledges the
  arm and then may execute the current action in the same tick because the
  `mayContinueScenarioActions` check preceded `armExpectedLoss`; its ACK/loss
  carries no completed prefix. The client wrapper independently races action
  against loss, so the two processes can record different prefixes.
- `run-persistent-matrix-client.mjs` did not change. It still seals immediately
  after the filesystem completion/loss signal; a required diagnostic emitted
  before that signal but delivered later through stdout becomes ancillary and
  cannot satisfy the final assertion. New cap tests do not fix this ordering.

Correct these as one causal closure under the same16 files; preserve accepted
work and do not add timing sleeps:

1. Make Java's exact arm ACK an immutable quiescence barrier. It must publish
   the current completed action prefix and prevent any later action completion
   in that segment (including returning immediately in the tick that claims the
   arm). Carry that same prefix in authenticated loss. Supervisor and client
   must validate equality/range and use this one value; remove probabilistic
   `Promise.race`/1ms prefix ownership. Test action-completes-before-arm,
   arm-interrupts-current-action, actions-exhausted and foreign/mismatched
   prefix through the actual Java/session-control and Node protocol seams.
2. Give the existing output wait a removable subscription or equivalent
   consumed close handle. Export/inject the real bounded probe wait and prove
   output, silent server exit, client exit, fatal, cancellation and timeout all
   close listeners. Keep exact serverRun/boundary/revision validation.
3. Run actual accepted arm/proof/release validation inside the consumed
   non-killing integration seam for all5 boundaries and two sequential crashes.
   Retain the resulting exact receipts, pass them to final plan-aware evidence,
   revalidate every receipt against its epoch/successor, and include them in the
   successful report. Generic lifecycle event names alone remain insufficient.
4. Put `compiledScenarioSha256` on admitted/runtime segment descriptors, retain
   original/compiled/runtime hashes distinctly, require every hash shape, and
   recompute/validate runtime plan content identity rather than accepting an
   optional label. The Java descriptor continues to bind the runtime bytes.
   Repeat the explicit non-hash/tampered-segment/runtime-content negatives.
5. Before sealing a segment, await its required reached stamped assertion
   values with a bounded event-driven router wait that observes client exit and
   fatal cancellation. Then seal immutably; delayed optional data stays bounded
   ancillary. Exercise diagnostic-before-signal but pipe-after-signal, missing
   required value, fatal and cancellation. Do not weaken final lane/COLD checks.

This is still the same endpoint and, except for the explicit revision3 path17
amendment below, the same write scope; it is not a new framework. Executor may
run only the exact focused8-file Node command, focused
SessionControl/ExpectedCrash Java tests if explicitly granted below, and diff
checks; all full/native/provider/Git work remains main-owned. Freeze with exact
consumed mapping and no completion claim beyond source Gate B.

### Revision3 scope amendment: legacy arm expectation

The focused Java gate exposed one pre-existing crash test outside the original
16-file list: `FrontierV3PilotExpectedCrashTest` line33 requires
`mayContinueScenarioActions()` to remain true immediately after a successful
arm. That expectation contradicts review3's accepted arm-ACK quiescence
barrier and would force a production race back into the implementation.

Add that one test file as path17. Replace the legacy post-arm continuation
expectation with quiescence and add only the completed-prefix equality/range,
single-claim and mismatched/foreign loss cases needed by review3. Preserve all
other accepted crash validation. This amendment does not authorize any other
file, production loophole, broader crash redesign or additional command. The
same executor may run the already listed focused Java command and focused Node,
then freeze. Main documentation gate is required before dispatch.

## Gate B review4 — returned for deterministic edge coverage

2026-09-05: revision3 froze with focused Node65/65 PASS204ms, focused Java
14tests PASS and diff check PASS. Main accepts the Java ACK/loss shared prefix,
same-tick action quiescence, required runtime hash shapes/recomputation,
closeable output subscription and retained receipt plumbing. Four narrow
consumed defects remain; correct them without redesign or additional scope:

1. `reachedAssertionsPresent` delegates to `assertionsPresent`, which requires
   `assertions.length > 0`. Therefore an authenticated crash prefix of0 with no
   reached assertions waits300s and fails instead of recording every later
   assertion as explicitly unreached. Empty reached requirements are vacuously
   complete only for the crash-prefix waiter; terminal segments still require
   their declared terminal assertions. Add prefix0 and early-prefix tests.
2. Ordinary/recovery waiting currently calls `terminalAssertionsPresent` only.
   A nonterminal assertion diagnostic may be emitted before segment completion
   but arrive through the pipe after the terminal value; the client then seals
   it as ancillary and main fails later. Before seal, await **all** assertions
   reached by the full segment, then retain the terminal-only result summary.
   Add a reordered nonterminal+terminal pipe test and missing-nonterminal fail.
3. Required-evidence waiting receives only `failureAbort`; an unexpected native
   client exit after the lifecycle signal does not abort it and can wait300s.
   Combine the exact child-exit cancellation with fatal/external cancellation,
   close it on every outcome, and add a consumed child-exit regression. Do not
   add polling or extend timeouts.
4. The probe test name claims server/client exit and timeout but exercises none
   of those branches. Add exact server exit, client exit and deadline cases and
   assert output/child/cancellation listeners close. The final crash-receipt
   validator has no focused test at all and binds the arm only to server PID/run;
   compare its epoch, client/port, worker/build identity and full descriptor to
   the admitted plan segment before self-proof validation. Exercise generated
   all5 boundary receipts (including two sequential in one evidence set), then
   missing/foreign descriptor/prefix/release/successor negatives through
   `validatePersistentMatrixEvidence`. A self-consistent foreign arm must fail.

Same17 paths and already granted focused Node/two-class Java/diff commands
only. No full/native/provider/Git work. Freeze for Gate B review5 with the exact
new test names and counts; do not claim Gate B acceptance yourself.

## Gate B review5 — ACCEPTED

2026-09-05: executor froze review4 with focused Node67/67 PASS217.09ms,
focused Java command PASS14 tests and diff check PASS. Main independently read
the consumed paths and accepts AC1–AC6/AC8 source integration: prefix0 is
vacuously complete only for reached crash assertions; ordinary/recovery waits
all assertions before sealing; required evidence owns exact client-exit/fatal
cancellation; probe output/server-exit/client-exit/fatal/cancel/timeout paths
close subscriptions; runtime content/hash provenance is required/recomputed;
all5 plan-bound crash receipts, including repeated cycles in one session, are
retained and revalidated with foreign/missing/prefix/release/successor negatives.

Main repeated the exact focused Node command:67/67 PASS203.45ms. Main repeated
the exact focused Java command: BUILD SUCCESSFUL,18tasks,14 selected tests with
no failures/errors/skips (3 SessionControl +11 ExpectedCrash); `git diff
--check` PASS. Gate B is ACCEPTED. Full critical verification is now main-owned
and authorized; it remains source/build evidence, not native physical proof or
F0.VA closure.

## Gate C — ACCEPTED source integration

2026-09-05: main ran `npm test --prefix tools/frontier-v3-test-pilot`:168/168
PASS in447.72ms. Main then ran the complete critical-code command from this
order: BUILD SUCCESSFUL in2m8s,71tasks (39 executed/32 up-to-date), all290
required GameTests PASS in57.50s, `check`, `build` and `verifyPackagedJar`
successful. Exactly one packaged non-source/non-Javadoc JAR exists:
`pale_mirror-0.3.0-SNAPSHOT.jar`, SHA256
`05117ce161a6288548ebaebb19a21fe56d2a08eaf63096fe26f3c0c77dfd68e7`.
Final nested/root diff checks PASS; nested HEAD remains0ac6f97a…05a1 with208
inherited/WIP entries, outer HEAD891a4dfe…9cb with only `.f0v-baseline/`.

Gate C ACCEPTS the revision3 source/build integration and closes this source
order. It does not prove the actual assigned native worker, persistent visible
client, real crash-boundary reachability, provider parallelism or F0.VA. The
next step is a separate main-owned native qualification order; executor grant
is closed and no Git/provider/deploy permission is inherited.
