# PM-F0VA-NATIVE-MIXIN-DIAGNOSTIC-01

Revision: 2. Parent: F0.VA / PM-F0VA-NATIVE-QUAL-01.
Risk: bounded disposable-server diagnosis; no implementation-source changes.
Executor: `/root/terra_crash_completion`, gpt-5.6-terra high.
State: ACCEPTED (diagnostic Gate C). All grants CLOSED.
Transformation, false-negative finding and local dependency API cause verified.

## Outcome and baseline

Locate the missing pilot crash-mixin transformation stage exposed by revision7
without a speculative source patch or another full client qualification.
The parent retains r7 source/build/plan identities and failure evidence.
This diagnostic cannot close native qualification, recovery or F0.VA.

Terra owns diagnostic assembly, local implementation choices for disposable
output helpers, evidence interpretation and a proposed minimal correction.
The engineer reviews the terminal evidence, not intermediate command choices.

## Authorized boundary

- One server-only launch using the pinned r7 prepared manifest, exact existing
  release crash-arm tuple and ordinary seed41 fixture preparation. Validate
  prepared identity before and after preparation; stale identity stops launch.
- New exclusively created output root beneath
  `build/pm-f0va-native-mixin-diagnostic-r8/`, new run UUID and world named
  `f0va-mixin-diag-<uuid>`. Ports25651/25652 must be free before launch.
- Existing offline world-preparation task may prepare that new world. Never
  clean the module, reset/retry an old world or overwrite retained evidence.
- Output-only diagnostic configuration/helpers may be written by Terra using
  apply_patch inside the new output root. No tracked source, tests, scenario,
  Gradle, CI, packaged artifact or production configuration edits.
- Use stock Mixin verbose/filtered export and narrowly scoped ModLauncher
  logging as proposed. Preserve existing required mixin/preflight semantics.
  Record resolved command/environment and diagnostic configuration hashes.
- Export must not clear an existing `.mixin.out`: use a fresh isolated working
  location or prove the runtime export destination absent before launch.
  Preserve any shared log evidence before a launcher could rotate it.
- No lifecycle control or client launch. Terra alone owns heavy execution;
  server runtime is bounded to90seconds. Prefer normal owned stop/save; if
  needed, bounded termination may target only this proven owned PID/group.

## Acceptance and stop conditions

Run both repository diff checks and documentation guardrails before launch;
discover current repository/runtime/process/disk/port state under the server
operations skill. Preserve both histories, all inherited WIP and r6/r7 worlds.
Retain prepared/plan identities, exact diagnostic deltas, stdout/debug/export
evidence and hashes, target application classification and remaining ambiguity.
If transformed target bytecode exists, inspect whether the crash hook is there.
Confirm owned PID absence and both ports closed, and report both Git states.

One actual diagnostic server launch only. Local output-helper assembly or
preparation errors before server launch may be corrected autonomously within
the existing boundary, retaining each failure and using fresh output paths.
Derive the exact fixture/profile mapping from the existing worker3 runner and
catalog; the prepared build profile is not necessarily a fixture property.
Do not change the selected scenario or build identity to make preparation pass.
After server launch, failure is retained evidence, not permission to retry or
add a plugin. Deliver a causal finding and smallest justified next
source correction or missing observation. No source patch, full worker3 run,
benchmark, deployment, live-service changes, commit, migration or push.
Engineer acceptance is required before the next implementation/native order.

## Revision1 preparation finding

The output helper supplied `disposable_lite` to the Gradle fixture-profile
property; Gradle rejected it before a server/client JVM was launched. No
transformation evidence exists from this preparation failure. Revision2 grants
local assembly correction, not another native qualification or source change.

## Revision2 runtime evidence

`build/pm-f0va-native-mixin-diagnostic-r8/attempt-2/result.json` retains run
`90d2a7b1-bc87-4981-b734-49c59002829d`, exact r7 source identity and unchanged
packaged artifact. At23:43:54 the log records the durable mixin being mixed
into FrontierStoreTransactionCommitter and ModLauncher reporting transformation;
the applied-info preflight nevertheless fails that same second.

Main independently inspected exported bytecode: append tail invokes the injected
handler, which invokes FrontierV3PilotCrashHooks.afterDurableAppend. Export SHA
`dda906d5ce7538b79af43950735460319ae57a544a9091eef58214d84cdac264`;
server-output SHA
`2de36e6ffcbfccafc2b23a8eddc302ec34e12982fa58694170dcceaa02d46c0c`.
All-dimensions save23:44:01; owned PID3450294 absent and ports25651/25652
independently confirmed closed. No client or actual armed crash was exercised.
This proves a preflight false negative, not native crash/recovery completion.

Main checked Mixin0.8.7 sources: MixinInfo.postApply records success on its own
mixin ClassInfo, whereas Mixins.getMixinsForClass reads the named cache entry.
Correction must query the mixin identity and enforce its exact single target;
presence alone cannot establish which target a multi-target mixin transformed.
Source order `PM-F0VA-NATIVE-MIXIN-PREFLIGHT-01` owns that correction.
The diagnostic report's6a1f-prefixed hash denotes ci-plan.json file bytes;
the retained logical plan hash remains e54f-prefixed, not an identity change.
Post-evidence documentation diff check and34-task guardrails passed in18seconds.
