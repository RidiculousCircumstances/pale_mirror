package io.farfrontier.palemirror.api;

/** Lower order wins when several layers claim the same semantic property. */
public enum SemanticLayer {
    SAFETY_RUIN,
    OCCUPANCY,
    PROJECT,
    CRISIS,
    OPERATIONAL,
    AMBIENT
}
