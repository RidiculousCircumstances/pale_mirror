interface:
  display_name: "PM Product Review"
  short_description: "Assess product promises and playtest gates"
  default_prompt: "Use $pm-product-review to assess the current Pale Mirror product milestone."
policy:
  allow_implicit_invocation: true
