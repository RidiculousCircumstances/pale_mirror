package io.farfrontier.palemirror.frontier.v3.model;

/** Lifecycle of one identified supply obligation; terminal delivery is added with route execution. */
public enum ContractStatus { ORDERED, LOADED, DELIVERED, INTERRUPTED }
