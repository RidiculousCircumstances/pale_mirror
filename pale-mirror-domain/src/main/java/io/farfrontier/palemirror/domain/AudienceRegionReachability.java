package io.farfrontier.palemirror.domain;

/** Coarse, adapter-neutral estimate used only to make intervention windows fair. */
public enum AudienceRegionReachability {
    LOCAL,
    REGIONAL,
    REMOTE,
    CONNECTED
}
