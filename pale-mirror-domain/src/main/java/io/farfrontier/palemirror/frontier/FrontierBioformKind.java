package io.farfrontier.palemirror.frontier;

/** A bioform is materialized as a colored Zombie in the graybox profile. */
public enum FrontierBioformKind {
    HARVESTER, RAIDER, BREAKER, PROPAGULE_CARRIER
}
