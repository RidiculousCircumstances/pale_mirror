# PM-F0VA-GRAYBOX-EXIT-01: local graybox exit and provider-ready handoff

Specification revision: 3. Parent slice: F0.VA inside F0.V. Risk:
critical-code. Engineer: supervising root. Executor:
`/root/terra_f0va_unrestricted`, `gpt-5.6-terra`, reasoning `high`.
Active phase and acceptance owner: `CONTINUITY.md`.

## Revision 2: unrestricted execution environment

The user explicitly grants the assigned Terra executor unrestricted host
filesystem, process, network and tool execution access for this order. It may
use the normal user Gradle/JDK/tool caches, task-owned `/tmp` paths, loopback
ports, the permitted display, dependency downloads and any non-destructive
diagnostic needed to complete GX-1 through GX-6. It must not emulate a sandbox
when that would change or prevent the real build/runtime result.

This removes environment permission and approval friction; it does not widen
the product outcome or mutation targets. Preservation requirements, sole-writer
ownership, natural-terrain exclusion and prohibitions on destructive cleanup,
unrelated services, secrets disclosure, commit, push, provider dispatch,
deployment and adoption remain binding until separately authorized.

## Revision 3: supported authored-asset build closure

The three already-reviewed `bunkhouse_2.nbt` substitutions and their bounded
curator/decoded-collateral recurrence proof are reclassified as GX-2
build/runtime dependency closure. Each substitution replaces only the two
unsupported `alexscaves:dinosaur_chop` provision states with the supported,
property-free `farmersdelight:rice_bag` state and preserves every other decoded
NBT value. These exact bytes may enter the publication projection together
with the smallest checked-in verifier/test inputs required to bind them.

This carve-out is not a natural-terrain, settlement-geometry, visual-quality,
SETTLED or RELOADED acceptance. All other Foundry, survey, compiler, runtime,
GameTest and physical evidence from the suspended terrain order remains
deferred and excluded. The ordinary Visuals GameTest aggregate must pass on the
resulting publication identity; a known red aggregate is not an acceptable
limitation of GX-2.

## Outcome

Close every still-applicable local F0.VA exit requirement on one exact
candidate identity and return a publication/provider-ready Gate C packet. The
result qualifies the accelerated graybox verification loop: persistent client,
content-addressed evidence, dependency selector and executable pyramid, exact
lifecycle barriers, deterministic four-worker workflow/merge, and immutable
development fixtures. It does not qualify production natural terrain.

Terra owns diagnosis, local design, implementation, focused through full local
verification and ordinary correction cycles inside this order. Reuse accepted
evidence only when its complete input identity and assertion meaning remain
unchanged; otherwise let the checked-in selector determine affected work and
use fresh-evidence semantics where the F0.VA exit requires it.

## Preserved and excluded work

Preserve all candidate WIP and evidence from suspended
`PM-FOUNDRY-SETTLED-ORDER-01`, including `physical-8` through `physical-11` and
the unverified early-genesis-origin correction, except for the exact revision-3
supported authored-asset closure. Do not delete, reset or rewrite any of it;
do not stage, commit or publish anything under this local order. Do not run a
natural-terrain campaign, authored whole-region Foundry audit, settlement-site
search, earthworks correction or real-terrain visual comparison.

The physical provider for this order is the flat graybox plus the smallest
controlled non-flat, blocked-port and restart fixtures required by the existing
provider-neutral contracts. Existing valid fixture evidence may be reused when
its exact relevant inputs are unchanged. No whole natural region, climate
beauty pass or seed-specific cleanup is required.

No gameplay breadth, MAT process, HOT/COLD semantic expansion, AI, movement
family, production deployment, checkout adoption, dependency-version upgrade
or v2 work is admitted. Normative Markdown, `architecture.yml` and the original
`CONTINUITY.md` remain engineer-owned.

## Acceptance matrix

| ID | Required result |
| --- | --- |
| GX-1 | Exact inventory separates F0.VA-required candidate changes/evidence from preserved deferred terrain WIP, without loss or mutation of either. |
| GX-2 | The public/clean build and runtime dependency closure needed by the F0.VA graybox matrix is reproducible and fail-closed; controlled Visuals/GameTest coverage passes where those inputs are affected. Natural-terrain acceptance is explicitly absent. |
| GX-3 | All six F0.VA capabilities satisfy their normal, negative and recovery requirements on the exact final local identity. Previously accepted 5.065026x feedback and 22.52% graceful-restart evidence retain only their valid unchanged scope. |
| GX-4 | One current fresh-evidence local matrix proves required graybox native/restart correctness, exact lifecycle identity, complete terminal results and packaged-JAR fixture exclusion. |
| GX-5 | The checked-in four-worker workflow, deterministic shard plan and fail-closed merge are provider-ready on the same identity; actual GitHub execution and the >=2.5x result remain a subsequent external acceptance gate. |
| GX-6 | Gate C maps every F0.VA exit bullet to exact artifacts, commands, source/build/assertion identities and limitations, and lists any invalidated historical evidence rather than silently reusing it. |

## Verification and resources

Work only in the isolated monorepo candidate
`/home/rd/proj/pm-monorepo-assembly.VJM6JZ`. Preserve the published remote HEAD,
original checkouts, recovery bundles, remote verification clones, shared paths
and unrelated live services. One task-owned server/client pair at a time may
use the already approved isolated loopback/runtime envelope; at most one visible
client may use `DISPLAY=:0`. No force-loading, mutable world reuse, blind retry,
timeout inflation or harness mutation of world data.

Use the F0.VA dependency selector and smallest affected tiers during ordinary
correction. The terminal local packet must include applicable guardrails,
checks, package verification and the fresh-evidence native/restart boundary
required by `frontier-v3-accelerated-verification-loop.md`. The original
checkout's pending documentation `git diff --check` and `guardrails` run only
at the final safe boundary and sequentially.

## Permissions and stop conditions

Gate A is `AUTHORIZED` for the complete local outcome with the unrestricted
execution environment above. No intermediate
heartbeat or permission handoff is required. Commit, push, provider dispatch,
deployment and adoption are not authorized; return one coherent Gate C packet.

Stop only for an unexpected writer collision, shared/live-path risk,
destructive or external authority, a required dependency/version or public
semantic change, contradiction between immutable F0.VA contracts, or an
evidenced repeated impasse. Ordinary local failures and corrections remain
Terra-owned.

## Review record

Initial Gate C packet rejected in FINAL_REVIEW. Its exact working-content
fingerprint and prepared artifact included preserved unverified deferred
terrain/Foundry WIP, so it could not also be the exact publishable provider
identity required by GX-1, GX-5 and GX-6. Omitting that WIP would change the
identity and invalidate the fresh matrix. The replacement executor is
authorized to preserve those bytes outside the publication candidate, form a
coherently segregated F0.VA identity and repeat the minimum complete fresh local
boundary on that exact identity. This correction does not authorize a natural-
terrain run, commit, push or provider dispatch. Final acceptance remains
pending the corrected Gate C packet.

The first segregated projection corrected the identity defect, but its Visuals
GameTest aggregate failed 14 of 39 tests because the base authored bunkhouses
still required unavailable `alexscaves:dinosaur_chop`. That is a reproducible
GX-2 build/runtime-closure failure, not a permissible natural-terrain
limitation. Revision 3 admits only the independently reviewed three-NBT
supported-state substitutions and their bounded recurrence proof. Final
acceptance remains pending a digest-consistent Gate C packet whose exact
publication identity passes the applicable Visuals aggregate.

Revision 3 local Gate C is `ACCEPTED_LOCAL`. The supervising engineer
independently verified report SHA-256
`e8d878d8deba2fbd4f5399822ea3798c76278bdd60ad39ef084378c06681401e`,
prepared-source identity
`c10647795a5bfa91cdbdbba233236c50f5da38dfa8f5a3eaca6c7c7e04085265`
(1,878 entries), packaged core JAR
`96197fc6e15d0e0188907f4870ccce69c12d24f46b3d0f87aff3152f032a2ad9`
and fresh matrix SHA-256
`3f2c567863bbe442172215d346a65f129aed728e2adb9c248b31c11a21925042`.
Run `ef64d48b-64a6-425f-baca-6911fb23991a` has one persistent client,
three fresh worlds, four server runs/results, sequences 1 through 28 under one
lifecycle identity, a real two-server restart, final durable save and closed
port. Independent checks reproduced Node 183/183, observed Visuals 39/39 and
core 290/290 saved-world shutdown, counted 1,189 JUnit tests in 239 reports
with zero failures/errors/skips, matched the admitted NBT/curator bytes and
found no packaged fixture entry/symbol leakage or surviving task process.

GX-1 through GX-6 are accepted at the local/provider-ready level. This does
not complete F0.VA: the actual provider run, complete four-worker merge and
`>=2.5x` result remain unproved and move to the next bounded order. Natural
terrain, product/human acceptance, deployment and adoption remain excluded.
