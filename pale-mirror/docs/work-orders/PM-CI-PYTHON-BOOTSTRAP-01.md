# PM-CI-PYTHON-BOOTSTRAP-01: reproducible core test dependencies

Revision1. READY / IMPLEMENTING, sole executor Terra high; engineer /root.
Risk small-code verification infrastructure. Original ledger is canonical.
Candidate-only writes: /home/rd/proj/pm-monorepo-assembly.VJM6JZ.

## Baseline and outcome

Published parent ead6040c4882c412900c0dd13e4a4a77508409a3, tree
9cc710ea99895ab27f18a8cf478a73bd8f85ed97. Prior runner-context repair is
accepted. Main independently checked exact commit paths/parent, remote main,
clean independent clone PoUqva HEAD/tree and actual GitHub job/log evidence.
Run34011355939 admitted core and failed three existing Automodel test tasks
because python3 could not import PIL. Native jobs correctly stayed skipped.
The failed run remains failed; this is dependency provisioning, not permission
to weaken the tests or modify Automodel implementation/experiments.

Make ordinary core verification reproducible from an isolated Python
environment without relying on host-installed PIL or other undeclared test
dependencies. Inspect all Python tasks used by the core gate and existing
dependency conventions to avoid repairing only the first import symptom.
Use a compatible explicit dependency declaration and interpreter setup;
Terra owns local design and dependency selection with evidence. Do not import
the unrelated Blender toolchain wholesale. Preserve test content/coverage,
core Gradle gate, Gradle isolation and native admission/timing invariants.

## Boundary and verification

Read scoped AGENTS, original ledger, engineering protocol, release-verification
skill and relevant architecture/build ownership. Acknowledge scope and proceed
under this bounded grant; no intermediate helper-design approval required.
Allowed components: root CI bootstrap, source engineering test-dependency
manifest/helper and focused CI recurrence tests. Build wiring may change only
if necessary to select the declared Python environment, with an explanation.
No game/domain/Automodel algorithm changes or runtime dependency additions.

Prove missing dependency failure in a fresh disposable Python environment
without system-site packages, then install the declared pinned dependencies
there and run the affected tests successfully through the intended interpreter.
Dependency installation into that environment is granted, not global pip/sudo
or a host toolchain upgrade. Retain its path and evidence; no cleanup needed.
Add a deterministic recurrence test that rejects missing or late provisioning
and ensure the actual Gradle Python tasks use the provisioned interpreter.
Run relevant focused CI tests, pack validation without refresh, diff-check,
and one sequential candidate ./gradlew guardrails check --no-daemon on final
bytes. No full native/GameTest build is needed for this infrastructure boundary.
Original docs diff-check/guardrails for this order may run sequentially once,
without original edits. Terra is the sole local heavy verification owner.

Return stable Gate B: diagnosis, complete diff, isolated failure/success proof,
commands/receipts and final identity, preserved statuses and remaining limits.
Stop for main acceptance before commit/push or another provider run. No rerun,
dispatch, runner registration, settings/billing/security changes, original
source edits, adoption, deployment, cleanup or historical evidence relabelling.

## Gate B acceptance and revision2 publication grant

Main reviewed the full three-file implementation and independently matched
prepared source1539/26fc22672fe8e4d0e4c799a09a62f2e07886fb3fbbb7f2bc60e2202ac2360da4.
Core provisions Python3.11 before Gradle, creates an isolated venv, installs
Pillow12.1.1/PyYAML6.0.3, checks imports and propagates its bin via GITHUB_PATH.
Existing Python tasks remain unchanged and resolve python3 from PATH.

Local evidence LIMIT: retained venv oRbYNO uses Python3.14.4, not3.11, with
system-site packages disabled, independently checked by main. Terra reports
actual missing-PIL/missing-yaml failures before installation, affected tests
12+4+2 passing after it, final venv-first guardrails check66tasks/19s,
focusedNode19/19, pack179 and original docs guards34tasks. Red/green/Gradle
receipts exist only in executor tool outputs, not standalone retained logs;
do not fabricate them. Installed package metadata declares support for3.11.
Main accepts the small-code correction and local isolation proof within these
limits; the actual Python3.11 core result remains unproved until GitHub runs it.
Native source identity is not a general fingerprint of every core dependency.

Sole Terra may make one Conventional Commit from exact parent
ead6040c4882c412900c0dd13e4a4a77508409a3 containing ONLY:

```text
.github/workflows/build.yml
pale-mirror/tools/engineering/requirements-ci.txt
pale-mirror/tools/frontier-v3-test-pilot/test/monorepo-ci-layout.test.mjs
```

Freeze actual path/blob/mode inventory, verify staged bytes and diff-check;
no source changes after acceptance, no generated inputs or blanket staging.
Apply the same destination/ancestry safety rules as PROVIDER-ADMISSION-01 r3:
exact origin git@github.com:RidiculousCircumstances/pale_mirror.git, remote
main must still be ead6040c; one nonforce main push, no other refs/settings.
Unexpected remote changes or content mismatch stop publication for review.

Observe the automatic core run on the exact new SHA until terminal, retaining
real provider logs/IDs for setup-python, pinned install and Gradle result.
No blind rerun or repair on failure: diagnose read-only and report. Preserve
PoUqva and wqPlX1 as immutable checkpoints. A new remote-only clone may verify
exact HEAD/tree, clean state, original ancestors, no nested Git/gitlinks, new
manifest bytes and prepared identity; run focused CI/identity/selector Node
and pack checks with pinned npm ci permitted, no local heavy/native launch.
Original docs diff-check/guardrails may run once sequentially for this revision.
Return frozen inventory, commit/tree/remote/clone identity and actual core
conclusion/limitations. No runner setup, dispatch, paid resources, settings,
deployment, cleanup, adoption or broad gameplay grant is added.
