# PM-F0VA-PUBLICATION-PROVIDER-01: exact publication and provider proof

Specification revision: 2. Parent slice: F0.VA inside F0.V. Risk:
critical-code plus bounded external operations. Engineer: supervising root.
Executor: `/root/terra_f0va_unrestricted`, `gpt-5.6-terra`, reasoning `high`.
Acceptance owner: the original `CONTINUITY.md` until checkout adoption.

## Revision 2 product stop decision

The user decided on 2026-09-06 that a standalone provider-speed certification
no longer justifies delaying product work. The numeric `2.5x` requirement and
the complete live provider campaign below are retained as revision-1 history,
but no longer block F0.VA, checkout adoption or F0.V resumption. Speed is
observed from ordinary later F0 work; it is not proved by repeated synthetic
runs.

Revision 2 authorizes only a terminal safe handoff: start no new workflow,
rerun or correction cycle; preserve completed/partial evidence; cancel or let
the current disposable run stop at the nearest safe boundary; then stop and
deregister exactly the four task-owned runners and prove both GitHub and host
inventory clear. Preserve all published reviewable fixes and every local WIP
or recovery artifact. No implementation or documentation change by the
executor is authorized by this revision.

F0.VA acceptance now consumes the already accepted local correctness,
identity, recovery, isolation, selector/cache, fail-closed merge and packaged
artifact evidence plus the verified remote publication. Provider correctness
evidence remains claimable only when a complete merged run actually exists;
an incomplete or cancelled run is retained honestly and proves no lanes it did
not finish.

## Accepted input

`PM-F0VA-GRAYBOX-EXIT-01` revision 3 is locally accepted. The sole publication
projection is `/tmp/pm-f0va-publication.tKJfmL`, based on remote/current parent
`b3d04959b6c0c1d51164f92bd35ad4483e0d825c`, with exactly nine tracked and
two untracked intended source paths. Its accepted working-content identity is
`c10647795a5bfa91cdbdbba233236c50f5da38dfa8f5a3eaca6c7c7e04085265`;
its local Gate C report and fresh matrix are recorded in the parent order.
The original dirty candidate remains preservation storage for excluded
terrain/Foundry WIP and must not become the publication source.

GitHub repository `RidiculousCircumstances/pale_mirror` is public, default
branch `main`, and currently has no registered Actions runners. The user has
authorized `gh` operations needed for this gate and unrestricted executor
access without approval prompts.

## Outcome

Publish exactly the accepted projection as a reviewable fast-forward commit,
prove it from an independent remote checkout, provision a bounded four-runner
provider envelope, and obtain one successful actual GitHub workflow whose
fail-closed merge proves complete four-worker correctness and at least `2.5x`
speedup over the separately serialized same-host matrix.

Terra owns the complete outcome: operational design, runner lifecycle,
diagnosis, in-scope workflow/build corrections, affected local gates,
non-force publication and provider reruns. Do not stop for ordinary provider
failures. Report only a genuine architecture/authority/risk impasse or the
terminal evidence packet.

## Publication acceptance

- Before mutation, re-read remote refs and require remote `main` to equal
  `b3d04959b6c0c1d51164f92bd35ad4483e0d825c`; any unrelated divergence stops
  the order.
- Commit only the exact accepted publication projection. No assembly
  diagnostic, generated output, private reference, deferred terrain/Foundry
  WIP or runner state may enter history. Use a concise Conventional Commit.
- Prove the working-content fingerprint remains exactly `c1064779...4085265`
  across the untracked-to-tracked transition, or invalidate the parent packet
  and rerun the affected local boundary before publication.
- Run the required pre-commit checks for the exact commit candidate, including
  the mandatory private supplied-reference gate with its authorized real local
  input, repository/pack validation, diff/status review and applicable
  guardrails. Never publish private inputs or derived private fixtures.
- Push only a fast-forward `main` to
  `git@github.com:RidiculousCircumstances/pale_mirror.git`. Never force-push,
  delete or rewrite a remote ref. Re-read refs and independently clone/verify
  exact HEAD, tree, ancestry, source inventory and clean status.

## Provider envelope and acceptance

- Register exactly four task-owned repository runners on this host for the
  bounded proof. Each has an isolated runner/work/temp namespace and the
  required `pm-native` label; exactly one also provides `pm-timing`. Do not
  reuse an unrelated runner, service directory, checkout or cache namespace.
- Keep registration tokens and credentials out of logs, artifacts, prompts and
  reports. The runners may serve only this repository and this bounded run.
  Preserve diagnostic logs, but stop and deregister every task-owned runner at
  the terminal safe boundary and prove both host and GitHub runner inventory
  are clear.
- Dispatch `.github/workflows/build.yml` on the exact published `main` with
  `run_f0va_native_matrix=true`. The ordinary GitHub-hosted core gate must pass
  before native preparation. Do not bypass or manually forge a workflow job.
- Require all three correctness measurements, each with workers 0 through 3,
  distinct private ports/displays/workspaces, complete terminal semantics and
  no automatic retry. The merge must reject rather than overlook any missing,
  duplicate, stale, foreign or incomparable evidence.
- Require the separate serialized timing job on the pinned `pm-timing` runner,
  with no correctness-matrix contention. Queue time remains separately visible
  and is not part of the claimed execution speedup.
- The terminal merged artifact must bind the exact remote commit, prepared
  source/artifact/classpath identity, deterministic plan, twelve worker
  results, serialized timing samples and a measured ratio `>=2.5x`.

If provider execution exposes an F0.VA build, workflow, runner or merge defect,
Terra may correct only that boundary, run the selector-required local gates,
create a further reviewable fast-forward commit and redispatch. A source,
artifact, assertion or workflow identity change requires fresh evidence at
every invalidated local/provider boundary. Do not weaken thresholds, suppress
failures, add retries, inflate timeouts or change scenarios merely to improve
the ratio.

## Exclusions and stop conditions

No natural-terrain/Foundry campaign, gameplay/MAT/HOT-COLD/AI/movement breadth,
production deployment, original-checkout adoption, v2 change, history rewrite,
private artifact publication or unrelated service mutation is authorized.
Do not delete the original candidate, accepted projection, recovery bundles or
evidence. Stop for remote divergence, an unrelated writer, inability to keep
runner credentials private, a required product/architecture change, or a
repeated evidenced impasse outside this order.

## Gate C return

Return exact commit/parent/tree and remote refs; clean-clone verification;
commands and checks; runner identities and final cleanup proof; workflow/run/job
URLs and conclusions; downloaded merge/evidence artifact hashes; complete lane
coverage; sequential/parallel timing and ratio; every failed attempt with its
classified cause; final dirty state of the original candidate, projection and
outer repository; and all remaining limitations. Only the supervising engineer
may accept provider F0.VA, authorize checkout adoption and resume original F0.V.

## Review record

Revision 1 provider-speed campaign stopped by the user product decision.
Revision 2 terminal cleanup is `ACCEPTED`. Remote `main` remains
`f3333c82402188926eb0ed8732eec12835927350` with parent
`41387eb32bc542831180f1b9aff5e4fdc37f1df0` and tree
`f60959fa5d0e060f18ee8e4b467094af76a69235`. Runs `34042975487` and
`34043019208` are terminal `cancelled`; their core and immutable-preparation
jobs passed, but no correctness sample, merged provider matrix or timing claim
is accepted. Cancellation evidence is retained under
`/home/rd/proj/pm-f0va-provider-runners.1UVVKr/cancelled-run-34042975487`.
Independent review found zero repository runners, no task-root process, a clean
publication projection and unchanged preserved candidate/outer WIP. Unrelated
`stream_miner` runners remain outside this order.
