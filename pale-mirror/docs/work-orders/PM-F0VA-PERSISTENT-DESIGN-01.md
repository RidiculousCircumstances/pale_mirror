# PM-F0VA-PERSISTENT-DESIGN-01: assigned-matrix lifecycle design

Revision: 1. Parent: F0.VA. Risk: docs/read-only investigation.
Executor: /root/terra_executor, gpt-5.6-terra high. Engineer: /root.

## Authority and outcome

READ_ONLY: inspect only; no file edits, tests, process launches, Git mutations
or recursive delegation. CI-composition order is ACCEPTED, all prior grants
closed. Preserve inherited WIP and current21 historical evidence.

Produce one concise source-backed design packet for the persistent-client
integration gap recorded in frontier-v3-architecture-audit.md. F0.VA.1 requires
one client JVM across a worker's complete assigned native matrix, not merely
the fixed smoke/graceful benchmark. Reuse the existing owner and protocols;
do not propose a second lifecycle implementation or a crash-lane exemption.

Read AGENTS/CONTINUITY, engineering-agent-protocol.md, architecture.yml's
test-harness ownership/critical flow, accelerated-verification-loop.md F0.VA.1
and .5, and matching release/server-operation skills. No product scope change.

## Required packet

1. Actual call graph and reusable owners across run-ci-matrix-shard,
   run-f0va-persistent-matrix, run-persistent-matrix-client, persistent-matrix,
   run-isolated-scenario and the Java persistent session/transport. Name the
   exact obstruction to contract-derived lanes and declared crash windows.
2. A single owner/state transition design for ordinary completion, graceful
   disconnect, expected server crash and unexpected client/server death.
   Exact build/worker/lane/segment/server identity, nonce, observed checkpoint,
   same-world recovery and fresh-world isolation must survive composition.
   Planned server loss is not normal disconnect, and crash pre-segment is not
   terminal domain success. Describe evidence and cleanup without inventing
   an acknowledgement from elapsed time or opening a second client.
3. Smallest coherent implementation cuts with expected files and prerequisites.
   Keep gameplay/contract assertions and schema-2 CI merge unchanged. Do not
   close the requirement with a helper that no real runner consumes. A pure
   plan compiler can be a first cut only with an explicit subsequent integration
   path covering all current lane kinds.
4. Deterministic tests, then the smallest native proof and exact critical gate.
   Native proof selection must exercise reused state across independent worlds
   and declared recovery, without resuming the complete original F0.V matrix
   before its ordered provider gate. Name remaining physical evidence honestly.

Send findings/decisions to main; engineer records acceptance and subsequent
implementation specification. Stop after the packet. Existing historical
reports are not reinterpreted and numeric speed targets are not changed.

## Engineer acceptance, 2026-09-05

READ_ONLY packet ACCEPTED. Main independently confirmed Java normal logout/
resume gating and actual immutable shard membership: worker-0 and worker-3 each
own only two lanes/worlds. Runtime admission must not inherit the benchmark's
three-independent-world minimum, nor may assignment be padded to satisfy it.

Accepted implementation sequence: immutable contract-derived worker plan and
real shard preflight; authenticated planned-crash protocol in existing session
owners; actual assigned-matrix runner integration; focused native proof. Crash
protocol must precede switching crash-bearing CI workers to the persistent
owner. No partial cut is complete F0.VA.1 and no fallback to fresh clients may
be called successful persistent execution.

Compiler inputs retain schema-2 plan/build/contract/worker/measurement identity;
ordinary lanes own one fresh world, recovery halves share only their lane's
world, crash pre-halves terminate by the exact fault boundary rather than
terminal domain success. Normal completion retains ordinary disconnect/save.
Planned crash needs separately authenticated client observation, exact server
boundary/PID termination and port closure, then replacement readiness before
same-client resume. Unexpected server/client loss fails, never creates a normal
acknowledgement or a successful pre-crash terminal record.

Native proposal: actual worker-3 (abrupt + graceful) for narrow adoption proof,
not full original F0.V closure. The unchanged >=3-independent-scenario proof
remains a separate requirement; current21 proves only its original protocol.
No implementation/native permission is granted by this design order.
