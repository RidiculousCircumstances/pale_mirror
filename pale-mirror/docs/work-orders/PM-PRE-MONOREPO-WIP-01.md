# PM-PRE-MONOREPO-WIP-01: intentional WIP and commit-boundary inventory

Revision1. Parent: post-local-F0.VA monorepo prerequisites.
Engineer /root; sole executor/auditor /root/terra_crash_completion (Terra high).
READ_ONLY granted. No file edits, staging/commits, bundle/clone creation, remote
access/configuration, migration, tests/build/native or service operations.

Read AGENTS, ledger, engineering protocol, implementation-plan migration gate,
local exit audit and relevant accepted handoff/work-order records. Preserve both
histories and all WIP. Current nested/root HEADs0ac6f97a/891a4dfe; last dirty210/1,
including substantial pre-existing Java/domain/scenario work beyond harness.

Inventory every modified/deleted/untracked entry in each repository, using
expanded paths where Git collapses directories. Classify into intentional
reviewed implementation, intentional preserved-but-unaccepted future slice,
normative docs, generated/local evidence, or unknown ownership/intent. Cite
source/accepted records, not inference from a filename. Flag staged secrets,
credentials, huge binaries and stale generated artifacts without printing
secret contents. Do not delete/exclude/commit unknown files by assumption.

Propose coherent separate nested and root commit boundaries, exact path groups,
evidence that covers each group and remaining review/verification. Distinguish
persisting intentionally paused WIP from declaring it feature-complete. Identify
whether the required history-preserving import can faithfully include all
intentional source while retaining any unrelated local files outside Git; name
conflicts needing user intent rather than manufacturing a clean status.

Report process-safe prerequisites and current .gitignore/workflow/layout hazards.
No migration plan execution; no change to remote or repository ownership. Main
reviews inventory before authorizing source corrections, commits/bundles or
disposable migration clone. Return a stable compact grouped table with complete
path coverage and explicit unknowns; do not dump all file contents or logs.

## Engineer review, 2026-09-06

Inventory and supplement ACCEPTED as a read-only inventory, not code or
migration acceptance. Executor returned an expanded grouped path manifest;
main independently read Git status and checked 239 entries: 103 modified,
1 deleted, 135 untracked. Groups: governance46, CI2, root-build1, Frontier57,
NeoForge48, debt3, harness82. Outer status remains only .f0v-baseline/.
No staging, commits or migration were performed.

The earlier blanket service-stop prerequisite is rejected. Executor reports
production/artifact readers can remain untouched during a separately authorized
disposable-clone operation. Future execution must verify exact affected paths;
this is no permission to mutate served artifacts or stop services. Main verified
additional registered linked worktrees: retain original common Git metadata,
refs and every linked checkout. Do not relocate, prune or delete them. Keeping
.f0v-baseline unchanged requires no decision about its eventual removal now.

Hunk partition is not required merely to preserve the evidence-bearing snapshot.
Preservation is distinct from feature acceptance; narrow F0.VA acceptances do
not cover the whole inherited Foundation/F0.V source. Before commit authority,
ask the user whether all inventoried nested source/docs should enter preserved
history with unfinished slices explicitly labelled WIP, or whether specific
paths must remain unpublished. No removal or exclusion is inferred. Subsequent
precommit review must still check actual contents, sensitive/generated hazards,
exact final-tree identity and required gates. All executor grants are closed
pending that decision and a new bounded order.
