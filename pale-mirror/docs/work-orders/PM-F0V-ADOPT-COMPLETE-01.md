# PM-F0V-ADOPT-COMPLETE-01: adopt the monorepo and close original F0.V

Specification revision: 4. Parent slice: F0.V. Risk: critical-code plus bounded
Git operations. Engineer: supervising root. Executor:
`/root/terra_f0va_unrestricted`, `gpt-5.6-terra`, reasoning `high`.
Acceptance owner is this original `CONTINUITY.md` until the adoption checkpoint;
after the exact governance transfer is committed in the adopted checkout, its
`pale-mirror/CONTINUITY.md` becomes the sole active ledger and this original is
preserved read-only.

## Baseline and prerequisites

- Previous provider work is terminal and accepted. Remote `main` is
  `f3333c82402188926eb0ed8732eec12835927350`, parent `41387eb32bc542831180f1b9aff5e4fdc37f1df0`,
  tree `f60959fa5d0e060f18ee8e4b467094af76a69235`.
- `PM-F0VA-GRAYBOX-EXIT-01` revision 3 plus the 2026-09-06 non-blocking timing
  amendment close F0.VA. Provider runs `34042975487` and `34043019208` are
  honestly cancelled and prove no complete native matrix or speed ratio.
- GitHub repository runner inventory is zero and no task-owned runner process
  remains. Do not provision a runner or begin another timing campaign.
- `/home/rd/proj/pale-mirror-monorepo` was absent at order creation and is the
  approved durable adopted checkout path. The original outer/nested repos,
  `/tmp/pm-f0va-publication.tKJfmL`, the deferred terrain candidate, recovery
  bundles and all evidence remain preservation inputs, never cleanup targets.
- Read the contract, execution semantics, implementation plan, integration
  feedback foundation, accelerated loop, architecture map/audit and relevant
  project skills before implementation.

## Outcome and exclusions

Create one verified clean clone at the approved durable path, transfer the
accepted engineer-owned governance delta without source drift, and then close
the remaining original F0.V integration-feedback contract on that adopted
monorepo. The delivered result is a reviewable fast-forward remote `main` whose
F0.V Gate C proves reusable HOT/COLD process contracts, exact lifecycle/crash
boundaries and the required real Minecraft recovery matrix.

No standalone timing/provider campaign, self-hosted runner provisioning,
natural-terrain/Foundry work, new MAT/process/scene/gameplay breadth, F0.2+
consequence/aftermath implementation, production deployment, v2 cutover/removal, history rewrite, cleanup of
the original repositories or human product-acceptance claim is included.
F0.1 remains paused except for the smallest correction to its existing harvest
reference process that is strictly necessary to satisfy the already-declared
F0.V contract; such a correction must not add a capability or broaden content.
Revision 2 resolves the r33 architecture impasse: the admitted pre-F0.2
harvest capability is traversal-only in both COLD and HOT. Preserve existing
HOT crop-effect WIP, but keep it fail-closed or unreachable until F0.2 owns the
matching COLD semantic consequence, physical eligibility and deferred
aftermath. Do not implement that later capability in this order.

## Contract and state transition

1. Clone only the authorized remote into the absent durable path and require
   exact `f3333c824021` HEAD/tree/ancestry and clean status. Never repurpose or
   overwrite an existing directory.
2. Transfer the complete accepted main-owned governance state from the original
   nested repo into the monorepo's `pale-mirror/` subtree. The transfer includes
   the current AGENTS/protocol/template/ledger/plans/audit and missing accepted
   work-order records, including this order. It excludes every implementation,
   build output and deferred terrain byte from the original dirty tree. Review
   the path/blob delta, run the docs gate, commit it separately and push only a
   non-force fast-forward. That commit is the adoption checkpoint and makes the
   adopted ledger canonical.
3. Audit the current implementation and retained evidence against every F0.V
   exit criterion. Reuse evidence only where its exact source/spec/artifact and
   semantic scope remain valid; do not rerun a gate merely for timing. Missing,
   stale or contradicted correctness evidence remains work to complete.
4. Implement and verify the remaining F0.V outcome autonomously. Generic
   lifecycle, scenario, crash and assertion code may not branch on a concrete
   family. Production state/reducers/codecs remain the only simulation owner.
   Any reference-harvest fairness correction must preserve demand as selection
   only, never process creation or acceleration.
   For revision 2, neutral lanes compare the complete admitted traversal
   projection exactly and retain zero irreversible crop/output progress in
   both modes. Do not weaken conservation, substitute unequal expected values
   or treat HOT-only crop completion as neutral. The native abrupt harvest
   matrix arms lease acquisition, retained HOT checkpoint and release-before-
   COLD boundaries. The generic crash controller still schema/negative-tests
   the complete five-boundary vocabulary; the two physical-effect/typed-
   observation windows are deferred native requirements of F0.2.
   Revision 3 resolves the r44 alignment impasse without weakening that
   comparison: both terminal projections must be sampled at one exact absolute
   canonical `SimInstant`. The server-thread mutation lane admits the bounded
   target against its current checkpoint and uses the ordinary due-action
   engine; client command-delivery time is not added as a relative advance. An
   already-crossed, unbounded or physically unsafe target fails visibly. The
   harness may not write a cursor/schedule or normalize either projection.
   Revision 4 resolves the r48 continuation-ownership impasse. The engine-owned
   `ScheduledAction` in the checkpoint is the sole durable deadline/ordering
   fact; do not copy it into `FrontierWorldState` or `SceneLease`. The process
   descriptor owns its meaning/cadence and HOT admission, semantic checkpoint
   and release bind to an exact typed view of the current action. State and
   schedule effects validate atomically. A release with no semantic step
   preserves that action exactly; a HOT step applies the same cadence rule from
   the bound action as COLD. Missing, duplicate, stale or mismatched bindings
   fail closed, and no deadline is inferred from observation/release time. The
   exact API/design remains the executor's responsibility; the model behavior
   registry must not receive the complete engine queue.
5. Use the accelerated selector/pyramid on this real work. Numeric speed is
   recorded when naturally available but never gates completion. Final F0.V
   evidence remains fresh where the contract requires fresh native/restart
   execution and may not be replaced by cache, mock, server command or a
   cancelled provider shard.
6. Commit coherent verified corrections and push only a reviewable non-force
   fast-forward `main`. Do not deploy. Return one terminal Gate C packet; do not
   self-start F0.1 after completion.

## Write scope

The adopted monorepo is the sole writable implementation checkout. Terra owns
diagnosis, local design, algorithms, helpers, file choice and focused-through-
full verification inside existing F0.V owners: the pure Frontier process/SDK
contracts, NeoForge execution/observation/persistence boundaries, declarative
test pilot, build/test integration and their tests. Root/nested normative docs
may change only for the exact accepted governance transfer; later requirement
changes are proposed to the engineer rather than authored by the executor.

The original workspace `/home/rd/proj/minecraft`, assembly candidates,
publication projection, recovery bundles, cancelled-run evidence, live
services and unrelated runners are read-only. No force push, ref deletion,
history rewrite, destructive cleanup, secret publication or broad staging.

## Acceptance matrix

| ID | Required outcome/invariant | Automated proof | Physical/final proof |
| --- | --- | --- | --- |
| AC-1 | Durable monorepo adoption preserves both histories and exact published baseline | remote/ref/tree/ancestry, path/blob allowlist, clean-clone and docs gates | N/A |
| AC-2 | Reusable closed process SDK uses production owners/codecs and has no concrete-family lifecycle branch | harvest plus structurally different existing-process conformance; missing/duplicate driver, cursor, schedule, payload, codec and stale-version negatives | N/A |
| AC-3 | HOT/COLD authority, observer aggregation and semantic comparison retain exact identity/custody/conservation over the admitted traversal-only reference; neither mode completes crops or creates output before F0.2; one engine-owned continuation binding keeps schedule identity/cadence exact across HOT checkpoint/release without duplicate domain storage or release-time reconstruction | pure matrix and focused NeoForge tests, including two-observer ordering, missing/duplicate/stale schedule-binding rejection and rejection of premature irreversible harvest progress | smallest applicable native variants and exact neutral differential |
| AC-4 | Crash controller is attributable and restart yields reclaim, safe fence/resume or smallest-owner ambiguity without duplication/loss | complete five-boundary vocabulary/controller negatives and recovery assertions | graceful plus the three applicable traversal/lease same-world abrupt boundaries; physical-effect/typed-observation native windows remain explicitly deferred to F0.2 |
| AC-5 | One declarative vertical contract composes never-loaded, arrive-mid-process, unload/return, intervention, graceful and abrupt variants | generator/schema/semantic assertions and fail-closed failure-bundle tests | fresh required F0.V matrix using one prepared build and persistent client where specified |
| AC-6 | Production artifact contains no fixture/fault-injection authority and all F0.V exit criteria are traceably satisfied | guardrails, check, applicable core/Visuals GameTests, build and packaged-JAR verification | terminal criterion-to-evidence Gate C report |

The final packet must map every bullet in
`frontier-v3-integration-feedback-foundation.md`'s F0.V exit gate to exact
current evidence; list reused versus fresh evidence and why reuse is valid;
record source/spec/artifact identities, commands/results, native run/world/
client/server IDs, failures and corrections; prove no owned process/port/
display remains; show remote refs and final status for the adopted and both
preserved original repositories. A cancelled provider run is historical
evidence only.

## Review and execution permissions

Gate A grants the complete outcome now: clone/adopt, exact governance transfer,
diagnosis, implementation, focused and full local verification, ordinary
in-scope corrections, coherent commits and non-force `main` pushes. Terra is
the sole heavy owner and may use task-owned disposable worlds, loopback ports,
private displays, caches and temporary directories with unrestricted host/
network access and no approval prompts. Preserve unrelated live services.

There is no intermediate approval checkpoint and no heartbeat. Report only an
actual `ARCHITECTURE`, `AUTHORITY`, `RISK` or `IMPASSE` boundary, or the terminal
Gate C packet. The engineer does not prescribe implementation or poll routine
WIP. A final run already in progress may reach its attributable safe boundary;
do not start another run after a terminal result merely to improve timing.

## Stop conditions

Stop before expanding for remote divergence, an existing adoption target,
unknown governance/source crossover, another writer, need for new persistent
public meaning, natural-terrain dependency, unavailable required human evidence
or a repeated defect outside F0.V. Preserve all evidence and identify the exact
unmet invariant.

## Review record

Revision 1 reached architecture impasse at fresh r33: the neutral COLD lane
retained zero completed crops while a non-intervening HOT/COLD visit completed
thirteen. This is real mode-switch acceleration, not flaky evidence, and the
comparator remains exact. Revision 2 restores the already-normative ordered
scope from execution semantics and seamless foundation: F0.V/F0.1 prove the
shared traversal owner first; irreversible crop/output consequences and their
two native effect/observation crash windows activate under F0.2. Pending
terminal Gate C.

Revision 2 then reached a narrower r44 verification impasse after all
non-release lanes executed: HOT/COLD terminated at `SimInstant 24623`, while a
client-derived relative fast-forward made COLD overshoot to `24735`, so its
cursor and next due instant were not comparable. Revision 3 requires
server-authoritative absolute-instant alignment through the existing event
engine. The locally corrected crash terminal/watchdog boundary remains
candidate work until the same fresh full matrix proves it. Pending terminal
Gate C.

Revision 3 reached r48 with exact native terminal alignment at `SimInstant
24609`, but the comparator still exposed a real schedule-ownership gap: COLD
retained `dueAt=24702`, while release rebuilt HOT/COLD continuation as
`submittedAt+1` and produced `dueAt=24747`. The HOT-checkpoint crash boundary
also could not fire because physical arrival and the due-action planner did not
share one attributable continuation fact. Revision 4 keeps the scheduler as
sole storage authority and adds the generic typed engine/process binding above;
it does not authorize a harvest branch, copied queue or weaker comparator.
Pending terminal Gate C.
