# PM-F0VA-LOCAL-GATE-01: remaining local verification

Revision:1. Parent:F0.VA. Verification-only of inherited critical-code WIP.
Engineer:/root. Executor:/root/terra_executor (gpt-5.6-terra high).

## Scope

Preserve the199-plus inherited dirty/untracked source entries, root
`.f0v-baseline/`, both histories and historical evidence. Timing policy order
is accepted;22.52-percent restart benefit is sufficient. No percentage retry.

Terra's assignment is READ_ONLY: audit actual local CI worker/shard/merge
evidence and identify the smallest remaining locally runnable proof. Inspect
workflow inputs, isolation, all semantic lanes, required sample counts,
fresh-evidence rules and whether proposed local commands would prematurely run
the original F0.V harvest/crash matrix. Distinguish isolated-worker correctness,
pinned-host timing and mock protocol tests. Return exact paths/commands/gaps;
do not create an execution plan artifact, run tests or alter source/docs/Git.

In parallel, the engineer may run the existing critical verification command
from `pale-mirror/`, after reading the task's target/reset behavior:

```bash
./gradlew guardrails check :pale-mirror-neoforge:runGameTestServer :pale-mirror-neoforge:build :pale-mirror-neoforge:verifyPackagedJar
```

The engineer is the sole local heavy-run owner because the executor's current
environment denied loopback networking. This is agreed verification, not code
authorship. Scope is the Gradle-owned `pale-mirror-neoforge/build/runs/core-game-test`
test environment and normal build outputs; no live service, pilot DISPLAY,
deployment, repository migration, commits or external writes. Preserve any
existing relevant failed report before rerunning its known failed flow.

## Acceptance

- Exact critical-gate result, test count and packaged-JAR fixture exclusion,
  or attributable failure with its smallest regression/correction proposal.
- CI local/provider gap map derived from actual command paths and artifacts;
  do not duplicate a previous code-existence inventory or launch an unreviewed
  full matrix. Missing provider evidence stays explicit.
- Next bounded order names the actual remaining implementation/proof, not a
  repetition of accepted timing or a speculative gameplay expansion.

No source writes under this order. A failing gate authorizes diagnosis only,
not an unrelated fix. Engineer accepts evidence at its actual scope; passing
GameTests alone cannot close F0.VA, F0.V, the inherited WIP review or M3.

## Engineer review, 2026-09-05

Critical gate PASS:71 tasks,1m15s;290/290 GameTests at15:24:45+05.
Packaged-JAR gate included; artifact SHA-256
`41ac2b1fe122d03361976f5614c7ef95052e5a94188297a3ff770a8ccdd82d53`.
Core test directory was regenerated by its standard task; no live world changed.

CI audit: historical local plans contain13 lanes but no completed shard,
sequential or merged proof. Provider correctness is three serial measurements
of four concurrent workers; timing executes the same13 lanes serially three
times. A local shard invokes actual F0.V harvest/crash lanes, not a dry-run.
The inherited V3-AUD-052 selector correction already has source and the focused
physical GameTest now passed inside the full gate; native later-field admission
still needs one bounded corrective proof, not a full closure matrix.

Main confirmed a cold-cache setup defect: private fresh Gradle homes first run
offline without provisioning. Next order PM-F0VA-CI-BOOTSTRAP-01 repairs that
bounded configuration. The reported additional Mineflayer dependency on the
native path is unsupported by the inspected call graph and is not accepted.

Gate C ACCEPTED for the local gate and audit, not full F0.VA or inherited WIP.
