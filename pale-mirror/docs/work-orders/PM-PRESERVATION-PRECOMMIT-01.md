# PM-PRESERVATION-PRECOMMIT-01: preserved snapshot readiness

Revision1. Parent: post-local-F0.VA monorepo prerequisites.
Risk: critical-code review; no implementation changes authorized.
Engineer /root; sole executor /root/terra_crash_completion, Terra high.

## Baseline and authority

User confirmed on 2026-09-06 that all current inventoried nested source and
documentation should enter Git history, with unfinished parts explicitly WIP.
This resolves publication intent, not feature acceptance. Retain all local
artifacts, original Git histories/common metadata and linked worktrees.
PM-PRE-MONOREPO-WIP-01 inventory is accepted: nested HEAD0ac6f97a,
103M/1D/135U before this new order; outer HEAD891a4dfe, only .f0v-baseline/.
Reconcile subsequent main-owned documentation additions without restarting
the completed inventory. Any other unexpected change needs classification.

Read AGENTS/ledger, engineering protocol, the accepted inventory, local-exit
audit, relevant acceptance records and mandatory release-verification skill.
Consult server-operations skill only if inspecting runtime/evidence logs.

## Outcome

Deliver one stable precommit packet for a byte-preserving nested snapshot.
Inspect the full tracked diff and actual untracked contents, including build,
CI, source, tests and normative docs. Identify concrete commit blockers:
sensitive content, generated/local files, invalid build wiring, architecture
contradictions or silently concealed failures. Distinguish known paused
foundation debt from new regressions and from readiness to publish the snapshot.
Do not redesign paused gameplay or require completion of F0.1 to preserve it.

Check content, not merely suspicious filenames, for credential/private-key
material. Report only path/category and redacted evidence; never print secrets.
Confirm no embedded Git metadata, linked worktree, worlds, logs or binary build
outputs enter proposed staging. Do not remove or silently ignore any finding.

Propose an exact explicit path manifest and Conventional Commit message/body
for preservation. A single coherent coupled source snapshot is permitted in
the proposal; artificial hunk partition is not required. Its description must
separate accepted local F0.VA corrections from paused F0.V/F0.1 functionality,
missing provider proof and all human/product gates. This is not a synthetic
repository import: both original histories still precede a later merge.

## Verification and boundaries

No source, test, script, build, CI or normative-document edits. The executor
may retain redacted review/verification evidence under
build/pm-preservation-precommit-01/ only; do not stage it. Main owns docs.
Granted verification, owned exclusively by Terra:

- npm test --prefix tools/frontier-v3-test-pilot
- ./gradlew guardrails --no-daemon
- git diff --check in each repository
- read-only identity/content/evidence checks needed for this packet.

Reuse accepted critical290/290, packaging and r15 native evidence only after
proving unchanged relevant inputs; do not relabel historical timings as current.
Identify any remaining mandatory precommit gate or evidence gap and propose
its exact command. Full Gradle check/GameTests/build and native launches are
not granted here, avoiding writes to served build/libs during this review.
New source fixes or heavy verification require an explicit amended grant.

## Acceptance and stop conditions

AC-1: all intended paths accounted for, sensitive/generated hazards checked
against actual contents, both repositories and linked topology preserved.
AC-2: full diff reviewed for snapshot safety, concrete blockers and known WIP
separated, no blanket claim of foundation or product acceptance.
AC-3: scoped fresh checks pass, reused evidence has exact identity justification,
remaining gates named without duplicate whole-suite execution by the engineer.
AC-4: proposed exact staging manifest and preservation message describe the
unchanged final tree and paused work honestly.

This order grants audit and listed verification after acknowledgement, not
staging, commits, bundles, clone, migration, remote operations or service changes.
Deliver the stable packet or a material blocker; no intermediate status needed.
Engineer alone accepts the packet and grants commit execution next.

## Engineer acceptance and commit grant, revision2, 2026-09-06

AC-1–AC-4 ACCEPTED for preserving explicitly unfinished WIP. Terra reports
actual-content sensitive/generated review, full104-entry tracked diff review,
175/175 Node tests (396.748ms),34 guardrails18s and both diff checks PASS.
No saved precommit evidence directory was produced; these fresh check results
are tool-session evidence, not claims of retained log files. Main inspected
current numstat and independently invoked requirePreparedF0vBuild against
build/pm-f0va-native-qual-01-r15/prepared-build.json: PASS for1539 source entries,
source61ba53d8b73095aed18db3a8cfaf012fe03ad4cd8c86fa37777de117c16d2e72,
JAR e4eefc3deab557c382fd79eeef4c35c22903a019a859faaec219c60123e5a996,
including actual launch/classpath hashes. Existing accepted critical/native
evidence remains scoped; no feature/product or provider acceptance is inferred.

Granted exclusively to Terra: one local non-amending preservation commit in
the nested main repository, parent exactly0ac6f97a695d636ae928313de7a807d1f3ea05a1.
Stage only the previously inventoried239 explicit paths plus this order
(240 total), including the already intended deletion. The only permitted
post-review content changes are main's revision2 in this order and current
ledger acceptance/grant text. Rerun docs guardrails/both diff checks for those
changes, then freeze bytes. Expand the reviewed manifest to literal paths;
do not use blanket git add, force-add, or stage all of a directory by default.
Compare actual paths with the reviewed set and record per-path bytes/modes
before staging. Verify staged tree exactly matches that snapshot, no extra
staged path exists, and prepared identity still passes before commit.

Use title: chore(frontier): preserve local F0.VA snapshot and paused F0 work

Body must state: preserve source/harness/CI/tests/governance before later
history-preserving migration; includes locally accepted F0.VA corrections;
retains unfinished F0.V/F0.1, with provider/original-matrix/human gates open.

After commit, prove exact parent, changed-path set, final tree bytes/modes,
clean nested tracked/untracked status and unchanged outer HEAD/status. Keep
ignored local artifacts, linked worktrees/common metadata and services intact.
No hooks/config changes, amend, resets, ref rewriting, outer commit, bundle,
clone, remote operation or migration authorized. A failed check/changed source
stops this commit grant; retain staged state and report, do not auto-reset.
Report commit/tree IDs, evidence and both repository states, then wait.

### Revision3: staged whitespace correction

The first staged diff check rejected a trailing blank line at EOF in
docs/work-orders/PM-F0VA-NATIVE-RUNTIME-IDENTITY-01.md:105. No commit occurred;
the240-path index was retained. Main removed only that final blank line.
The prior checks on unstaged changes did not cover this newly added file.
Include git diff --cached --check as an explicit final check covering additions.
Renew the same single preservation-commit grant after re-freezing only this
corrected document, this revision record and the ledger update. All other
previously frozen path bytes/modes must remain identical. Refresh docs guards,
explicitly re-stage these three literal paths, verify full staged path/content
equality and prepared identity, then perform the originally authorized commit.
No new paths, code changes or expanded authority; stop on any other drift.

### Revision4: staged deletion inventory transition

The r3 stop retained exact tree8929f8d1711a7e990944dba711eabd357d5f22d4.
Terra's attribution to governance docs is rejected by independent evidence:
those paths are outside PREPARED_SOURCE_PREFIXES. Main compared complete
current and r15 source manifests. The only difference is removal of the
MISSING record for the already absent ResourceSiteHarvestTraversalAdvanced.java
under pale-mirror-frontier/src/main/java/io/farfrontier/palemirror/frontier/v3/model/.
Staging its intended deletion removes it from git ls-files --cached; no source
bytes changed. Historical1539-entry hash61ba53d8 becomes current1538-entry
hash87460b9323c029caa860978641772959952750a01de55d24a3fe165ae9ce685b.
Main requirePreparedBuild PASS and fingerprintPaths over all historical paths
exactly equals the original r15 sourceContent, including the absent-file marker.

Renew the same single commit grant. For this preservation operation only,
replace the impossible raw pre-staging inventory equality condition with all
three checks: unchanged artifact/launch/classpaths via requirePreparedBuild;
exact historical-path fingerprint equality via fingerprintPaths; exhaustive
manifest delta containing only the above MISSING-record removal. No additional
path, content or state difference is permitted. Retain old prepared JSON and
native reports unchanged; current hash is not relabelled as historical identity.
No validator/cache source change or automatic reuse across distinct keys is
authorized. Future native work prepares its own identity normally.

Re-freeze/re-stage only this revision record and the ledger; all other238
frozen blobs/modes stay exact. Refresh docs/cached diff checks, staged snapshot
proof and these three identity checks, then the original preservation commit.
Report both inventory hashes and the explicit equivalence proof. No heavy
rebuild is required for a Git membership change without changed source bytes.

## Preservation commit accepted

2026-09-06: b82c0406c50fd85dda08d8bd27114541bf8aec32 created, exact parent
0ac6f97a695d636ae928313de7a807d1f3ea05a1 and tree
f67ad99e4ed219cd2ddbd71f87ec506804279a26. Main independently compared all240
changed paths and final blob/mode values against frozen-paths.tsv with only
approved r3/r4 document overrides: PASS. Main read commit message and confirmed
clean nested status; outer only .f0v-baseline/, unchanged per Terra. Terra
reports34guards18s, all diff checks and r4 identity equivalence PASS.
This completes the preservation order, not paused functionality or F0.VA.
All grants here closed. Next: PM-MONOREPO-RECOVERY-01, backup step only.
