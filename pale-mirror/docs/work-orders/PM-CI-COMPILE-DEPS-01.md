# PM-CI-COMPILE-DEPS-01: reproducible visual compile dependencies

Current revision11; earlier sections retain historical decisions and are
superseded by later revisions. Parent F0.VA monorepo/provider qualification. Risk small-code build
boundary, subject to module-specific verification. Engineer /root; sole
executor /root/terra_crash_completion, gpt-5.6-terra high.

Interpretation rule: the global protocol's binding anti-micromanagement
amendment and the latest revision govern execution. Earlier READ_ONLY, Gate A,
Gate B, exact-command and intermediate-return language is historical evidence,
not current permission machinery. Terra owns the complete authorized outcome;
the engineer may intervene only under one of the protocol's exhaustive named
reasons and may specify outcomes/invariants, not an implementation recipe.

## Baseline and prerequisites

Candidate /home/rd/proj/pm-monorepo-assembly.VJM6JZ at published
b3d04959b6c0c1d51164f92bd35ad4483e0d825c. Preserve all diagnostics and original
repositories; original CONTINUITY.md remains canonical. Previous order's
publication grant is consumed and executor reports no active task.

Actual provider run34013738944/job101433861671 failed visuals compileJava:
missing GeckoLib and Villager Overhaul symbols. Main independently checked
exact run identity, failed log and three compileOnly host-file declarations.
Create shares the same undeclared host dependency although it did not produce
the reported errors. Node/Python bootstrap succeeded; private input split is
not implicated. Read project protocol, scoped AGENTS, relevant architecture
build/integration boundaries and complete release-verification skill/matrix.

## Outcome and exclusions

Make the three existing visual compile dependencies reproducible from a clean
checkout without installed server mods, preserving exact versions/artifact
identity and failing closed on invalid inputs. Terra owns diagnosis, dependency
acquisition design, helper placement and focused regression choices. Audit the
remaining core build graph for the same missing-input class while in scope;
report additional boundaries before expanding implementation.

No gameplay, visual assets, adapter ABI/version upgrades, runtime packaging
semantics, private-art publication, production server changes or migration
adoption. No new Create functionality. Existing explicit local overrides must
not silently bypass integrity or change accepted dependency meaning.

## Contract and proposed write boundary

Candidate-only Gradle dependency acquisition/wiring and pinned metadata,
associated engineering/CI recurrence tests and necessary core setup wiring.
Expected starting points: visuals/build.gradle, existing root/subproject
verified-download mechanics, gradle.properties and current Packwiz pins.
Pack metadata is evidence of an existing pin, not proof of classpath delivery.
Do not copy binaries into Git or invent latest versions. Conflicting or missing
authoritative pins require a decision before implementation. Existing runtime
mod folders are read-only; no HOME reassignment or cache/world cleanup.
Normative documentation and ledger remain engineer-owned.

## Acceptance matrix

| ID | Required evidence |
| --- | --- |
| AC-1 | All three exact existing pins traced to authoritative metadata; required artifacts and their compile/runtime distinction explained. |
| AC-2 | Clean-input compilation succeeds with no server-mod folder access and no stale compiled-output substitution; record isolation method and exact command. |
| AC-3 | Missing or corrupt artifact/checksum fails before Java compilation or reuse; valid cached artifact is verified under declared identity. |
| AC-4 | Deterministic focused recurrence checks cover all three declarations, clean resolution and rejected inputs; unchanged public/private test obligations. |
| AC-5 | Fresh scoped small-code gate, visuals module gates required by release skill, core package verification and eventual actual provider result on reviewed identity. |

Runtime/GameTest requirements must be identified explicitly: the visuals build
also declares eight local runtime JARs and deletes its disposable GameTest
world. Do not grant or execute that deletion implicitly from this order.
Gate A must propose exact safe verification scope and identify remaining
runtime provisioning rather than claiming a compile fix proves native readiness.

## Review and execution permissions

READY for Gate A READ_ONLY: inspect repository/metadata/provider logs and
existing receipts; return one concise packet with exact pin reconciliation,
bounded proposed footprint, unresolved decisions and clean/negative/final
verification plan. No file writes, downloads/installations, test launches,
commit/push/dispatch, settings, native/server operations or cleanup yet.
Explicit IMPLEMENTING grant follows this acknowledgement. Subsequent local
choices inside the accepted outcome will not require per-helper review.
Review stable milestones; no routine polling within ten minutes or duplicate
full investigation. Preserve failed provider run as failed; no blind retry.

## Stop conditions

Conflicting pins, unavailable public artifact, changed versions/ABI, new runtime
authority, unexpected WIP/writer, private data delivery, or broader build graph
changes: report evidence and alternatives before expanding scope.

## Revision2: Gate A acceptance and implementation grant

Engineer accepts Gate A acknowledgement (executor's word ACCEPTED is not
self-acceptance authority). Main independently read Packwiz URLs/SHA512 and
existing Create properties: GeckoLib4.9.2, Villager Overhaul3.10.17.16 and
Create6.0.10 have coherent exact pins. No version change is required.

IMPLEMENTING granted inside the proposed candidate-only boundary above.
Terra owns local implementation and focused iterations; preserve compileOnly
semantics, verify defaults/cached artifacts/explicit overrides, and prevent
stale task caching from hiding changed or invalid bytes. No requirement to
use a particular helper design. Keep runtime provisioning behavior unchanged.

Terra is sole heavy owner for focused tests and isolated
:pale-mirror-visuals:compileJava verification, sequentially. Downloads of the
three publicly pinned artifacts and ordinary declared build dependencies into
candidate or newly created disposable build environments are allowed. Use
fresh task outputs or a safe isolated build to prove compilation actually ran;
do not delete existing outputs, worlds or runtime directories. Do not repurpose
HOME or mutate installed server mods. Negative tests must exercise actual
resolution/verification behavior, not only inspect build-script text.

Return stable Gate B with complete diff, exact focused commands/results,
retained genuine logs, clean-input isolation and negative evidence, source
identity and both original/candidate dirty states. No full check/package,
GameTest, integration harness, client/native run, commit/push or dispatch yet.
Final module/runtime gate planning remains outstanding, not waived. Main's
original documentation diff-check/guardrails may run once sequentially after
Gate B, coordinated with Terra; no competing heavy process.

## Gate B review: correction within revision2

Main read the complete three-path diff at reported source1539/db5f4113…26a2d.
Gate B is NOT accepted yet. Exact pin metadata and compileOnly intent match,
but the resolver declares cached outputs without declaring actual override
file contents as inputs. A successful override invocation followed by changed
bytes at that same path can therefore avoid doLast verification on ordinary
up-to-date reuse. Forced task execution cannot discharge this invariant.

Terra may resume implementation under the same focused grant to close:

- Prove the actual resolver rejects same-path mutated and missing override
  bytes after a successful warm invocation WITHOUT --rerun-tasks. Cover all
  three artifact declarations and corruption of default cached content too.
  Select a correct Gradle task/input model; engineer does not prescribe it.
- Retain executable behavioral regression coverage in the repository or its
  focused test harness. Current new Node test inspects source text and pins;
  it cannot by itself catch checksum verification skipped by task reuse.
- Restore pre-existing runtime provisioning semantics, as revision2 requires.
  The changed visualsRuntimeJars entries and prepare dependency currently
  broaden this compile-only correction. If sharing verified runtime acquisition
  is necessary rather than optional, stop with evidence for an explicit scope
  amendment; do not silently retain this expansion or weaken checksum checks.

Supply absolute paths to retained logs and exact command lines, distinguishing
forced clean compilation from normal incremental verification. Existing failed
and green receipts remain scoped to their original bytes. Re-review stable
correction/focused evidence before full gates or publication. No new runtime,
server, cleanup or external publication authority is granted.

## Revision3: complete local outcome, no intermediate permission gate

User-approved 2026-09-06 anti-micromanagement amendment applies immediately at
the executor's next safe boundary. Original engineering-agent-protocol.md is
authoritative over stale copied Gate A/B wording in the candidate checkout.
Do not stop or restart an active measurement to adopt this governance change;
retain its original evidence identity and prepare normally for later tests.

Terra owns diagnosis through a completed, verified local correction. The
checksum/cache finding remains a required invariant to resolve, not a prescribed
implementation. No further Gate B approval is needed to select helpers, repair
tests or run the local full build gates below. Return one final result or an
actual conceptual/authority question or impasse; no heartbeat reports.

One sequential heavy-run owner: Terra. Approved environment is candidate
/home/rd/proj/pm-monorepo-assembly.VJM6JZ/pale-mirror and new isolated build/test
directories created for this order. In addition to focused tests and real clean
compile proof, the standing local verification grant now includes:

```text
./gradlew guardrails check :pale-mirror-visuals:build :pale-mirror-neoforge:build :pale-mirror-neoforge:verifyPackagedJar --no-daemon
```

Terra chooses sequencing and focused reruns after evidence-backed fixes; this
is not a requirement to repeat the whole suite after every edit. The existing
mandatory authentic-reference local gate is included with the authorized exact
private source and private temporary outputs, never upload/publication.
Original normative-docs diff-check/guardrails is also granted sequentially at
a safe boundary after main finishes this documentation batch, no source writes
in the original checkout. Record actual evidence once; no duplicate main run.

Replace the previous mechanical runtime-restoration demand with a semantic
boundary: unchanged exact artifact versions, checksum guarantees, explicit
override intent and runtime mod set are required. Terra may choose coherent
reuse of the same verified artifacts within existing build wiring if it can
preserve those guarantees, and explains material effects at final review.
No rewrite is required merely to preserve an internal path/layout preference.
New runtime requirements, adapter behavior or version changes remain out of scope.

Existing native/GameTest/integration-harness launches remain outside this
environment grant because their world/reset/runtime targets have not yet been
safely resolved. They are required evidence where applicable, not waived. If
needed, report one consolidated exact resource/runtime plan for that genuine
boundary; do not fragment it into per-command permission requests. Production,
existing worlds, installed mods, service processes and immutable remote clones
remain untouched. No commit/push/provider dispatch/paid resource/cleanup or
checkout-adoption authority is introduced. Final engineer acceptance is still
required; a partial local result must state outstanding physical/provider gates.

## Revision4: scoped local acceptance and Visuals GameTest resource envelope

Main reviewed all four changed paths and the executable recurrence harness;
independently matched source1539/1921b8eae41f0b67ca4984259ecfd772cc1bddfaf6d3f64e6b2447478aeb4452.
Actual warm-H0Iv0v transcript proves11normal successes and9expected integrity
failures, no --rerun-tasks or UP-TO-DATE. Clean compile5executed/2m47 and local
gate67tasks/18s (37executed/30up-to-date) verified from retained logs; packaged
core verification executed. Authentic-reference-green.log independently read:
actual private integration task passed1task/5s. Original documentation gate
34tasks/18s is executor tool-session evidence, not a retained file receipt.
The compile/integrity correction is technically accepted at that scope only.

Module-specific verification decision: Visuals GameTest and build are required.
The old packaged worldgen visualsIntegrationHarness is NOT required for this
compile-input-only order: no genesis/NBT, gameplay, recovery implementation,
resource content or packaged mod composition changed. Main read its script:
it uses explicit forceload diagnostics and offline mode without an explicit
loopback bind. Earlier references to that harness as necessarily outstanding
were overbroad. Do not run it under this grant, weaken it, or use it as Frontier
causality evidence. Its suitability for future actual genesis/packaging changes
must be resolved then; all Frontier native/provider/product gates remain open.

Terra owns the complete remaining Visuals GameTest cycle, no intermediate
permission exchanges. Approved resource boundary:

- Candidate source /home/rd/proj/pm-monorepo-assembly.VJM6JZ/pale-mirror;
  one heavy owner Terra, no client/display, no production service changes.
- Stage exactly the eight existing runtime JARs into a fresh mktemp directory
  beneath candidate build/pm-ci-compile-deps-01. Read-only copy from installed
  mods is allowed after full SHA512 comparison with each existing Packwiz pin.
  Revalidate after copying; no unpinned extras or installed-mod writes.
- Supply the stage through PALE_MIRROR_RUNTIME_MOD_DIR and the three existing
  explicit integration overrides, retaining their resolver integrity checks.
- Exact run directory is candidate pale-mirror-visuals/build/runs/visuals-game-test;
  exact disposable level-name is world. Terra reported mods/world absent;
  recheck absent/non-symlink targets before first setup. Unexpected existing
  content or redirected paths must be preserved and reported, not overwritten.
- Permit setup of this task-owned server.properties with server-ip=127.0.0.1,
  server-port=0 and level-name=world. Verify launch actually respects isolation;
  never fall back to live port25565. The existing task may Sync only its own
  run/mods and perform its existing deleteDir only on this exact run/world.
  No existing world deletion, broad cleanup or force-loading is authorized.
- Run :pale-mirror-visuals:runGameTestServer --no-daemon using the verified stage;
  include required visuals build/verification as appropriate. Terra selects
  command grouping, normal preflight and evidence capture. Retain logs, reports
  and resulting world; prove ordinary task completion and owned process exit.
  Record exact resolved paths, dependency/source/JAR identities and isolation.
- Check actual process/port ownership before and after. Unrelated live server
  (reported PID2330125/25565, frontier-v3-live-r71) and client artifact host
  remain untouched; discover current state instead of trusting old PID alone.

On a material runtime failure, retain evidence and diagnose. Local fixture/
preflight adjustments within this disposable environment are autonomous;
no blind world reset/retry, timeout inflation or changed product assertions.
A runtime implementation defect or missing dependency/version boundary requires
one consolidated report rather than expanding the compile-input task silently.
No integration-harness launch, provider dispatch, commit/push, deployment or
checkout adoption. Return final scoped acceptance packet; main does not rerun
the same gate. Original doc verification may run sequentially with this cycle.

## Revision5: complete ModDev test-classpath correction and verification

The first r4 GameTest failed before world startup/listen, not on integrity or
gameplay assertions. Main independently read the retained game-test.log at
build/pm-ci-compile-deps-01/visuals-runtime-r4.uexD65: NoClassDefFoundError for
frontier.v3.model.GrayboxCell. Main compared Visuals and core ModDev declarations:
Visuals omits the existing Frontier source set from its pale_mirror mod entry.
The failed r4 run remains failed and cannot prove port-zero startup or tests.

Extend this same build-input outcome to correct Visuals ModDev test-classpath
closure. Terra owns diagnosis, the smallest coherent build-wiring correction,
and executable/focused recurrence before another server attempt. Preserve mod
identity, source ownership, exact dependency versions and packaged gameplay
implementation. No production/domain/Frontier Java changes or new features.
Expected file is Visuals build.gradle plus focused build/test recurrence;
helper design is not prescribed. Report any genuinely broader defect.

The existing complete local verification grant and r4 isolated server envelope
remain in effect, including relevant focused/full gates, authentic local gate
where required, Visuals build and GameTests. No intermediate approval after the
local classpath fix. This is a cause-backed corrected run, not a blind retry.

Preserve r4 receipts before its run-directory logs can be overwritten. Terra
may recoverably move the exact task-owned candidate directory
/home/rd/proj/pm-monorepo-assembly.VJM6JZ/pale-mirror/pale-mirror-visuals/build/runs/visuals-game-test
into a freshly created archive directory under candidate
build/pm-ci-compile-deps-01, after verifying no owned process remains and the
source is not a symlink/redirect. Record the exact archive path. Recreate only
that task-owned run directory with the same loopback/port0/world settings and
revalidated eight-JAR stage. Do not delete the archive or stage. This preserves
failure evidence without permitting a module clean or other world reset.

Retain final logs, tested source/build identity, actual GameTest results and
process/port exit proof. Ordinary focused correction iterations remain local;
new material runtime failure requires evidence-based diagnosis, not increased
timeouts or weakened tests. One consolidated final packet; no heartbeat.
No live service/client, legacy integration harness, provider dispatch,
commit/push or adoption authority. Original docs verification remains a
sequential part of the envelope, not a parallel engineer run.

## Revision6: complete authored-input test-runtime closure

The corrected r5 classpath reaches actual GameTest execution. Main read
game-test-r5.log lines528–542: fourteen tests report a missing authored block
alexscaves:dinosaur_chop. Executor reports39tests,14required failures and
normal saved-world shutdown. This is not yet proof whether the runtime lacks
a dependency or the authored data has an invalid registry reference.

Terra owns a consolidated diagnosis of the complete authored-input dependency
closure, not repeated launches to discover one missing name at a time. Inspect
the relevant project skills, all applicable authored palettes/references and
existing authoritative pack pins, including transitive runtime dependencies.
Distinguish valid block requirements from item-only, obsolete or invalid IDs.

Within this same outcome, authorize candidate-only build/test provisioning
corrections using existing compatible public Packwiz-pinned artifacts with
verified checksums. The earlier exact-eight restriction is superseded only
for this evidenced test-runtime dependency closure. Preserve production pack
composition, versions, source semantics, assets and assertions. No new mod
selection/version, invented registry entries, silent palette substitutions,
asset rewrites or gameplay changes. Missing/conflicting authoritative pins or
an actual authored-content defect require a consolidated conceptual decision
with evidence and alternatives, not a guessed replacement.

Diagnosis, bounded implementation, recurrence and applicable full local gates
are one autonomous assignment; no intermediate design approval is needed.
The existing loopback/port0, sole-heavy-owner, private-reference and source-only
limits continue. Before a cause-backed rerun, preserve the exact task-owned
r5 run directory using the recoverable archival procedure in revision5; retain
all prior archives. No blind retry, timeout increase or weakening of tests.
Return the final tested diff/evidence, or a genuine boundary decision. No
commit/push, provider launch, live service operation or migration adoption.

## Revision7: supported authored provisions, explicit semantic remediation

The executor's completed palette audit corrects the earlier count: six real
placements in THREE bunkhouse_2 assets (temperate, cold_taiga, dry_arid), each
15x14x19, at local (4,2,6) and (4,3,6). There is no block entity or canonical
resource binding. The source states are facing=up, bites=0/2, waterlogged=false.
This is an authored-module compatibility defect, not missing test provisioning.

Engineer approves the remediation direction: replace these imported meat props
with the existing pinned Farmer's Delight rice_bag provisions. Accept the
explicit loss of raw-meat/bitten silhouette and eating behavior; do not claim
exact collision equivalence (bite2 was partial). Preserve housing identity,
capacity, ports, footprint and canonical economy. Prove that the full-cube
replacement does not obstruct circulation or create unsupported/floating props;
an unexpected support/layout problem requires reconsidering the composition,
not declaring the old occupied coordinates sufficient evidence.

Terra owns this complete bounded correction in the candidate, risk critical-code:
three NBT derivatives and their catalog hashes, reproducible import/curation
recurrence where needed, and focused tests. No generic missing-block fallback,
new mod, registry alias, unrelated asset edits or changes to existing worlds.
Preserve originals for comparison and decoded before/after evidence. Normative
provenance documentation remains engineer-owned; report the exact change for it.

Required tests cover actual templates, exact affected positions/properties,
unchanged dimensions and unrelated decoded contents, no introduced block entity,
all climate variants, and unknown-ID rejection through the real compiler.
Check the complete authored palette set for the excluded dependency, not strings
inside unrelated item payloads. Preserve all r5 failure evidence as previously
authorized. Existing local focused/full build and Visuals GameTest envelope
continues, with one consolidated final packet rather than intermediate approvals.

This is now an NBT/genesis correction: revision4's compile-only exemption for
the integration harness no longer discharges acceptance. Foundry compiled AND
settled evidence, applicable critical gate, packaged integration/restart and
real before/after visual comparison remain required before whole-order acceptance.
Do not run the unsafe legacy forceload/non-loopback harness as-is, claim a
GameTest count proves visual quality, or use the live world for comparison.
Before asset mutation, retain available before evidence and identify any missing
capture requirement. If the existing isolated envelope cannot supply these
physical/visual gates, return ONE consolidated safe resource/harness proposal;
this is an actual environment boundary, not a per-command approval loop.
No ungranted native/client launch, publication, deployment or adoption.

## Revision8: consolidated physical verification authorization

Engineer accepts Terra's r7 resource proposal with the following explicit
evidence boundaries. This grants the complete correction, harness work and
verification cycle, not another mandatory intermediate review checkpoint.

- A supported-runtime BEFORE image cannot exist for the unresolved old block
  while Alex/Citadel remain excluded. Preserve the exact original NBT bytes,
  decoded cells/properties and available private source-model evidence before
  mutation. Label these source evidence, never a runtime screenshot. Produce
  genuine player-eye AFTER images and manually inspect the surrounding room,
  support, door and circulation in each climate. This is a documented baseline
  limitation, not acceptance of visual equivalence or permission to fabricate it.
- Required settled/reloaded scope for this correction is the affected modules
  and their support/circulation, not the entire region plus distant mines/rail.
  Use exact loaded-world observations and existing Foundry exports, retaining
  whole-region unverified findings honestly. Every cell needed for the scoped
  claim must be loaded and checked; no unknown/pending local cell may pass.
  Do not introduce a new production audit owner or widen this into a general
  Foundry framework redesign. Full-region visual/product gates remain open.
- Task-owned resource root is
  /home/rd/proj/pm-monorepo-assembly.VJM6JZ/pale-mirror/build/pm-ci-compile-deps-01/r7-physical/.
  Create unique run directories beneath it, resolve their exact server/world,
  client, log and archive paths before launch, reject symlinks/foreign contents.
  No broad clean or existing-world deletion. Retain every failed/successful
  world and its identity-bound logs/screenshots/reports.
- One heavy owner Terra; one isolated audit client on the standing DISPLAY=:0
  after rediscovering authorization and confirming no competing client. The
  proposed Xvfb substitution is not the real-display evidence granted here.
  Use separate task-owned client options/mods/run directory, never the shared
  railway client or a real player's account/session. No secret logging.
- One task-owned server at a time, explicit server-ip=127.0.0.1 and port0;
  discover the actual bound port for the isolated client. No fallback to25565.
  Use exact verified compatible pinned runtime dependencies and tested core/
  Visuals artifacts. No excluded mod or new production pack composition.
  Ordinary dependency acquisition and private local copying into this root
  are permitted with checksums; no upload or distribution.
- Terra may implement and test the necessary replacement for the unsafe legacy
  integration/capture harness, in candidate test tooling only. Retain the
  applicable packaged genesis/restart assertions while removing dependence
  on forced chunks or shared mutable/live paths. Test isolation/cleanup failure
  paths before launch. This is not a waiver of visualsIntegrationHarness's
  applicable verification purpose; explain the replacement coverage at final
  review. No separate pre-launch approval is needed after those checks pass.
- Natural player demand, including explicit audit travel to generated semantic
  module locations, is allowed. No forceload/tickets, world-file mutation,
  injected canonical state or replayed placement masquerading as production.
  Existing supported fresh-world profiles/seeds may cover climate variants
  sequentially; retain all identities. Do not force a biome or weaken terrain
  admission merely to obtain a passing screenshot.
- Save/stop and restart each tested task-owned world for the scoped reload
  proof, then ordinary rejoin/observation. Stop only owned processes; verify
  process exit and ports, and preserve unrelated live server/artifact services.
  An unavailable real display or unsupported launch profile is an actual
  blocker to report, not permission to touch the live service.

All focused/full local gates, source-only/private-reference limits and original
docs verification remain granted sequentially. Terra chooses helpers and test
sequencing and repairs evidenced failures within this outcome autonomously.
Return one final packet or a genuine conceptual/authority impasse. No provider
dispatch, commit/push, deployment, repository settings or adoption. Publication
also awaits the user's outstanding asset-provenance decision.

## Revision9: metadata-driven isolated client dependency closure

The first isolated client launch is retained as a genuine failure: the task-owned
server reached Done on loopback/port42533, while the client failed before join
because Villager Overhaul3.10.17.16 declares client-required ezactions>=2.0.1.7.
Main matched that declaration in the staged JAR and the crash report. The outer
pack documentation already selects EZ Actions2.0.3.5 for this Villager Overhaul
compatibility family, but no current Packwiz entry supplies it. This is a real
test-runtime authority boundary, not a reason for per-artifact supervision.

Authorize Terra to close the complete recursively declared client load graph for
the already selected Villager Overhaul artifact inside r7-physical only. Start
from the documented EZ Actions2.0.3.5 choice; acquire official public artifacts,
record authoritative download identity and checksum, and inspect actual JAR
metadata before launch. Stage another documented companion only when the resolved
metadata makes it required for this client path; do not add optional conveniences
merely because they share the compatibility family. A missing authoritative
artifact, version conflict, undocumented required gameplay mod or need to change
the production pack remains a genuine decision boundary.

This does not approve a Packwiz/installer/production-profile change, a version
upgrade, or new gameplay. Keep all added files and caches under the task-owned
resource root, never publish binaries, and retain the failed client evidence.
Add or extend a preflight/recurrence check that resolves the complete required
client graph before launching so subsequent missing dependencies are not found
one restart at a time. Terra owns acquisition design, helper/file choices,
sequencing, cause-backed retries and all remaining revision8 acceptance work.
No further intermediate approval is required inside this closure. Return only a
completed coherent result, a new genuine boundary, or an evidence-backed impasse.

## Revision10: consolidated Gate C findings before the Foundry prerequisite

Gate C accepts the reported real same-client join and the SETTLED failure as
genuine evidence, but returns this order to EXECUTING for three bounded proof
gaps. These are invariant/evidence findings, not prescribed helper designs:

1. The metadata preflight must reject a present but incompatible required
   dependency and identity-bind the complete final client module graph. Current
   `verify_r7_physical_client_graph.mjs` parses dependency ID/type/side but drops
   module versions and dependency version ranges. Its retained graph lists
   twelve external top-level archives while the successful client stage also
   contains `pale_mirror_visuals-hosted.jar`; consequently the report does not
   check that host's declared Pale Mirror, GeckoLib, Villager Overhaul,
   Supplementaries and Create constraints. A separate hash records bytes but
   does not prove metadata closure. Terra chooses how source-provided modules
   and staged JARs are represented, but no final load input may be implicit.
   Add executable negative coverage for an installed incompatible version and
   for an unaccounted final top-level/provider input.
2. The new arbitrary-unknown-registry test now lives in
   `MaterializationMatrixGameTests`, but the final packet reports only
   `compileJava` plus a JUnit `CuratedModuleCatalogTest`. The earlier39/39
   Visuals GameTest result predates this new GameTest. Execute the smallest
   matching real GameTest gate on the corrected identity and retain its result;
   a compile is not execution evidence.
3. The NBT acceptance requires unchanged unrelated decoded content. The current
   curator verifies dimensions, the two expected cells, replacement properties
   and final hashes, but it neither compares the preserved original decoded tree
   with the result nor proves that the edited palette entries are referenced by
   no other cell. Add an executable before/after semantic comparison showing
   that only the approved `Name` and removed `Properties` at the six placements
   changed and every other decoded tag/value/reference is identical.

Terra owns all local design, implementation, focused iterations and applicable
final gates for these findings under the existing resource envelope. No
intermediate approval is required. Preserve all r7-physical evidence and do not
retry/reset or repair the discovered Foundry world in this revision.

The meaningful player-loaded SETTLED report remains failed with105 errors and
one unverified-chunk warning. Main independently confirmed the report phase,
catalog hash, metrics, representative findings and player join/export log, and
inspected the three delivered temperate PNGs. Those frames do not show the rice
bags or prove the three climates/reload; physical/visual acceptance is therefore
open. The errors are not located at the six substituted cells and establish a
separate reusable terrain/materialization-order prerequisite. After the three
proof gaps close, the engineer will issue that bounded critical-code order; no
Foundry source broadening is granted here.

## Revision11: r10 evidence accepted; physical acceptance suspended

Gate C independently accepts the three revision10 proof corrections on the
current candidate identity:

- `client-runtime-graph-r10.json` binds the actual thirteen top-level client
  archives, all recursively declared jar-in-jar archives, nineteen modules,
  forty-two required client dependency edges and the three explicitly provided
  modules. Its final graph SHA-512 is
  `3095b261667141dbe5874f38af2a7568fc9fbaa5baae75efa38ce893047250ee4431d7d3503cbd347322c1e76952a336b17870a5f3f6c3d49c482d489b80eb96`;
  the retained stage reproduces that identity with zero missing, incompatible
  or unused-provided entries. Executable negatives reject missing,
  version-incompatible, unexpected top-level and unaccounted provided inputs.
- The changed `MaterializationMatrixGameTests` class was compiled before the
  retained Visuals GameTest run. The genuine server log records all forty
  required tests passing followed by ordinary world save and shutdown.
- The curator now requires a retained before root for `--check`, deep-compares
  every decoded root tag and block reference, and proves that only the two
  approved palette `Name`/`Properties` entries per climate changed. Main
  independently reran the six lightweight Node recurrence tests and the exact
  preserved-before check; all three current assets reproduce SHA-256
  `44592dbb3b6330341e03399531ef24292729ccf862fe3d8e3fb207008b1682fe`.

This accepts the r10 evidence correction, not the whole work order. The
compile/dependency portion is locally accepted, while NBT physical and visual
acceptance remains suspended behind `PM-FOUNDRY-SETTLED-ORDER-01`. The failed
r7 world/report and incomplete frames remain negative baseline evidence. Do
not publish, commit, adopt the candidate, retry that world or claim the three
climate/reload gate from this acceptance.
