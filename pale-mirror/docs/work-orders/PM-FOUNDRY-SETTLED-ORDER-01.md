# PM-FOUNDRY-SETTLED-ORDER-01: real-terrain materialization ownership

Specification revision: 2. Parent slice: deferred real-terrain hardening. Risk:
critical-code. Engineer: supervising root. Executor:
`/root/terra_crash_completion`, `gpt-5.6-terra`, reasoning `high`.
Active phase and acceptance owner: `CONTINUITY.md`.

## Revision 2: architectural suspension

Revision 1 incorrectly promoted production natural-terrain cleanup into an
F0.VA prerequisite. The contract and implementation plan allow the current
graybox provider to remain physically flat and require controlled non-flat,
blocked-port and restart fixtures for provider neutrality; they explicitly do
not require the production terrain surveyor, settlement earthworks or a clean
whole authored region in this gate.

This order is therefore `SUSPENDED_DEFERRED`, not failed and not accepted. No
further natural-terrain run, correction or visual capture is authorized under
it. Preserve all implementation WIP and `physical-8` through `physical-11` as
diagnostic input for a later bounded terrain-provider/hardening order. None of
those results may be promoted to graybox or F0.VA acceptance, and their open
findings do not block F0.VA unless independently reproduced at an applicable
graybox/provider-neutral boundary.

The safe-boundary handoff records no surviving task-owned server, client,
Gradle or GameTest process and no listener on the task ports. It also retains a
cause-backed but unverified correction for early genesis origin selection in
`FrontierTerrainSurvey` plus its focused regression. That WIP is deferred with
this order; it is not accepted, discarded or included in the next order's
required result.

## Baseline and prerequisites

Work only in the isolated monorepo candidate at
`/home/rd/proj/pm-monorepo-assembly.VJM6JZ`. Preserve its published HEAD
`b3d04959b6c0c1d51164f92bd35ad4483e0d825c`, all tracked WIP from
`PM-CI-COMPILE-DEPS-01`, all untracked assembly diagnostics and every retained
test world/report/log/frame. The original two checkouts, recovery bundles,
remote verification clones, live services and shared player/runtime paths are
read-only and out of scope.

Revision11 of `PM-CI-COMPILE-DEPS-01` accepts its three r10 proof corrections
but suspends physical/visual acceptance. The immutable failure baseline is:

`pale-mirror/build/pm-ci-compile-deps-01/r7-physical/run.NkPYpX/server/world/pale-mirror/foundry/pale_mirror_iron_frontier_72cb352355d9-settled-tick-36827/report.json`

It is a genuine player-loaded `SETTLED` report for region
`pale_mirror:iron_frontier_72cb352355d9`, catalog
`7e14475ec8bcace96bcbddfc48081b06db359e77887bd571e8a0271d6f48ceb6`:
108240 compiled cells, 33 modules, 98695 checked cells, 128 loaded and 19
unloaded chunks. It contains 105 errors: 55 `surface.datum.drift`, 30
`world.cell.mismatch`, nine `surface.walkable.missing`, nine
`world.cell.cannot_survive`, and two `navigation.entrance.blocked`; it also has
nine structural floating-component warnings and one honest unverified-chunk
warning. Preserve the failed world exactly. It is diagnosis input, never a
world to repair, reset or retry.

Read the current protocol, architecture flows `authored-frontier-genesis` and
`foundry-materialization-audit`, Foundry/Visuals documentation and the complete
Foundry, visual and release-verification skills before implementation.

## Outcome and exclusions

Deliver one reusable real-terrain materialization result: after ordinary fresh
genesis and natural player loading, every writer observes the immutable plan's
ownership/order so required authored cells, surface datums, supports and
entrance throats remain correct at `SETTLED` and in the same saved world at
`RELOADED`.

Terra owns the complete diagnosis, local design, implementation, test design,
command sequence and ordinary correction loop. Classify every one of the 105
baseline errors and all nine structural floating warnings by reusable cause
family and owning layer. A cause map may group findings when the grouping is
demonstrably complete. A genuine audit false positive may be corrected only by
showing the general invariant and adding stable positive/negative coverage; it
may not be hidden, downgraded or filtered to make this seed pass.

The result must fix the owning compiler, plan, materialization phase, late
writer, module grammar or audit contract. Coordinate/seed-specific patches,
desired-state repair, forced chunks, post-hoc block replacement, report
suppression, weaker severity/assertions, broadened tolerances without a
physical invariant, or retrying until terrain happens to pass are prohibited.

No domain gameplay, HOT/COLD economy, AI, movement, new assets, new dependency
versions, Packwiz/profile changes, production operation, publication, commit,
push, checkout adoption or v2 work. Do not expand into general visual polish;
only structural readability needed to prove support, access and the already
approved rice-bag substitution is in scope. If the correct fix requires a new
public/persistent semantic owner or pure-domain contract, stop with evidence
for an architecture decision rather than silently broadening.

## Contract and transition

- The immutable manifest, compiled catalog and `SiteSurfacePlan` remain the
  only desired physical geometry. Minecraft chunks are observed materialization,
  never a second source of truth.
- Genesis/materialization owners write each planned cell in one declared phase.
  Terrain grading, curated NBT, circulation, perimeter, decoration, block-entity
  finalization and chunk-load reconciliation must not overwrite another
  owner's later or exclusive result.
- A clean `COMPILED` catalog is necessary but not sufficient. `SETTLED` follows
  neighbour updates, gravity, fluids and every admitted late writer;
  `RELOADED` observes the same ordinarily saved/restarted world.
- Runtime audits remain bounded and read-only. They do not load, ticket, mutate,
  repair or adopt matching-looking unclaimed cells. Every loaded cell in a
  declared proof scope is `CURRENT`; no `PENDING`, conflict or unverified cell
  may be represented as success.
- A foreign/player change remains a locatable conflict/finding and is never
  repaired automatically. Fixing initial materialization must not weaken this
  reverse-causality boundary.
- Structure support and survival use actual Minecraft predicates after all
  updates. Public/facility entrances preserve their semantic throat, two-body
  clearance and connection to the compiled public topology.

## Write scope

Approved candidate components are the existing Visuals genesis/catalog/surface
and Foundry owners, NeoForge authored-region registration/materialization
integration, and their candidate-only tests or disposable audit tooling.
Build/test wiring may change only when necessary to execute this outcome under
the existing pinned runtime. Expected areas are navigation hints, not an exact
file list; Terra may choose helpers and internal structure without approval.

Normative Markdown, `architecture.yml` and the original `CONTINUITY.md` remain
engineer-owned. Production pack/deployment files, remote workflows and
unrelated modules are forbidden. Report an evidenced owner-boundary conflict
before crossing it.

## Acceptance matrix

| ID | Required outcome/invariant | Focused/negative evidence | Physical/final proof |
| --- | --- | --- | --- |
| FS-1 | Complete cause ownership for the baseline | Machine-readable or concise deterministic mapping covers all 105 errors and nine structural warnings with no orphan finding | Preserved baseline and before/after report comparison |
| FS-2 | Stable materialization order/ownership | Positive ordering coverage plus a late-writer/overlap case that would reproduce the old drift; repeated compile/materialize identity is deterministic | Fresh same-profile region has zero error/blocker in every naturally loaded baseline-target scope at `SETTLED` |
| FS-3 | Surface, support and entrance correctness | Non-flat fixtures cover datum, supported falling/attached states and a blocked/damaged entrance; invalid geometry still fails closed | Every previously failing target family is loaded and `CURRENT`, with no pending/unverified cell in its declared scope |
| FS-4 | Player/world conflict remains causal | A separate disposable case changes a governed loaded cell and proves a stable conflict/finding with no repair or canonical mutation | Conflict survives ordinary audit/reload and stays isolated from a clean-world proof |
| FS-5 | Restart continuity | Recovery coverage proves no replayed one-shot NBT/finalizer and no order-dependent second write | Same clean world is saved, stopped, restarted and passes equivalent `RELOADED` scopes |
| FS-6 | Three-climate bunkhouse acceptance | Catalog/NBT checks remain green for temperate, cold-taiga and dry-arid variants | Real-display player-eye frames show both rice bags, support, doorway and circulation in all three climates at settled state and after ordinary reload |

The union of physical scopes must include every baseline error target, not only
the six rice-bag cells. A whole-region unverified warning outside that union is
reported honestly and prevents a whole-region-clean claim, but does not fail a
precisely declared naturally loaded scope. Conversely, a scoped pass is invalid
if any cell needed for that claim is unloaded, pending or omitted. Structural
floating warnings must be either eliminated or accepted individually with
general support evidence; aesthetics alone cannot dismiss them.

## Verification envelope

Terra is the sole heavy-run owner. Use only new unique directories below:

`/home/rd/proj/pm-monorepo-assembly.VJM6JZ/pale-mirror/build/pm-foundry-settled-order-01/`

One task-owned server at a time must bind `127.0.0.1` with port `0`. At most one
task-owned visible client may use the real `DISPLAY=:0` after rediscovering its
authorization and proving no competing audit client. No Xvfb substitutes for
visual evidence. Use separate options/mods/world paths, ordinary player travel
and natural chunk demand. Existing r10-verified dependency bytes may be copied
read-only after rechecking their hashes; no installed/shared mod directory may
be mutated. Resolve and reject symlinks or foreign contents before setup.

Each new failure is retained with its world, logs, reports and frames. A
cause-backed new world after implementation is allowed; blind retry, seed
shopping, world reset, force-load, manual world-file edit and broad clean are
not. Stop only owned processes and retain proof of normal save/exit and closed
owned ports.

Run focused tests selected by Terra, then the applicable critical boundary:

```text
./gradlew guardrails check :pale-mirror-visuals:runGameTestServer \
  :pale-mirror-visuals:build :pale-mirror-neoforge:runGameTestServer \
  :pale-mirror-neoforge:build :pale-mirror-neoforge:verifyPackagedJar --no-daemon
```

Provide a safe task-owned packaged genesis/restart harness that discharges the
purpose of `visualsIntegrationHarness`; do not invoke the legacy unsafe
force-load/shared-path behavior. If a Foundry rule changes, apply the complete
rule-authoring contract, including unloaded, player-conflict and restart cases.
Reuse accepted private-reference evidence only if its tool, wiring and reference
inputs are unchanged; otherwise rerun the mandatory local authentic-reference
gate without publishing private data. At the final safe boundary, also run the
original checkout's pending documentation `git diff --check` and `guardrails`
sequentially, without editing implementation there.

Retain exact source/spec/build/catalog identities, commands and reports. The
executor may use focused reruns after fixes and proceeds through all authorized
verification without intermediate permission.

## Review and execution permissions

Gate A is `AUTHORIZED`: diagnosis, implementation, local focused/full gates,
safe packaged worlds, real-display captures and ordinary corrections inside the
envelope are granted together. Gate B exists only for a genuine
architecture/authority boundary or evidenced impasse. Return one coherent
Gate C packet mapping FS-1 through FS-6 to artifacts and limitations.

No heartbeat or routine progress handoff. The engineer does not inspect
intermediate work. It intervenes only under the protocol's exhaustive reasons
and specifies violated outcomes, not implementation recipes. Commit, push,
provider dispatch, deployment and adoption remain ungranted.

## Stop conditions

Stop for an unexpected writer/WIP collision, shared/live path, unavailable real
display after bounded diagnosis, need for destructive/external authority,
dependency/version or production-pack change, new public/persistent owner,
contradictory immutable plans, or an evidenced repeated impasse. Preserve the
failure and propose alternatives; do not weaken the goal or evidence.

## Review record

Suspended by the supervising engineer after the user identified the graybox
scope mismatch. Revision 1 evidence remains useful diagnosis, but this order
has no F0.VA acceptance role. A future terrain milestone must issue a new or
explicitly resumed bounded order before implementation or physical runs
continue.
