# PM-MONOREPO-REMOTE-01: nonforce publication and independent clone

Revision1. Parent: accepted local ASSEMBLY-01; not provider or adoption gate.
Engineer /root; sole executor /root/terra_crash_completion, Terra high.
Risk: external repository publication, no gameplay or deployment changes.

## Accepted immutable input

Candidate /home/rd/proj/pm-monorepo-assembly.VJM6JZ, main
05712315a81bff8a3a42207fcb736b729ca82704, exact parent
9f3bcb12affc74b98fc000a58ecff782f9ea77c7,
tree f5b5750401c9bc03be8dcaf3214dbb452874eb22.
Main independently verified exact24 changed paths, authored governance bytes,
both original ancestors, unchanged prepared source1539entries SHA256
37081dda3cb9a64c5ca530c7aef2b9eef1826717945ec1dce5274f3dac6858e7 and
JAR96197fc6e15d0e0188907f4870ccce69c12d24f46b3d0f87aff3152f032a2ad9.
The accepted critical receipt and scoped Node/pack/docs evidence remain in
ASSEMBLY-01. Do not rerun heavy verification or edit this accepted candidate.

Original /home/rd/proj/minecraft/pale-mirror/CONTINUITY.md is canonical.
Read AGENTS, this order, engineering protocol, accepted assembly order and
implementation-plan migration procedure; apply release-verification.
User explicitly authorized publication to the exact SSH destination below
and gh use for CI. This order sequences that authority without force-push,
settings/security changes, paid provisioning or production effects.

## Granted bounded sequence

1. Verify candidate main/HEAD/tree/working and staged bytes still match the
   accepted commit. Retained .assembly-* and Python bytecode are local evidence,
   not publication inputs; never remove them merely to clean status. Confirm
   original HEADs/status unchanged and no competing candidate writer.
2. Inspect existing candidate remote configuration (including effective push
   URLs), repository identity/access and destination refs READ-ONLY first:
   git ls-remote git@github.com:RidiculousCircumstances/pale_mirror.git.
   Use existing credentials without printing tokens or private keys. Never
   disable SSH host-key verification. A missing/inaccessible repository or
   unknown host identity is a stop, not authority to create/configure it.
3. The ONLY destination is
   git@github.com:RidiculousCircumstances/pale_mirror.git. If origin is absent,
   after successful identity inspection add it with exactly:

   ```bash
   git remote add origin git@github.com:RidiculousCircumstances/pale_mirror.git
   ```

   Any different existing fetch/push destination or contradictory URL rewrite
   stops the grant. No original-repository config changes. Read-only fetches
   into candidate remote-tracking refs or a unique verification repository are
   permitted to prove ancestry; do not overwrite local main/tags.
4. Inspect all existing remote heads and tags. An empty destination is valid;
   related history must be proven, never presumed from a matching name. Stop
   on unrelated history, tag collision, or a main update that is not a fast
   forward. Do not delete refs, force, rebase, amend or manufacture a merge.
   Retain inspection evidence. A concurrent ref change requires reinspection,
   not a forced retry.
5. If all checks pass, publish ONLY the accepted main using
   git push -u origin main. No tags or additional branches are granted here;
   original refs remain recoverable in retained bundles/checkouts. Automatic
   ordinary push CI is an expected publication effect; no expensive native
   workflow_dispatch or self-hosted runner registration is granted yet.
6. Re-read remote main and require the exact accepted commit. Clone main
   independently from that remote into a new mktemp directory under
   /home/rd/proj/ with prefix pm-monorepo-remote-verify. Do not clone from local
   candidate, share object stores, copy local outputs or adopt this checkout.
   Prove exact HEAD/tree, both old ancestors, one root Git metadata directory,
   no embedded .git/gitlinks, clean tracked state and source fingerprint.
   Run root pack validation and focused monorepo/identity/selector Node tests
   there; ordinary lockfile-pinned dependency installation is permitted, with
   no pin updates or heavy/native/build/Gradle invocation. Retain clone/results.
7. Report pre/post remote refs, destination identity, push receipt, exact
   commit/tree, independent clone path/equality/fingerprint, checks and both
   original statuses. Read-only gh inspection of automatically triggered core
   CI, available runner metadata and workflow availability is permitted to
   inform the next provider gate; report URLs/status without dispatching jobs
   or altering settings. Do not claim a pending job passed.

On a failed condition, preserve outputs and diagnose read-only; report before
any code/config repair or broader action. Main independently accepts remote
publication/clone before provider orchestration. No heartbeat required.

## Non-authority

No source or normative candidate edits, additional commits, history rewrite,
cleanup, original worktree/ref changes, deployment, service changes, client
launch, provider matrix, runner installation, billing/security/visibility
changes or checkout adoption. Bundles, failed attempts and original worktrees
remain retained. Publication alone is not complete migration, F0.VA, F0.V,
materialization or human acceptance.
