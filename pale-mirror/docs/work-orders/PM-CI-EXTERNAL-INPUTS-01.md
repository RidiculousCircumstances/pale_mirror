# PM-CI-EXTERNAL-INPUTS-01: core gate input portability

Revision1. READ_ONLY diagnosis. Engineer /root; sole executor Terra high.
Original ledger canonical; candidate VJM6JZ at published735245608627eee1f8f0c5b9bd81edc2d84bafa2.

## Observed failure and outcome

Python bootstrap is accepted: provider run34012003946 actually installed
CPython3.11.16 and pinned Pillow/PyYAML, then failed the existing
testHunyuan2mvPreparation on missing external primary image
/home/runner/harvester_references/01_harvester_biomass_collector.jpg.
Main independently read the actual failure log, the exact three-path commit
and test source: it derives the input from an environment override or the
user home. Failure is not a dependency-import or workflow-admission defect.

Diagnose the input boundary of ALL core-gate engineering tests, not just the
first missing file. Identify external home paths, ignored fixtures/assets,
machine-only executables and environment assumptions that affect a clean
remote checkout. Separate contract/unit fixtures from actual art/reference
integration checks and explain each existing assertion's intended evidence.

Propose the smallest coherent portable test arrangement preserving those
claims. A synthetic fixture can prove transformation mechanics, not likeness
or verification of the supplied source image. Do not silently replace a
real-reference integration check with a synthetic green check. If a required
source image must be published, establish its provenance and publication
authority; existing local access alone is not permission to add it to public
GitHub. No private image upload, synthetic art promotion or weakening/skipping
tests is granted. Report genuine reference requirements for engineer decision.

Read scoped AGENTS, original ledger, release-verification and applicable lab
instructions before proposing changes. No model inference, model downloads,
canonical art edits or visual acceptance are involved in this diagnosis.
Read-only source/metadata/provider-log inspection is allowed; no edits,
installations, test/Gradle/native launches, commits/pushes, reruns/dispatch,
settings, cleanup, deployment or adoption. Return one Gate A packet with
root cause, footprint, proposed coverage-preserving remedy, scope and exact
isolated recurrence/acceptance proof. The implementation grant follows review.

## Revision2: user-approved split and implementation grant

The user explicitly confirmed portable public CI plus a separately mandatory
local check using the real private supplied image, without publishing it.
The decision blocker is resolved. Gate A diagnosis accepted: only the Hunyuan
positive among25Python/oneNode core contracts consumes the private source;
Node setup remains an undeclared hosted-tool prerequisite. Candidate HEAD and
tracked-clean state independently rechecked by main at735245608627eee1f8f0c5b9bd81edc2d84bafa2.

IMPLEMENTING is granted to the same sole Terra high in candidate VJM6JZ only.
Risk small-code test/build/CI boundaries, no simulation or art implementation.
Terra owns helper/task/fixture design and focused iterations within:
root core workflow setup, Pale Mirror Gradle test wiring and engineering
tests/helpers for the supplied-reference boundary, and CI recurrence tests.
Expected starting files are build.yml, build.gradle and the existing Hunyuan
preparation test; they are guidance, not a design or exact-file whitelist.
No image-to-3D tool algorithm, protected model, canonical visual asset or
unrelated test family changes. Report any need to cross those boundaries.

Required outcome and evidence:

- Public core runs using tracked/temporary inputs and explicitly selected
  Node/Python dependencies. Preserve mechanics and fail-closed missing,
  wrong-name and altered-primary contract coverage; synthetic fixture output
  must never claim supplied-reference or likeness acceptance.
- Retain every genuine supplied-image positive assertion in a named local
  integration task, with an explicit reference environment path, exact
  declared input identity and no implicit user-home fallback. Missing or
  changed input must fail, not skip or pass from stale evidence/cache.
- That local task remains mandatory for full local pre-commit/release
  acceptance and changes affecting its test/tool/reference boundary. A green
  public core alone does not discharge it. Return the exact task and command
  so main can align persistent verification instructions before commit.
- Prove portable core does not access the private image, even on this host
  where it exists. Use isolated test/path-resolution controls, not renaming,
  deleting or modifying the actual reference or repurposing HOME/home.
  Prove explicit integration invocation fails on missing/wrong/changed input
  and succeeds with the authentic local source and retained assertions.
- Ordinary local read-only use of the existing primary image for this check
  is approved; outputs stay temporary/ignored. No private-image upload, Git
  addition, provider delivery, inference, art promotion or runtime export.

Verification grant: focused Python/Node and negative cases, root pack
validation without refresh, diff-check, one sequential candidate
./gradlew guardrails check --no-daemon on final bytes, and the named local
integration positive/negative checks. Pinned dependency installation only
inside disposable test environments is allowed; record actual versions.
Retain real command logs as produced, never reconstruct historical receipts.
Original docs diff-check/guardrails may run once sequentially for main's
normative changes, without original edits. Terra is sole heavy owner.
No native/GameTest/client/model run, service mutation or cleanup is granted.

Return stable Gate B with full diff, old-to-new assertion mapping, independent
public/local evidence, identities and both original/candidate statuses.
Stop for acceptance before commit/push/provider dispatch or checkout adoption.

## Gate B acceptance and revision3 publication grant

Main reviewed the complete implementation diff including the new contract test
and its own normative AGENTS addition. The original positive assertions are
unchanged below explicit-path checks; only their task boundary moved. Public
tests cover missing/wrong/altered inputs and tracked-panel mechanics, not
likeness. Gradle registers the exact private input and disables up-to-date
reuse; public guardrails excludes the private task. Node22 is provisioned.

Terra reports authentic positive and named missing/altered negatives passed,
portable guardrails check with a missing reference root passed66tasks,
focusedNode19/19 and pack179. Final main-owned AGENTS-only delta was checked
separately with candidate guardrails34tasks/20s after explicit main grant;
it does not relabel the earlier implementation gate. Original docs34tasks
passed. Main's prepared-source check independently confirms1539 entries,
8e5cfae955269eea5e2ffef44b1408ce02e6a11ee41ebdefaa93949e4644003c.
Local small-code split is ACCEPTED; remote public core is still unproved.

Sole Terra may freeze path/blob/mode inventory and make one Conventional
Commit with exact parent735245608627eee1f8f0c5b9bd81edc2d84bafa2 and ONLY:

```text
.github/workflows/build.yml
pale-mirror/AGENTS.md
pale-mirror/build.gradle
pale-mirror/tools/engineering/test_hunyuan2mv_preparation.py
pale-mirror/tools/engineering/test_hunyuan2mv_preparation_contract.py
pale-mirror/tools/frontier-v3-test-pilot/test/monorepo-ci-layout.test.mjs
```

Verify staged identity and diff-check; exclude every private/generated file
and retained diagnostic. The AGENTS path is main-owned accepted normative
content, not executor edit authority. Apply prior publication destination and
ancestry checks: exact origin git@github.com:RidiculousCircumstances/pale_mirror.git,
remote main still7352456, one nonforce main push, no other refs/settings.
Unexpected remote/content changes stop for review. No further implementation.

Use a new independent remote-only clone to verify exact commit/tree/ancestry,
clean state, no nested Git/gitlinks and fingerprint; focused CI/identity/selector
Node and root pack checks with pinned npm ci permitted. Preserve all previous
clones. Observe automatic public core on the exact SHA to terminal and retain
provider logs/run IDs; a failure grants read-only diagnosis only, no blind
rerun or repair. Report public/local gates separately with retained receipt
paths (or explicitly tool-only evidence), inventory and original/candidate
statuses. Original docs diff-check/guardrails may run once for this revision.
No native dispatch, runner setup, private upload, paid/settings changes,
deployment, cleanup or adoption authority.
