package io.farfrontier.palemirror.internal.materialization;

/** PRESENTATION never gates canonical capability; CAPABILITY does until verified. */
public enum MaterializationJobClass {
    PRESENTATION,
    CAPABILITY
}
