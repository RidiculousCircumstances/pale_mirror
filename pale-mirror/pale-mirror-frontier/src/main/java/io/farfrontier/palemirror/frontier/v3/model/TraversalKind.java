package io.farfrontier.palemirror.frontier.v3.model;

/** Physical traversal family; rail is never an implicit extension of a walking edge. */
public enum TraversalKind {
    PEDESTRIAN,
    GROUND_BIOFORM,
    RAIL
}
