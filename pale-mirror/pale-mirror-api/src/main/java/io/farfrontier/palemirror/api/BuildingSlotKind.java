package io.farfrontier.palemirror.api;

/** Physical semantic slots that future gameplay may validate without interpreting an NBT template. */
public enum BuildingSlotKind {
    BED,
    WORKSTATION,
    STORAGE,
    SERVICE,
    GATHERING,
    PATROL
}
