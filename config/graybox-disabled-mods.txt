# Server-only gameplay mods excluded from the source-authoritative Pale Mirror
# graybox. A mod marked `side = "both"` in the Packwiz manifest must never
# appear here: ordinary player clients use the standard pack and NeoForge
# requires an equal network-mod set on both ends. PM's world-side firewall,
# not a one-sided JAR exclusion, owns suppression of shared-mod activity.
hostile_tactics-neoforge-1.21.1-0.10.1.jar
