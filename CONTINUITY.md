# Continuity Ledger

## Goal (success criteria)
- Implement Pale Mirror Frontier v3 from `docs/frontier-v3-contract.md` and `docs/frontier-v3-implementation-plan.md` as a greenfield Java event-driven simulation with production two-way Minecraft materialization.
- Deliver a finite autonomous 1024×1024 world with 12 settlements, one distributed hive rooted at two seed nests, exact people/bioforms/items, continuous HOT/COLD processes and unrestricted accounted physical consequences.
- Cut production over to v3 and remove the v2 runtime only after deterministic, full-scope, performance, restart/recovery, real-world and player-comprehension gates pass.

## Constraints/Assumptions
- Pale Mirror is the independent nested repository `pale-mirror/`; the outer repository owns pack/deployment artifacts. Inspect and commit them separately.
- Java 21, Minecraft 1.21.1 and NeoForge 21.1.248 remain the pinned development platform.
- `pale-mirror-frontier` will be a pure-Java module under `io.farfrontier.palemirror.frontier.v3` with no dependency on `pale-mirror-domain`, `frontier.reference`, Python, Minecraft or NeoForge.
- V3 is fresh-world-only. V2 remains frozen and isolated until the v3 cutover gate; there is no shared state, save migration or runtime fallback.
- Every v3 format or immutable projection change recreates every affected V3 world (disposable, local or deployed): snapshot schema 101 and persistence-envelope v10 are the only accepted state/WAL inputs. Older snapshot/WAL bytes fail closed before hydration or replay; reset is an explicit deployment operation, never an in-process migration.
- One resident is one canonical person and one HOT Villager; one bioform is one canonical creature and one HOT graybox Zombie; one Minecraft item is one matching canonical item.
- No player, settlement, hive, ownership class or operation boundary is a safe zone from legitimate physical effects. Every actual consequence must be observed and reconciled.
- Desired-state materialization never silently overwrites an unknown/player change. Physical effects and post-impact accounting are a separate boundary.
- No simulation, materialization or audit path force-loads chunks. Start at most one visible client on `DISPLAY=:0` and run only relevant gates.

## Key decisions
- The v3 product promise is one seamless autonomous Minecraft world: foreground and background execution differ in location, not truth or fidelity.
- Canonical mutation is one ordered server-thread lane of typed commands, deterministic events and persisted due actions; there is no indivisible daily phase.
- One simulation unit is one running server tick. Zero-player server time advances; stopped-server time pauses; sleep advances through an explicit command; `/time` alone does not.
- Fixed-point arithmetic, checked rounding and keyed/counter RNG make v3 deterministic from its own input stream without Python numerical parity.
- Exact-person migration is one bounded Transit journey: its adjacent-position cursor and destination-bed reservation are canonical; COLD advances the same journey in short steps, while the forthcoming naturally loaded HOT executor may only take/release that identity and cursor, never create a second movement state.
- HOT scenes use persisted atomic leases; Minecraft supplies actual movement, collision, combat, inventory and effect results while leased COLD actions are suspended.
- The accepted terrain-aware movement contract treats the flat graybox as one provider, not the spatial model. Typed surface/body/port/transport positions, semantic oriented facility ports and one bounded immutable 3D capability topology own movement; COLD and HOT advance the same retained edge/cursor, while loaded obstruction/damage changes topology through typed evidence. Pedestrian/bioform and future rail graphs are distinct, and Create may only implement the rail physical provider.
- V3 persistence is checksummed snapshots plus ordered WAL. Player custody and non-replayable physical effects are durable before acknowledgement/execution and recover by postcondition inspection, never blind replay.
- Buildings/organs have stable IDs, semantic block parts and domain state. Infection uses a sparse 4×4 surface field; territory and actor positions use separate resolutions; baseline plus bounded sparse deltas retain physical aftermath.
- Human and hive AI use event-triggered utility selection, durable HTN-like processes and bounded HOT local goals. The two seed nests belong to one hive economy; hive expansion may consume only fresh bounded territorial beliefs from local Hearts/Scouts, never the global infection field. The hive keeps one durable asymmetric doctrine (`CONSOLIDATE`, `EXPAND`, `INTERDICT`) derived only from exact reserves and fresh local facts; it gates operation kinds rather than cloning settlement policy.
- The accepted hive physiology contract is `docs/frontier-v3-hive-physiology.md`: infection substrate, destructible dual-capacity hiveroot and attached organs are distinct; the whole intelligence is the Hivemind, local integration uses `GANGLION`, territory uses connected stationary `RELAY`, and every coordinated remote expedition requires an exact mobile `OVERSEER`. Exact inactive bioforms occupy cocoon slots and wake only for current work or perceived threat, never because a chunk loaded. Purposeful Sentinels must physically sense and deliver observations. Biomass, carcasses, adaptations, grafts and payload charge are conserved visible processes, and controller loss degrades rather than deletes subordinates. Existing `HEART` and mixed `BioformRole` are provisional implementation debt requiring explicit stable-tag/schema migration.
- The accepted human capability contract is `docs/frontier-v3-human-capabilities.md`: one exact resident separately owns condition, trainable skills, profession/employment, exclusive current assignment, exact equipment and work/tactical organization. The first composition is small exact defence, patrol/escort, engineering/recovery and medical/evacuation units, never cohorts or permanent mob classes. Combat functions are derived rather than permanent classes; mobilization has explicit civilian opportunity cost, and leader/equipment loss degrades the same people instead of replacing them. Existing `ResidentRole` is provisional bootstrap affinity requiring an explicit stable-tag/schema migration before broader Wave-4 role expansion.
- `docs/frontier-v3-create-direction.md` is the accepted post-graybox industrial direction, not a current cutover gate. Create will be a NeoForge physical provider over unchanged v3 authority. Transport scope is rail only—long lines, exact freight trains, stations/yards, switches/signals, bridges/tunnels, loading and repair; road freight and aeronautics are excluded. The future physical scope is factories, farms, weapons, turrets, extraction, energy, metallurgy/materials, warehousing/rail logistics and construction/engineering. Current graybox work must retain provider-neutral facility ports, rail topology, exact train/cargo leases and observed consequences so this remains an incremental replacement rather than a second economy.
- A settlement assault is a distinct bounded hive operation rooted in one fresh Scout settlement sighting and fresh local territorial evidence. It retains its own attackers, target, lifecycle and HOT/COLD scene cause; it never uses a route operation, cargo carrier or hidden global query as a substitute.
- Exact slot-owned resources, containers and cargo are the economy. Every exact stack has one canonical economic claim distinct from custody: active owned slots and confirmed cargo receipt transfer it, while player/world carriers retain it; there is no hidden aggregate stock and meaningful player supply may require tens or hundreds of stacks.
- An insolvent account rejects new debits, reservations and work admission, but may still receive an exact zero-sum payment. Debt recovery or recapitalization must never be blocked by the debtor's status or compensate it by minting money.
- Each materialized container surface will have a durable canonical lifecycle (`UNMATERIALIZED`, prepared/owned, active, conflict) distinct from its exact slot inventory. A missing or altered previously active surface is a conflict, never permission to recreate it; a prepared surface recovers only by loaded-chunk postcondition inspection.
- Evidence levels remain separate: architecture, automated correctness, real-world continuity and unbriefed player comprehension.
- Frontier v3 causal changes use the smallest relevant checked-in pilot scenario: read-only fixture, ordinary player action, terminal domain assertion and PMV3 trace; visible claims add a semantic camera/frame check, while durable/recovery changes add a declared restart split.
- A pilot visual frame is a local test-only barrier, not a player action: `clean` is the default and closes incidental screens, hides generic UI/chat, settles rendering and waits for exact capture acknowledgement before the next action. `player` is explicit for UI evidence; ordinary HUD toggles are prohibited.
- The active continuity ledger is capped at 1000 lines; Java sources are capped at 1000 lines by the Gradle guardrail. Both limits are ratchets, not permission for mixed-responsibility coordinators.
## State

### Done
- V3-AUD-017 is closed at schema 83: each route owner embeds its sole immutable
  exact `RouteUnitManifest`, without a global roster or a second assignment
  authority. New patrols retain one leader plus one to three scouts; cargo
  operations retain one logistic crew member plus two to four separately named
  escorts. Schema-82 snapshots and historical patrol WAL bytes recover only
  their same explicit understrength legacy members. Assignment, COLD formation,
  death/loss and byte-recovery tests pass; the ordinary native cargo theft and
  route-return scenarios retain the same three named caravan members across
  HOT unload/reload. The full critical gate passes: `guardrails`, `check`,
  packaged-JAR verification and 251/251 required GameTests; Node pilot schema
  tests pass 28/28.
- The first human tactical read model is explicit and deliberately noncanonical:
  exact assignment, retained defender-unit leadership, profession and
  actor-held exact weapon derive `CIVILIAN`, `MILITIA`, `ARMED_DEFENDER`,
  `GUARD` or `SQUAD_LEADER`. A weapon alone cannot create a tactical role. The
  first exact issue boundary is now durable: an active settlement-depot sword
  can move only through an `EQUIPMENT_ISSUE` intent binding assault, defender,
  item and source slot; its typed receipt is snapshot/WAL round-trippable and
  only an exact source-empty/actor-custody match confirms it. Forged-source
  receipts are rejected. The first outbound executor now performs that
  durable-before-effect transfer only when the exact active depot chest and
  leased Villager are naturally loaded; mixed/restart evidence becomes
  `UNKNOWN_AFTER_RESTART`, never a replacement. Its function/nameplate refresh
  comes solely from the pure projection, while civilians keep quiet labels.
  Fresh `disposable-defender-equipment-restart` evidence proves issuance,
  bounded trace, graceful restart, rendered visible `ARMED DEFENDER` nameplate
  and retained actor custody. Focused Java tests, 28/28 Node tests, the
  12-test economy slice and the complete critical gate (249/249 GameTests,
  build and packaged-JAR verification) pass. Inverse return, observed
  loss/drop/destruction, readiness and broader tactical roles are separately
  staged boundaries.
- Exact defender equipment return is now the inverse durable boundary: after a
  resolved assault, one actor-held tagged weapon may move only to one named,
  free active-depot slot through `EQUIPMENT_RETURN` and an inverse receipt.
  Schema 82 retains the typed target slot while schema 81 remains readable.
  The executor uses revision-scoped command IDs, so long semantic IDs cannot
  invalidate the authoritative command lane; the existing issue path uses the
  same bounded rule. Fresh `disposable-defender-equipment-return-restart`
  evidence proves the same sword's canonical slot custody, PMV3 correlation,
  graceful restart and ordinary player chest opening on one `:0` client.
  Focused Java/Node/economy checks and the full critical gate pass: guardrails,
  check, build/package verification and 250/250 GameTests.
- Managed defender death now has one exact causal outcome for the current
  graybox hand: the retained tagged weapon moves durably from `Actor` to one
  deterministic `WorldCarrier` before its real Minecraft item entity appears;
  a missing/altered hand records exact destruction instead. Normal pickup then
  moves the same item to `Player`; no stack, actor inventory or equipment
  receipt is reconstructed. Historic issue/return receipts validate their
  original intent identities without falsely requiring permanent custody, so a
  later legal drop, pickup or destruction remains restart-safe. The native
  `disposable-defender-equipment-death` pilot proved an ordinary player kill,
  `defender_equipment_dropped` trace for `resident:1-1`, and the exact iron
  sword in player custody on one `:0` client. Focused pure/Node tests, the
  14-test economy GameTest slice and the full critical gate (251/251
  GameTests, guardrails/check, build and packaged-JAR verification) pass.
- First defender readiness is now an explicit pure projection, never a second
  persisted unit record: active exact assignment, actor vitality and exact
  actor-held weapons yield `UNAVAILABLE`, `IMPROVISED`, `DEGRADED` or `READY`.
  COLD settlement assault damage grants guard output only to armed members of a
  `READY` unit; individual weapon loss and leader loss reduce the same retained
  survivors to militia output without replacement. The ordinary HOT label has a
  second `UNIT …` line from that projection. Focused pure/NeoForge/Node tests,
  economy GameTests 14/14 and the complete critical gate pass (251/251,
  guardrails/check, build and packaged-JAR verification). The updated
  `disposable-defender-equipment-restart` pilot passed after a graceful restart:
  `item:development-defender-sword` remained `ACTOR` custody and the visible
  client semantic check found `UNIT READY`; its clean frame is
  `build/frontier-v3-scenarios/disposable_defender_equipment_restart-e621effd-8f36-449d-ac44-fa63bab7352b-armed-defender-after-restart.png`.
  This is correctness evidence only: the vanilla-scale text remains too small
  for the accepted large bright tactical grammar.
- `3fa281b` establishes the first exact human-capability foundation in source schema 81: `ResidentProfile` now keeps provisional bootstrap affinity, separate persisted `ResidentProfession` and an eight-family bounded `HumanCapability` vector. Production, farming, logistics, patrol, assault defence, combat validation and actor labels select the profession/capability path rather than `ResidentRole`; pre-schema-81 snapshots deterministically derive the new fields. `HumanAssignmentProjection` derives one exclusive current assignment from persisted jobs, harvests, operations, patrols, active assault defender units and Transit without a second mutable ledger, rejecting collisions. An assault now owns one ordered exact defender unit with a stable leader; admission excludes dead, starving or civilian-claimed people, and snapshot/WAL recovery retains that unit without a second roster. A fresh Scout-rooted defence may now atomically interrupt one exact COLD market-backed production job: the original input returns, the reservation/order release and production blocks before that same person derives defence assignment. Materialized/prepared/running work remains unavailable. The harvest completion path now retains the exact claimed farmer instead of wrongly demanding a second free worker. Focused normal/negative/recovery tests and the complete critical gate pass: `guardrails`, `check`, packaged JAR and 248/248 GameTests. Equipment issue/recovery, other typed interruption owners, readiness and return remain the next human slice.
- Exact actor equipment custody is now available without a second inventory: one exact stack may retain `InventoryCustody.Actor(resident|bioform)`, snapshot and observed-transfer WAL retain it, actor existence is validated against canonical locations, and item loss may name an actor physical surface. Focused normal/negative/recovery tests pass. Issuing/recovering equipment under a unit lifecycle remains next.
- V3-AUD-003 is closed: the registry resolves an exact command/event owner and dispatches it only to the corresponding closed executable domain module. The command and reducer facades are now 33 and 16 lines; physical, ambient, logistics, population, economy, resource-site, hive, infrastructure and strategy transition policy live with their owning modules. Catalog startup rejects a declared non-kernel process without one executable owner, while the debt ratchet makes both former global-branch ceilings zero. The complete critical gate passes: `guardrails`, `check`, build/package and 248/248 GameTests.
- V3-AUD-011 is closed: all 33 behavioural processes, the trusted-command planner, event reducer and process catalog now live in `frontier.v3.process`; all 23 snapshot/WAL codecs live in `persistence`; `model` has neither `*Process` nor `*Codec(s)` source files and mechanically cannot import `process`, `persistence` or `runtime`. The move uses only public immutable state/named transitions, and a focused recovery regression preserves the durable Scout sensor position when the Scout subsequently moves. The complete critical gate passes: `guardrails`, `check`, build/package and 248/248 GameTests.
- V3-AUD-012 is closed: each process has a literal emitted wire-type contract, and the debt ratchet rejects the former domain-union fallback. `persistence` maps its codecs independently by the same ten stable process IDs; composition rejects descriptor/persistence disagreement, unowned/missing codec, emitted non-kernel fact without a reducer and undeclared emissions before engine start. Negative tests cover omission, reducer absence, duplicate scheduled ownership and codec-owner disagreement. One representative codec payload per owner, plus their combined versioned WAL transaction, round-trips exactly. The complete critical gate passes: `guardrails`, `check`, build/package and 248/248 GameTests.
- V3-AUD-006 is closed: all snapshot/WAL enum fields now use explicit, non-reused `FrontierWireTags` rather than declaration order; unknown tags fail closed. Stable-tag and direct historic company-registration-byte recovery tests preserve existing meanings, while the architecture-debt ratchet rejects every codec `ordinal()` or `values()[tag]` use. Focused debt/pure-module tests and the full critical gate pass (248/248 GameTests, build and production-JAR verification).
- V3-AUD-007 is closed: immutable `FrontierRuleset` data is retained by bootstrap manifest and snapshot selector (ID/schema/content hash), and exact installed selection fails closed before hydration. Cadence, radii, COLD strides, gains, economic rates, facility capacity and COLD combat output now read it across canonical and physical execution; pre-ruleset snapshots bind a named legacy anchor, never the current default. The fixture catalog pins `ruleset=production`, and the debt ratchet rejects process-private tuning. Focused determinism/recovery/physical regressions and the critical gate pass (248/248 GameTests, build/package verification).
- `9b6c5f8` closes V3-AUD-013 with a production-JAR test-boundary, an explicit no-forced-GameTest-load ratchet and native canonical-coordinate restart evidence.
- V3 test-boundary follow-up `V3-AUD-013` is closed: the test-only scene helper and resource-site GameTests now live only on the pilot classpath and are explicitly rejected from the production JAR. The architecture-debt validator rejects every `getChunkAt` in v3 GameTests. Cross-coordinate physical scene proofs remain the named native disposable scenarios, because a GameTest template is deliberately outside the finite canonical 1024×1024 map; it must not impersonate a remotely force-loaded canonical scene. Node pilot schema tests pass 26/26; focused scene/economy slices pass 28/28 and 11/11; the full critical gate passes 248/248 GameTests, build and JAR verification. Fresh visible native graceful-restart runs prove both the Scout observed-carrier/intercept flow and the 27-member confirmed settlement assault without a remote GameTest chunk load.
- The user accepted replacement of the Python/source-parity direction with greenfield Frontier v3 and approved the stable contract, implementation-wave structure and legacy-removal policy.
- The complete pre-v3 ledger is archived at `docs/archive/CONTINUITY_2026-08-27_pre_frontier_v3.md`.
- `a1a6931` establishes the stable v3 contract, decision-complete implementation waves, valid version-3 architecture transition and explicit frozen-v2 notice.
- Wave 0 adds executable architecture validation and future `pale-mirror-frontier` source-boundary scans. Focused validator tests, `git diff --check` and `./gradlew guardrails check --no-daemon` pass; the full Gradle gate completed 56 tasks successfully.
- Wave 1 foundation adds the independent pure-Java `pale-mirror-frontier` module, typed IDs and simulation values, checked fixed-point arithmetic, keyed RNG golden vectors, deterministic bounded scheduling and versioned kernel codecs. Focused tests and the critical Gradle gate, including 168 existing NeoForge GameTests, pass.
- `eea89c1` records the Wave 1 module/primitives foundation.
- `84d3136` adds immutable command/event envelopes, typed rejection, one-thread atomic in-memory transactions, bounded receipt/transaction retention, reducer quarantine, non-destructive due-action admission and a reproducible pure-kernel benchmark fixture.
- Explicit kernel schedule-created, schedule-cancelled, schedule-rescheduled and schedule-consumed events now mutate the due-action index only inside the same successful transaction; failed due work remains scheduled and visibly quarantines the engine.
- Wave 1 now has explicit payload registries, versioned command/event/transaction codecs, built-in schedule-effect codecs and fail-closed deterministic transaction replay. Focused replay tests and the critical Gradle gate, including 168 GameTests, pass.
- The Wave 1 review tightened event-time causality: a command is accepted only at the engine's current `SimInstant`, so retained history cannot acquire a backdated event that replay would reject.
- A fresh-JVM fixture now serializes the same command/schedule transcript twice and proves byte-identical event and checkpoint output across independent Java processes.
- Wave 2 defines the v3-only `FrontierStore` port, typed durability/append/snapshot/compaction receipts and `RecoveryImage`; foreign world checkpoint/WAL inputs fail before replay.
- Wave 2 adds checksummed pure snapshot/WAL schemas for complete checkpoint state; bad checksum, unknown schema version and truncated bytes fail closed before any recovery mutation.
- Snapshots now bind an exact `coveredWalSequence`, so recovery can prove the retained WAL tail is complete before compaction deletes covered records.
- NeoForge now provides a v3-only atomic file store: ordered checksummed WAL, snapshot install, covered-only compaction and recovery rejection for revision gaps/checksum corruption; residual uncommitted `.tmp` files are ignored and safely replaced.
- Recovery images now independently reject non-contiguous or time-reversing WAL, duplicate transaction/event identities, malformed event envelopes and duplicate/future checkpoint receipts before any reducer or state codec can observe history.
- The fresh-world bootstrap manifest now deterministically defines the finite 1024×1024 profile: twelve named settlements with 20–40 exact residents and six functional structure identities each, plus one hive with two seed nests and 48 exact bioforms. Golden SHA-256 manifests cover two seeds and a Turkish-locale regression.
- Recovery-capable state codecs and the public pure engine factory now restore checkpoint plus WAL tails without a new-world fallback. Accepted-command receipts are atomically part of each WAL transaction (kernel transaction schema 2), preserving restart idempotency; schema 1 transactions are rejected.
- `FrontierWorldState` is now the immutable mutable-state owner for every bootstrap actor location, every functional structure condition and sparse 4×4 infection intensity. Its versioned codec reconstructs the deterministic profile and rejects incomplete/foreign indexes before reducer recovery.
- Exact inventory is now canonical and persisted: each settlement starts with one named 27-slot depot, every stack has exactly one container-slot/cargo/player-UUID custody, and state codec v2 round-trips container, item, cargo and player records fail-closed.
- The pure kernel now has an explicit write-ahead transaction committer: it assembles and validates a complete transaction, appends it through the NeoForge v3 store adapter, and only then installs state/revision/schedules/receipt. An append failure visibly quarantines with no acknowledged or partial canonical mutation; a mismatched storage receipt fails closed.
- The isolated NeoForge v3 lifecycle host now owns fresh creation versus verified recovery, one monotonic simulation tick per running server tick, bounded checkpoint/install/compaction, orderly shutdown and visible startup/runtime quarantine. Replacing a checkpoint at the same covered WAL sequence is atomically safe, preserving idle canonical time. Focused tests prove write-ahead restart/idempotency and corrupt-history quarantine; it is not yet wired into the production event bridge or materialization.
- The v3 world now has a concrete pure composition root and bounded audit projection for the deterministic 1024×1024/12-settlement/two-nest profile. A `pale_mirror.frontier_v3.enabled` opt-in NeoForge bridge starts/ticks/stops that isolated runtime without reading V2; the packaged jar now contains the frontier module and verifies that fact. The initial bridge exposes canonical bootstrap/recovery only; autonomous process events and materialization remain unimplemented.
- The first autonomous v3 process is now canonical: both seed nests initialize sparse infection, and one bounded deterministic pulse every 100 simulation ticks emits a serialized `InfectionChanged` fact and schedules its successor. It never writes beyond the finite world bounds; only canonical state changes, not Minecraft blocks.
- The first economic process is now an event-driven, two-phase exact custody flow: a named settlement workshop and exact crafter consume the bootstrap wheat stack into one durable `ProductionJob`, then only place its named bread output in a verified free depot slot. Typed durable blocked facts distinguish missing input, unavailable storage and unavailable facility; there is no aggregate stock or overwrite path. Codec/state tests cover the active job, payload replay, insufficient input and a fully occupied depot. Scheduler workload is now constant rather than using a growing ordinal as `weight`, with a 600-pulse regression proving continued admission under a bounded work budget.
- The first causal supply flow is now canonical: a scheduled settlement demand creates a named ordered supply contract only from an exact bread stack, then a separate persisted due action moves that same stack into its named cargo batch and changes the contract to `LOADED` atomically. Contract/cargo state and payload codecs fail closed; cargo loading verifies source ownership, exact kind/count and one custody. Focused tests and the critical Gradle gate, including 168 GameTests, pass.
- The first COLD route operation is now canonical: loading that identified cargo atomically creates a bounded named operation with one exact hauler and guard, durable finite waypoints and persisted progression. Sequential advances move those same canonical people; skipped waypoints, foreign participants, foreign cargo, duplicate assignments and malformed snapshot/payload data fail closed. State codec v5, operation payload codecs and the bounded audit projection retain the operation; focused tests and the critical Gradle gate, including 168 GameTests, pass.
- The first physical-intent boundary is now canonical: the route arrival atomically prepares an exact cargo hand-off intent, tied to its operation and cargo with a typed postcondition. Prepared intents enter the version-6 canonical snapshot/payload ledger and projection; their transaction is selected for `DURABLE_BEFORE_EFFECT` before a future executor could observe it. Intent records are bounded and fail closed for foreign/duplicate subjects or malformed effect semantics. This is durability evidence only: no v3 Minecraft effect executor or restart postcondition inspector exists yet.
- Physical-intent lifecycle is now exact and persistent: only `PREPARED → RUNNING → CONFIRMED` or `UNKNOWN_AFTER_RESTART` is legal, confirmation requires a stable observed-postcondition ID, and every transition is `DURABLE_BEFORE_EFFECT`. Snapshot/payload codec v7 retains that receipt; the state rejects skipped confirmation and stale/terminal transitions. The NeoForge postcondition inspector that submits these transitions remains unimplemented.
- The NeoForge v3 host now exposes only immutable checkpoint images to server-thread adapters. On restart, its temporary fail-closed guard turns every persisted `RUNNING` intent that has no supported Minecraft postcondition inspector into durable `UNKNOWN_AFTER_RESTART`; it never replays or assumes success. The read-only projection distinguishes prepared from unknown intents. The focused restart test and the critical Gradle gate (including 168 GameTests and packaged-JAR verification) pass. This is not the eventual real-world inspector: it deliberately has no executor evidence to inspect yet.
- The v3 bootstrap now contains three stable functional organs (`HEART`, `BROOD`, `STORE`) at each of its two seed nests. Each `STORE` owns one exact 27-slot hive receiver container. Exact inventory also has a bounded UUID-keyed `WorldCarrier` custody surface with a required reverse index; it rejects dangling or duplicate carrier ownership. This supplies the canonical receiver and carrier identity needed before a real CARGO_HANDOFF can exist. Focused tests and the critical Gradle gate (including 168 GameTests and packaged-JAR verification) pass.
- CARGO_HANDOFF now has immutable exact physical evidence: a bounded observation names its intent, cargo and every exact item placement in the destination nest's `STORE` slots. Canonical state schema 9 persists observations; a confirmed transition atomically records that evidence, transfers the same item IDs out of cargo custody, completes the contract, and rejects foreign receiver, incomplete/duplicate placement, duplicate observation and invalid recovered receipt states. Focused tests and the critical Gradle gate (including 168 GameTests and packaged-JAR verification) pass. No Minecraft executor has emitted this evidence yet.
- The first v3 NeoForge physical executor now handles `CARGO_HANDOFF` in naturally loaded chunks only. It creates and claims only a fresh supported receiver chest, carries an exact canonical container ID and per-stack item ID in Minecraft data, durably marks intent `RUNNING` before placing cargo, and confirms only an exact observed chest postcondition. An existing/unowned chest, changed stack, missing result or executor fault becomes a visible `UNKNOWN_AFTER_RESTART`/runtime quarantine rather than a repair or blind replay. Restart leaves this supported kind for the loaded-chunk inspector; unsupported kinds retain the conservative quarantine path. Two materialized GameTests cover exact identity and unowned-chest refusal; focused tests and the critical gate pass with 170 GameTests.
- The first HOT/COLD correctness boundary now exists in the pure v3 state: a durable `SceneLease` binds exactly one en-route operation, its named participants, cargo, route-point hand-off, tick, revision and deterministic Minecraft UUIDs. The lease persists in state schema 10 and WAL payloads, blocks COLD route progression through the durable `OperationColdSuspended` fact, has bounded observable projection counts, and survives a NeoForge file-store restart without duplicated movement.
- The first loaded-chunk v3 scene executor completes `PREPARED → HOT → DRAINING → CLOSED` for exact route Villagers. It admits only a naturally loaded, player-demanded route point, durably creates a lease before body creation, owns only its deterministically tagged Villagers, captures their actual positions before close, then schedules COLD continuation. An unloaded PREPARED scene defers; a foreign/missing/changed body becomes durable `UNKNOWN_AFTER_RESTART`, never a repair or guess. Closed lease details compact deterministically at the bounded checkpoint retention limit. Materialized GameTests prove owned-body creation and unowned-entity refusal; a restart test proves release resumes the suspended COLD route. Focused tests and the full critical Gradle gate pass with 172 GameTests.
- `1bdbd84` establishes the bounded physical settlement-food provisioning boundary: the current settlement cycle consumes the required number of named `minecraft:bread` items every 24,000 ticks, COLD directly from canonical custody and ACTIVE only through a durable `EXACT_ITEM_CONSUMPTION` intent with the exact observed physical remainder. A derived two-cycle reserve protects future food from exports without becoming a second inventory ledger. Depot boards and read-only diagnostics expose `SECURE`, `RATIONED`, `SHORTAGE` and `CONFLICT`. The checked-in `disposable_settlement_provision` pilot starts from 64 exact bread for Northwatch's 37 residents, naturally materializes it, reaches the terminal 27-item/`CONFIRMED` state, survives a graceful restart, and captures the readable exterior `FOOD SECURE · 27 / 74` board frame at `build/frontier-v3-scenarios/disposable_settlement_provision-4dec504d-e3a1-4805-9441-ff2e6fc74a43-northwatch-food-secure.png`. Node 25/25, focused domain/diagnostic tests, economy GameTests 10/10 and the critical gate (guardrails/check, build/package, 245/245 GameTests) pass. The full gate also exposed and fixed a test-only graybox field fixture that had not loaded all of its exact 8×8 boundary chunks before checking COLD materialization.
- H0.6 closes the final Frontier v3 architecture-audit debt at the declared current limits: bounded noncanonical metrics, a retained schedule-capacity guard, the repeatable seed-41 twelve-front scale/WAL proof, and a 120-second naturally loaded 27-member physical JFR route. The full critical gate passes with 248/248 GameTests; larger physical limits remain separately gated by a fresh same-seed JFR plus terminal causal scenario.
### Now
- V3-AUD-028 is closed: harvest now reserves the mature field/task/output and emits one durable
  `RESOURCE_SITE_HARVEST` intent; only the loaded executor's exact field-reset + chest-stack
  receipt creates inventory, completes the task and schedules regrowth. Its bounded site work
  set skips unloaded fields, so one deferred field cannot starve a later naturally loaded one;
  it runs as an `EFFECT` after active container provenance. The obsolete direct-output payload
  and codec are removed, and the current snapshot schema 101/persistence envelope 10 require a fresh world.
  Focused normal/negative/head-of-line checks, economy 15/15, scene 38/38 and native
  `disposable_redwillow_harvest` run `9b424ba2-7a80-4a44-82e6-4da1aac5dd24` pass. The run proves
  exact wheat at `CONFIRMED` before graceful restart and the same field `GROWING` epoch 2 with an
  active physical depot afterwards; autonomous production legitimately transforms that wheat to
  exact bread, rather than the test mistaking a living economy for lost state.
- V3-AUD-022 now has one registered canonical route-maintenance chain: an observed retained PM
  surface/foundation loss admits one exact local engineering team, issues/returns its exact tool,
  advances one shared COLD/HOT worksite, separately observes one active maintenance-chest unit
  becoming exact cargo, and accepts only one exact target receipt to remove that delta and reopen
  the retained edge. Source and target defer on natural demand; neither side force-loads or adopts
  a foreign block. The owner then compacts its terminal operation, receipts and closed worksite.
  It is deliberately distinct from structural repair and route construction. A route-scene
  regression found the remaining admission race: an active-maintenance-only guard allowed the
  bypass planner to win before the maintenance aggregate existed. The correction now blocks
  bypass admission directly for every PM-owned retained baseline loss, while test-only
  construction fixtures remain explicit preconditions rather than an invented production cause.
  Snapshot schema 99/envelope 8 reject earlier worlds. Focused pure route/codec/catalog tests,
  the 37/37 scene slice and the complete critical gate (`guardrails`, `check`, build/package,
  266/266 GameTests) pass. The materialized normal/conflict/restart and native player
  break→repair evidence remain before V3-AUD-022 may close.
- `a0352db5` fixes a live recovery defect: `TraversalTopology` permits and writes a one-surface,
  zero-edge stationary assembly topology, while its decoder had rejected both bounds at restart.
  `TraversalTopologyStateCodecTest` now proves stationary round-trip and empty-topology rejection;
  its focused test and the complete critical gate (266/266 GameTests, build/package) pass. The
  quarantined disposable `frontier-v3-live-r59` was stopped and moved to the local reset archive;
  the new `frontier-v3-live-r60` is deployed from a clean detached `a0352db5` artifact
  (`SHA-512 4a332361…49cea0b`) on `25565`, reaches `Done` and `Frontier v3 runtime started`
  without quarantine. Next infrastructure work is a checked deployment preflight/runner enforcing
  commit purity, Java 22, exact reset targets, server-only profile validation, pinned install and
  post-start health before a live deployment.
- V3-AUD-024 is closed. The renewed hive-mob jerk report was a saved-entity restoration race, not steady-state TPS/GC: a chunk could expose blocks while `PersistentEntitySectionManager` still restored entity columns, temporarily leaving `getEntity(UUID)` empty and permitting a second deterministic body. Production ambient, scene-member and cargo admission now defer on `ServerLevel.areEntitiesLoaded(chunk)`, with no force-load, absence inference or replacement body; the old pending-join bridge remains the second protection layer. Fixture-only GameTest entry points preserve synthetic test columns. The focused scene slice passes 37/37 and the clean detached critical gate passes: `guardrails`, `check`, build, packaged-JAR verification and 266/266 GameTests. Fresh disposable `frontier-v3-live-r58` runs the checksum-pinned `c8a2bd58…fee9c3b` artifact on `25565`. Its one-client fullscreen western-hive run `c70e58ea-fd16-4d0b-97da-70b0a2077205` retained `bioform:west-0` as one indexed UUID and observed exact positions `(-424.500,414.480) → (-424.727,415.134) → (-424.807,415.221) → (-424.944,415.331) → (-425.363,415.491)` with clean before/after frames; no duplicate UUID, pending-admission or quarantine trace occurred. This closes the defect, not the broader creature-animation/player-comprehension gate.
- Current V3-AUD-022 native evidence is deliberately still in progress. It first exposed a constant-Y engineering approach (`support Y=63` to retained route deck `Y=64`), now corrected through immutable terrain-surface candidates and one-grade retained assembly steps. It then exposed two missing durable-boundary registrations for route-maintenance pickup (physical-process emission and WAL observation union), both now protected by focused registry/codec tests. The post-pickup graceful-restart run next exposed a distinct recovery defect: `AmbientActorProcess.goalFor` recognized only construction assemblies and recreated a maintenance engineer as ordinary `WORK`; it now resolves both construction and maintenance through `EngineeringWorkOrder`, with a focused goal regression. Both native recovery split points now pass: `19d96775-65e4-4d4c-874f-45a3dac1e6fb` restarts after exact pickup before target repair, and `d8e7ef3d-196a-4cde-94c1-31a541dabf42` restarts after observed repair during the same tool holder's return journey, then reaches `human_equipment_returned`, maintenance compaction and `settlementTraversalAvailable=true`, without a force-load. The remaining audit exit is the focused foreign/unavailable source/target plus exact running-effect recovery matrix and final critical gate.
- V3-AUD-022 additionally exposed a queue-boundary error: a route-maintenance source was classified runnable from `hasChunkAt`, but its executor separately required ordinary nearby player demand and therefore no-op'd after selection. Material pickup and target repair now share the one natural-demand predicate; a cache-resident, player-undemanded source defers only itself and cannot head-of-line block a later demanded repair. The focused scheduling test, 40/40 scene slice, 30/30 pilot schema tests, and native normal/restart run `5bc82e07-68cb-4db4-babb-7181168c938b` pass. The remaining V3-AUD-022 exit is still the foreign/unavailable source/target plus exact running-effect recovery matrix and final critical gate.
- The V3-AUD-022 `RUNNING` recovery matrix now has a snapshot round-trip for both durable boundaries: exact maintenance-source postcondition creates one cargo unit; exact repaired-target postcondition consumes that unit and clears only its own loss. A forged/absent source or target instead leaves the source/cargo/loss untouched and conflicts only that aggregate. Focused `RouteMaintenanceProcessTest` passes; the remaining V3-AUD-022 exit is the foreign/unavailable source/target matrix and final critical gate.
- V3-AUD-029/030 are closed. Physical intent scheduling now skips only naturally deferred exact equipment endpoints while preserving invalid-head quarantine, and engineering tool issue/return share one bounded adapter predicate over the pure exact depot-service station rather than separate chest radii. Fresh disposable native `disposable_route_repair_cargo_restart` run `19d96775-65e4-4d4c-874f-45a3dac1e6fb` proved normal player route loss → exact repair → graceful restart → return of the same `item:bootstrap-1-engineering-tool-1` → terminal maintenance compaction, with the repaired operation reporting retained traversal availability. Economy GameTests pass 16/16; Node scenario checks pass 30/30. V3-AUD-022 remains open for its broader negative/recovery exit set and final critical gate.
- V3-AUD-031/032 are closed. Route maintenance now makes a bounded deterministic round-robin over retained repairs plus one independently checked candidate; exact nonterminal source reservations prevent double use, while a COLD source defers only itself. On first ordinary loaded visit an exact known semantic loss receives a repairable provenance tombstone only when the plan owner/part is still air; foreign or unclaimed damage remains without repair authority. A temporarily absent tombstone makes the worksite `DEFERRED` for ordinary projection rather than a false conflict or a force-load. Completion/conflict atomically drains only the exact HOT engineering lease, preventing stale bodies on terminal aggregates. Pure normal/foreign/unknown/recovery regressions, the 40/40 scene slice, native `disposable_route_maintenance_cold_source_fairness` run `62d89442-49fb-4550-be65-5e6bcafb58e4`, and the full critical gate (272/272 GameTests, build/package) pass. The native run proves the naturally loaded second repair completes while the first source remains COLD, cargo-free and pending.
- Recurrence prevention now has mechanical deployment gates in the outer pack repository:
  `scripts/frontier-v3-deploy-preflight.sh` rejects a non-detached/dirty exact source
  ref, wrong Java, artifact hash, runtime target or selected world before mutation;
  `scripts/frontier-v3-deploy-verify.sh` separately requires the installed hash, live
  PID/socket and post-marker journal `Done`/v3 startup with no new quarantine. Both
  are read-only and syntactically/Packwiz-index verified; current WIP remains ineligible
  for deployment until its causal scenario and critical gate pass.
- `b1dc2b5f` closes `V3-AUD-027`: the owning-thread immutable canonical state/revision/instant view serves physical adapters directly; full `CheckpointImage` bytes remain persistence/diagnostic work. The runtime regression proves repeated physical reads plus a command make no codec call until an explicit checkpoint. Its complete critical gate passed (`guardrails`, `check`, build/package, 266/266 GameTests); the relevant scene slice passed 37/37 after also fixing a real callback-order stop/go defect in which a new future local intent could overwrite an intent already due for the entity pre-tick. Fresh live r56 is deployed on `25565` as `frontier-v3-live-r56` (Java PID 1909417 at verification). Cold first teleport to the western hive still logged 2.362 s/47 ticks, but its JFR sampled native Minecraft chunk-load/serialization work and no whole-world Frontier codec frames. A warm replay of the same native full-pack scenario (`1a440cf3-6a73-4b04-a377-e14555f9143b`) logged no backlog; player-present ticks were 3.15–7.13 ms and its indexed HOT `bioform:west-0` advanced across four exact observations. This closes the periodic snapshot/stop-go sources of the hive-mob jerk report. One cold-view chunk-entry hitch is intentionally recorded as Minecraft loading, not falsely assigned to PM simulation; ordinary travel-route/player-comprehension acceptance remains separate.
- `V3-AUD-025` remains source-closed: one immutable `RouteFootprint` compiles route surface/foundations together and readability consumes its local structure/organ index rather than rebuilding the plan. The focused tests, 37/37 scene slice and re-run 266/266 critical gate passed; r54 live evidence was superseded by the v2 boundary finding and r55's newer codec finding.
- `b0aca4b0` closes the observed hive-mob jump replication defect: PM's collision-authoritative HOT actuator already advanced every loaded server tick, but direct `Entity.move` did not set Minecraft's tracker impulse flag. The normal Zombie tracker therefore coalesced multiple server positions into one client-visible jump. Each accepted physical step now requests same-tick tracker replication without changing canonical position, cursor, collision, lease or movement authority. `physicalAdmission.observedExact` is a bounded read-only diagnostic coordinate, and `visible-hive-motion-west-nest` samples its HOT actor at 250 ms intervals. Focused diagnostic/Node tests, the 36/36 scene slice and the full critical gate (`guardrails`, `check`, build/package, 265/265 GameTests) pass. The checksum-pinned JAR `7482d017…04e83b2d` is installed on r51; `far-frontier-v3-live.service` restarted normally at 01:14 +05 as Java PID 1625404, reached `Done` and `Frontier v3 runtime started` on `25565`. Native full-pack `:0` run `2041051f-71e9-426d-ab02-27f406f4bffb` passed: the same indexed HOT `bioform:west-0` progressed through exact samples `(-424.520,414.500)`, `(-424.305,414.519)`, `(-424.172,414.555)` and `(-423.779,414.807)`; clean frames are `build/frontier-v3-scenarios/visible_hive_motion_west_nest-2041051f-71e9-426d-ab02-27f406f4bffb-hive-west-nest-{before,after}-motion.png`. This is strong narrow replication evidence, not broader creature-animation/player-comprehension acceptance.
- Live r51 then exposed a separate blocking HOT-materialization failure during a pilot visit to the western hive: the server reported a 2,009 ms/40-tick stall, rejected two `HIVE SCOUT` admissions for duplicate UUIDs, and later quarantined at tick time with `physical observation does not match its route operation`. The deployed JAR already contains the tracker-replication fix, so this incident must not be misclassified as the former client packet coalescing defect. Do not use or reset the quarantined live world as motion evidence. First reproduce with a disposable many-actor hive-entry fixture, prove one physical UUID/body per canonical actor across ambient/scene hand-off, and trace the invalid cargo observation to its intent/operation/cargo owner; then run the relevant HOT scenario and deploy a new fresh world only from a verified commit.
- The quarantine's canonical cause is now reproduced in `TerminalLogisticsProcessTest`: terminal compaction removed a delivered operation/contract but retained its confirmed `CARGO_HANDOFF` receipt, whose validator correctly still requires the live operation. `FrontierOperationStateSupport` now retires only the exact confirmed `CARGO_LOADING`/`CARGO_HANDOFF` receipt pairs owned by that terminal delivery before its active graph is removed; the focused normal and snapshot/recovery tests pass. This removes the known quarantine path, but does not yet explain or close the preceding 2,009 ms hive-entry stall or duplicate-UUID admission; those remain the next materialization evidence boundary before any live reset/deploy.
- Live r52 then reproduced the remaining UUID defect under ordinary player chunk entry: two Southgate managed residents were rejected with duplicate deterministic UUIDs even though `ServerLevel#getEntity(UUID)` returned no body to the ambient executor. The cause is confirmed in the join bridge: Minecraft can set `isAddedToLevel()` before publishing that UUID in the global index, while the old bridge discarded the exact candidate on that weaker flag or after 20 ticks. The ambient bridge now retains a bounded exact candidate until the index resolves to that same Java object or the candidate is removed; a second admission and restart-absence inference defer visibly as `PENDING_UNINDEXED`. The negative/recovery GameTest is in the active ambient scene slice. The full critical gate passes with 266/266 GameTests, `guardrails`, `check`, build and packaged-JAR verification. `V3-AUD-024` records and closes this defect. This is not yet live-server evidence: publish a verified artifact, start a fresh disposable world and rerun only the western-hive entry scenario on one `:0` client before claiming the player-visible jerk resolved. The captured r52 JFR did not include a two-second stall and showed no matching GC pause; do not attribute that separate entry stall to this fix without a capture that contains it.
- T0.3 has a first real terrain-provider boundary, not a player-built test ramp: immutable bounded `TerrainSurfacePlan` (uniform baseline plus sparse surveyed columns) is bootstrap-hashed and persisted at schema 98/envelope v7. The route compiler materializes exact provider-owned `ROUTE_FOUNDATION` gray-concrete cells below elevated carriageways; a confirmed foundation loss blocks the same retained topology edge and cannot be hidden by restoring only its carpet. The native `disposable-stepped-route-restart` scenario proves footing → route surface → graceful restart without player scaffolding, and a focused sparse-survey recovery test proves one sampled column stays exact through generic snapshot hydration. The provider deliberately fails closed rather than excavating terrain or making a facility terrace. Remaining V3-AUD-019 gates are a facility on another surveyed datum, bridge/rail-grade, explicit foundation repair/replan and cross-family HOT/COLD continuity.
- V3-AUD-022 was confirmed while closing the explicit foundation-repair gap: the generic structural-repair executor needs its source chest and target loaded together, so it cannot own a remote route repair without force-loading. The correct next owner is a bounded route-maintenance operation using exact source decrement → retained cargo → same retained engineering team/COLD-HOT worksite → observed target repair; it reopens only edges no longer affected by any retained route loss. The temporary generic-repair implementation was rejected before commit; its focused domain tests passed only as evidence of the unsafe boundary, while the re-run unchanged 36/36 scene slice confirmed no unrelated motion regression.
- T0.1 is now coherent at the whole current scene boundary: `SceneMemberPosition` and `ActorDied` retain `BodyPosition`, so logistics, settlement assault, medical and engineering ambient hand-off, release and death evidence cannot silently reinterpret a feet cell as a support block. `SceneBehavior` receives/returns body positions directly; old source-level SceneLease compatibility constructors and legacy coordinate helpers are removed. The current schema 98/persistence-envelope v7 reject prior bytes before hydration or WAL replay. A bootstrap clearance regression found that a tall `BROOD` organ could intersect the upper collision cell of a perimeter Zombie; the slot compiler now reserves support, feet and both head-overlap cells. The pure bootstrap hash contract has new deterministic values for that intentional manifest change. Focused persistence/death/medical/engagement tests, the 33/33 Scene GameTest slice, and the complete critical gate (`guardrails`, `check`, production JAR/package verification, 261/261 GameTests) pass. While investigating a player report of jerky hive mobs, the old HOT actuator was found to call `Entity.move` from `ServerTickEvent.Post`, after ordinary entity ticking/tracking. `FrontierV3ControlledMobMotion` now records only a bounded one-tick noncanonical local intent at the post-canonical executor boundary; `EntityTickEvent.Pre` applies its same collision-checked direct/side/low-step move before the normal entity tick and tracker replication. Canonical target/cursor, NoAI, collision authority, and observed arrival remain unchanged.
- `3f3757ba` closes the remaining visible ambient-patrol stop/go defect. A local circular target may move less than the ordinary exact-arrival radius per tick; applying the exact-arrival stop to it made each HOT bioform wait, then take one fixed 0.26-block step. The actuator now has an explicit continuous-local mode that reaches the current local target without overshooting while directed transit, assembly, assault and scene routes retain exact arrival semantics. A new GameTest proves that a continuous target inside the normal arrival radius still creates a small physical step. The scene slice passes 34/34 and the complete GameTest suite 262/262; guardrails, `check`, production build and packaged-JAR verification pass. A full-pack visible live run against r50 (`visible-hive-motion-west-nest`, run `7c871ab8-e8f7-463c-915e-8c43424c7a1c`) reached the real western nest and proved `bioform:west-0` HOT/grounded with two clean frames; the lite client cannot connect to the full-pack live server because Better Combat requires client network channels, so live visual tests against that server must use `FRONTIER_V3_PILOT_PROFILE=pack` until a genuinely protocol-compatible dev profile is defined.
- On 2026-09-02 the live test service was reset to fresh disposable `frontier-v3-live-r51` on `25565` (seed `9031746258841137206`) because committed `001f23dd` introduces schema 98/persistence-envelope v7; r50 remains on disk but is no longer selected. The hosted and installed Pale Mirror JAR SHA-512 is `de3bf0ca5aa3894c46f698405ce2a8e43a2d1828c00972aeff4fda4f253e137b7471b9e644f2a88d094fed20170c974a2a2dab81f3d6f95696a642fe148954d4`; transient `far-frontier-v3-live.service` runs Java PID `1496462`, reached `Done`, logged `Frontier v3 runtime started for ./frontier-v3-live-r51/`, and listens on `25565`. Full-pack visible scenario `visible-hive-motion-west-nest` run `f600d3e8-24cc-4e39-8960-34dc3175d97a` succeeded: `bioform:west-0` was the same UUID, `ALIVE`, `HOT/PATROL` and physically indexed, with clean before/after frames under `build/frontier-v3-scenarios/`. This proves deploy/boot plus the narrow smooth-ambient-motion flow, not whole-world player-comprehension acceptance. No client update is required for this server-side revision.
- The live r44 server exposed V3-AUD-020: a following HOT operation-assembly body could try to enter a canonical member's still-occupied cursor, and the executor constructed an invalid overlapping `OperationAssembly` before the command lane could reject it. The runtime correctly quarantined instead of committing an inconsistent state, but the world could no longer progress. The current verified replacement defines one assembly move as immediately collision-free only: `OperationAssembly` owns deterministic safe-cursor selection and a single-member advance; COLD and HOT logistics consult it before movement and before arrival, while engineering assembly uses the same rule. The regression proves leader-first queue hand-off and rejects the follower move. Node pilot schema tests pass 30/30; focused domain/NeoForge tests pass; the repeated critical gate passes (`guardrails`, `check`, build/package verification, 260/260 GameTests). The first full attempt had one unrelated `HumanHealthProcessTest` failure, which immediately passed isolated; the repeated full gate is green. This is ready for a fresh r45 deployment, not product/visual acceptance.
- The test-only `stepped-route` profile and `disposable-stepped-route-restart` pilot provide a bounded read-only `route_topology` diagnostic and prove ordinary player support placement, raised route-cell materialization and graceful-restart retention for a declared four-edge grade. The profile/catalog remain absent from the production JAR. Its captured frame is functional materialization/recovery evidence only: temporary support blocks and nearby actors obstruct the grade, so it is not player-comprehension or visual acceptance. `V3-AUD-019` remains open for a genuine non-flat provider, bridge/rail-grade and cross-family HOT/COLD proof.
- The HOT cargo provider now consumes its typed `TransportAnchor` strictly: an exact support must be present for materialization, readiness and arrival; X/Y/Z all participate in arrival; and the no-gravity graybox cart rises to a retained one-block grade before its horizontal crossing and descends only afterward. It never uses the ambient heightmap fallback. `cargoCarrierFollowsTheExactRaisedTransportSupportRatherThanFlatteningItsGrade` passes in the 33/33 Scene slice. The investigation exposed the still-open V3-AUD-019 actor-boundary debt: historical `ActorLocation`/ambient lease `BlockPosition` values mix feet and support semantics, so their heightmap adapter is now deprecated and explicitly isolated pending one fresh-world typed actor/lease migration; it must not become a provider for new route, scene or transport code.
- The r46 fast-forward exposed a market-planning defect before any invalid event reached the reducer: it priced a demand from an arbitrary Works founder contract, then admitted production for whichever exact industrial worker was currently available. A change in availability could therefore make the quote disagree with the job and quarantine the server. Market clearing now derives the quote only after `ProductionStarted` has selected its exact worker and active contract; missing terms or a surviving mismatched quote retain the open demand and schedule the bounded canonical retry, never a fabricated reservation or a runtime exception. The negative stale-quote regression and focused `ProductionProcessTest` pass; the full critical gate passes with 261/261 GameTests, build/package verification and guardrails/check.
- The annual audit previously advanced directly between due actions, so it could not prove the server's actual one-tick cadence that exposed r46. `deployedSeedStaysActiveForOneRealServerCadenceDay` now advances the deployed seed through all 24,000 individual ticks and proves the kernel remains active while every live market order matches the exact worker contract that priced it. The targeted annual-audit task and `guardrails check` pass. This is a canonical liveness regression, not a replacement for live player/terrain evidence.
- On 2026-09-01 the checksum-pinned `4afec446` artifact (SHA-512 `34a45bf3c9d7b8a46d88d104de1e1fb4bcc43c272f55796a84b705f880afe114c2105b6e458620f9e61f97b856fc3fd984b0d449b5ed4e0ac8934f237f46277d`) was installed from the hosted pack into a new disposable `frontier-v3-live-r44` world. Earlier `r42` and `r43` directories remain untouched but are no longer selected. The transient `far-frontier-v3-live.service` runs Java PID `747989` on `25565`; fresh logs contain both `Done` and `Frontier v3 runtime started for ./frontier-v3-live-r44/` without a crash loop. This proves current-revision deployment/boot only, not a product or visual acceptance gate.
- `4afec446` removes the horizontal-only assumption from the retained supply-route compiler. A persisted replacement may now declare an orthogonal bounded grade; the pure compiler deterministically expands it into adjacent grade-0/1 nodes, materializes the identical three-wide envelope, and maps a raised lateral loss only to the retained edge it supports. It neither queries Minecraft terrain nor finds a detour. The physical executor still refuses unsupported upper cells, so an elevated surface appears only where the loaded world already supplies its exact terrain datum. Focused topology tests and a physical stepped-support GameTest pass; the full critical gate passes with 259/259 GameTests, guardrails/check, build and packaged-JAR verification. This is a reusable compiler/materialization boundary, not yet the missing production non-flat terrain provider, rail-grade evidence or cross-family HOT/COLD proof; `V3-AUD-019` remains open.
- The current T0.5 Foundry slice makes loaded port and edge state explicit without granting Foundry mutation authority. `FrontierV3TraversalFoundryAudit` now derives `OPEN`/`BLOCKED`/`UNVERIFIED` for each semantic throat and `OPEN`/`PENDING`/`BLOCKED`/`UNVERIFIED` for each declared edge, with stable metrics and a bounded first failure in the read-only diagnostic. Fresh native `disposable_traversal_foundry_port_obstruction` passed on one fullscreen `:0` client: an ordinary player placed gray concrete at the exact Northwatch Infirmary throat, `SETTLED` then graceful-`RELOADED` both retained `blockedPorts=1`, `blockedThroats=1`, zero support mismatch and the same `frontier.port.availability.blocked` position `(-363,64,-318)`; frame `build/frontier-v3-scenarios/disposable_traversal_foundry_port_obstruction-6d0528af-c346-42b1-8379-f097d1b1bc76-northwatch-infirmary-port-blocked.png` is narrow player-visible technical evidence, not a visual/product acceptance claim. Node scenarios 29/29, focused audit JUnit, the 32/32 scene slice, two 14/14 economy slices and the critical gate (258/258 GameTests, guardrails/check, build/package) pass. The same commit fixes an unrelated full-suite fixture leak: the exact-equipment death test now keeps its non-AI witnesses inside its own supported test bounds, so a neighbouring GameTest cannot erase the observed drop. The remaining V3-AUD-019 gates are a genuine non-flat provider, bridge/rail-grade and cross-family HOT/COLD continuity.
- On 2026-09-01 the committed `dc69e5c4` terrain-aware traversal/T0.5 revision passed the complete critical gate (258/258 GameTests), was checksum-published (`166073823cf2e874e519860d4d4bd8b3e2cbe89ccf793f7dc96adab3accf77ad33ef0175b1bca288db4df2ca2630bafbb8bb231d1a2d55fd7264ed60487c10f1`) and deployed as a mandatory fresh world `frontier-v3-live-r42` on `localhost:25565`. The transient `far-frontier-v3-live.service` is active; startup reached `Done` and `Frontier v3 runtime started`. The former r41 directory was recoverably moved to `/home/rd/far-frontier-server/.far-frontier-installer-cache/retired-worlds/frontier-v3-live-r41-reset-20260901T1421Z`; it is not an active compatibility world. This proves fresh deployment/boot only, not immediate battle or player-comprehension acceptance.
- `V3-AUD-019` is the accepted open terrain/movement foundation debt. The architecture contract, implementation T0 gate, Create direction and engineering rules now prohibit extending mixed `BlockPosition`, constant-Y corridors, fixed-compass entrances or adapter-owned alternate paths. The current graybox may remain physically flat only as a uniform-datum provider of the same typed 3D topology. No implementation or real-terrain support is claimed by this documentation milestone.
- `b8bec7bd` begins T0 implementation at the medical scene boundary: immutable `SurfaceAnchor`, `BodyPosition`, `FacilityFacing`, `SettlementInfirmaryTreatmentPort`, bounded grade-checked `TraversalPath` and observed-only cursor values distinguish floor, body and oriented access. The treatment approach deliberately crosses the raised infirmary datum and its HOT executor moves only toward the same compiled support sequence. Closed-scene cleanup is now generic before every registered behavior, so a released medical/engineering/assault projection cannot remain tagged by a historical lease; it does not treat a same-UUID ambient hand-off as a conflict. The checked-in disposable medical HOT/COLD/restart run `disposable-hot-medical-treatment-restart-1788242673859` reached terminal `RECOVERING`/`COMPLETED`/`CONFIRMED` without a false UUID conflict. Node 29/29, focused pure/Scene GameTests 29/29 and the full critical gate (`guardrails`, `check`, build/package, 253/253 GameTests) pass. Audit closure remains pending T0.3–T0.5.
- T0.3 now retains one movement cursor per owner: persisted supply-route waypoints compile into one bounded immutable `TraversalTopology`; `OperationTravel` retains that topology/cursor with exact body and transport anchors; and every `OperationAssembly` member retains its own typed pedestrian topology/cursor rather than a raw support list. The current schema 98/persistence-envelope v7 are the sole accepted persisted format; all other world bytes fail closed and the world is recreated. The strategic and patrol sub-decoders reject historical layouts too, so an internal caller cannot bypass the fresh-world rule. The HOT adapter translates every formation/cargo position and its target Y by the retained edge grade, rather than flattening a stair; the assembly adapter converts the typed support exactly once through its standing provider and observes the distinct feet cell. A focused graded-assembly regression retains surface `y=65` and derives body feet `y=66`; focused pure tests and the 31/31 Scene GameTest slice pass. The first route-return failure was a compiler defect: only the centreline had physical support although the retained convoy formation occupies three adjacent columns. `FrontierRouteNetwork` now compiles a three-wide physical carriageway while topology remains one canonical centreline, and the plan rejects accidental ownership loss at the Hall/public-route seam. Restart recovery waits a bounded 20 ticks after every exact member/cargo column becomes naturally loaded before a missing UUID becomes durable evidence; it neither force-loads nor creates an entity. Fresh native `disposable_route_obstruction-1788268565742` closes the edge-availability/restart half: normal player removal of `(-380,64,-304)` yielded the exact `KNOWN_SEMANTIC_LOSS` route delta, an unavailable retained topology edge and a clean `ROUTE DAMAGE` frame before graceful restart; after an ordinary visit of the exact scene columns the same retained resident UUID reclaimed HOT, then natural COLD release terminally failed `operation:supply-1-2` with the delta and blocked topology still retained. It used only `viewDistance:2` test scope and no force-load. The remaining T0.5 work is the reusable non-flat port/route and rail-grade Foundry evidence at `COMPILED`, `SETTLED` and `RELOADED`; V3-AUD-019 remains open.
- The T0.5 implementation now has a reusable plan-owned facility boundary instead of the former implicit west/XZ conventions: `SettlementStructure` owns stable `FacilityFacing`; `FacilityTraversalPort` names exterior approach, threshold, interior connector, bounded stations, capabilities and two-body throat clearance; Hall and Infirmary derive it from that plan. The current flat provider does not falsely claim excavation or a physically non-flat ramp: its support stays at the uniform datum. The compiled public sidewalk joins its exterior to the Hall route in a bounded bidirectional `SettlementLocalCirculation` topology and is materialized as settlement-owned public route surface. `FrontierTraversalPlan` binds each Hall/Infirmary to exactly one declared public topology, ready for Foundry rather than a nearest-route/runtime path query. This changes the bootstrap manifest digest; by policy every V3 world using it must be recreated. Focused pure port/circulation/traversal-plan tests pass.
- `FrontierV3TraversalFoundryAudit` now consumes that one immutable traversal plan. Its Minecraft-free `COMPILED` pass verifies every topology edge's grade/clearance/capability, every semantic support and throat, and each port's declared public connection; it reports bounded stable Foundry metrics/findings. Runtime auditing is strict about provenance: fresh plan-owned air without any ledger claim is `PENDING` projection work, while `CURRENT` requires exact plan/ledger/block agreement and foreign, changed, unclaimed-looking or conflicted blocks remain errors. The `disposable-traversal-foundry-restart` pilot passed `COMPILED`, then naturally visited scope `structure:1-infirmary` to reach scoped `SETTLED` and graceful-restart scoped `RELOADED`, both with zero pending/unverified/mismatch/throat blockage; it captured `build/frontier-v3-scenarios/disposable_traversal_foundry_restart-ea970c6e-b542-4d2d-8cef-e67cbd5fe993-northwatch-infirmary-public-connector.png`. This is narrow materialization/recovery evidence, not non-flat terrain or broader player-comprehension acceptance. The separate real-terrain, blocked/damaged-port, bridge/rail-grade and full HOT/COLD continuity gates remain T0.5 work.
- `4ddfa1ba` closes V3-AUD-018: after restart, a naturally loaded exact actor column with an
  `UNKNOWN_AFTER_RESTART` ambient lease and no exact physical body emits one typed absence
  observation that closes only that failed lease. The living canonical actor is retained; the
  next ordinary player demand materializes the same deterministic UUID. Unloaded, foreign or
  mismatched bodies remain unknown/conflict. Pure recovery/forgery checks, the focused HOT/COLD
  slice (29/29) and the critical gate (`guardrails`, `check`, build/package, 252/252 GameTests)
  pass. The checksum-pinned artifact `8b01f33d…ced87f` is installed on retained
  `frontier-v3-live-r41` without a reset; it is healthy on 25565. A native pack-pilot scenario
  on one `:0` client proves `bioform:west-0` as `ALIVE`, `HOT`, physically `INDEXED` at
  `(-426,64,415)` with UUID `74dea1a5-3577-3fac-b13e-2a084920465c`; the same frame visibly
  contains its hive organs and multiple Zombies at
  `build/frontier-v3-scenarios/visible_hive_west_nest-88cd6b36-44d7-46fb-acec-db8ed00dd4ef-hive-west-nest.png`.
- The native pack-pilot runner now directs screenshot acknowledgement to its actual pack-client
  run directory and resolves default frame destinations from the Pale Mirror root. The retained
  live hive scenario asserts stable world facts and its terminal actor state, not the incidental
  live count of autonomous bioforms; Node checks pass 28/28 and the live manifest is `ok`.
- The committed schema-90 slice completes the first HOT medical-treatment scene and repairs
  retained same-floor Scout patrol leases without weakening quarantine. `ScoutPatrolLeaseRecovered`
  accepts only the living exact HOT Scout at its exact stale lease goal, keeps its canonical
  cursor, and atomically retargets the deterministic next patrol cell; forged/moved/foreign
  recoveries reject. The closed pure/NeoForge behavior registries and process emitted-payload
  contract now include medical/recovery ownership. Disposable
  `disposable-hot-scout-patrol-recovery-restart` passed on one fullscreen `:0` client after a
  graceful restart: the same `bioform:west-1` UUID remained HOT and retargeted to
  `(-407,64,414)`. Focused tests, Node scenario schema checks and the full critical gate
  (`guardrails`, `check`, build/package, 251/251 GameTests) pass. Two pre-existing GameTest
  fixture assumptions were made explicit: nearby exact drops no longer depend on the global UUID
  index, and the harvest fixture clears/claims its own depot socket.
- `4b4bb0f` records the H0.6 nested-repository milestone. `88a6128` and `035ba09` close V3-AUD-015/016 without weakening quarantine: recovery selects the verified header ruleset before bootstrap, and only unambiguous field-target evidence selects the legacy strategic layout. Focused old-byte tests and the critical gate pass (248/248 GameTests). The exact r41 snapshot plus WAL restarted on hosted/server SHA-512 `61e5d3c0…c71c1d0`, reached `Frontier v3 runtime started`, and remains healthy on 25565.
- The approved Frontier v3 durable goal is active.
- The accepted hive physiology/infection refinement is now normative in the dedicated contract, main v3 contract, Wave 5/6 plan and architecture map. Implementation has not yet replaced `HEART`, split bioform chassis/mutation/assignment/lifecycle, added hiveroot segments/cocoons or implemented Relay/Overseer/Sentinel mobilization; these are goal scope, not completed claims.
- The future Create orientation is now normative in its dedicated document, the v3 contract, Wave 6/post-graybox plan and architecture map. No Create-backed v3 mechanism or train is claimed implemented; the current goal adds only the provider-neutral seams needed before the separate post-graybox goal.
- The human capability/unit model is now normative in its dedicated document, the v3 contract, Wave 4 plan and architecture map. Source schema 81 separates profession/capabilities from provisional `ResidentRole`; active settlement assaults now own an exact defender unit and its exclusive derived defence assignment. The first typed civilian interruption is limited to pre-effect COLD market production. Exact issue/return, physical death/drop/pickup and first exact defender readiness now exist; automatic admission, broader recovery, role expansion and HOT/COLD mobilization proofs remain current-goal scope.
- Static architecture audit `docs/frontier-v3-architecture-audit.md` at `5ca5291` records four confirmed defects, six structural debts and two unmeasured scale risks. Its remediation table is the active closure register. `architecture.yml` and `AGENTS.md` own the prevention rules; `tools/engineering/frontier_v3_architecture_debt.yml` plus its fast validator prevent ceiling growth.
- H0.1 is closed in `e9bc0d0`: `SceneLease` no longer exposes logistics-only accessors; complete pure and NeoForge `SceneBehavior` registries own generic scene lifecycle policy. Duplicate/missing registration fails before mutation; the scene-branch ceiling fell from 47 to 4 sealed-codec compatibility branches. Focused registry tests, scene GameTests 30/30 and the complete critical gate (250/250) pass.
- H0.2 is closed in `9281ab5`: every fixture builder and the sole profile catalog live in test-fixtures/pilot sources; production startup always creates only the normal world configuration and ignores fixture-profile JVM properties. The pilot bootstrap is absent from the production JAR and may pass only an explicit prebuilt configuration. Java/Gradle/Node derive or validate the same catalog; unknown/duplicate profiles and any production JAR fixture symbol fail. Catalog/lifecycle negative tests, Node 26/26, a disposable pilot smoke and the complete critical gate (250/250) pass.
- H0.3 has closed V3-AUD-004. A pure deterministic stage plan rejects duplicate writers, missing/reversed dependencies and cycles; the NeoForge server runs one registered physical plan instead of 22 causally ordered source calls. Every executor declares a stable ID, explicit stage, dependencies, exclusive physical write kind and one-invocation bound; `PMV3_DIAG execution` exposes that read-only order. Stage identity does not rely on enum source order. The direct 22-call debt ceiling is zero. Scheduled planner routing now lives in the closed process catalog rather than a runtime string switch, and its ceiling is also zero. The same work establishes the closed process ownership/catalog foundation; V3-AUD-003, V3-AUD-011 and V3-AUD-012 are now all closed.
- H0.4 has closed V3-AUD-005: `FrontierWorldStateUpdate` is the typed named aggregate-update boundary. It names only changed sub-aggregates, rejects duplicate declarations, permits a revalidation no-op and retains every undeclared component by identity. Every process/support direct aggregate reconstruction was removed; the source ratchet fell from 38 to 14 remaining constructors, confined to `FrontierWorldState`, fresh bootstrap and versioned hydration. Its pure property coverage asserts preservation of all 21 unrelated components.
- Scene leases have one typed canonical cause rather than treating every coupled HOT/COLD scene as a caravan: `LogisticsSceneCause` owns the existing operation/cargo/engagement/anchor relation, while `SettlementAssaultSceneCause` owns a cargo-free battle. Snapshot schema 79 writes cause-specific fields and still reads v77/v78 logistics snapshots. Legacy logistics WAL remains deliberately unable to encode an assault; separate versioned assault prepare/handoff payloads carry only the exact battle cause, members and floors. Typed owner resolution and aggregate validation reject a foreign cause, duplicate/ambient-owned participant, destroyed Hall, missing combatant or fictitious cargo before persistence.
- Settlement assault now has a distinct cargo-free physical executor, not a logistics compatibility branch. Its exact Scout-sighted assault (`27` retained attacker/defender identities in the development fixture) atomically hands ambient actors into a typed `SettlementAssaultSceneCause`, creates only the persisted exact Villager/Zombie bodies, applies ordinary bounded physical strikes, and never asks for a carrier/cargo. In same-JVM recovery it reclaims only the exact persisted bodies; after restart a missing/changed body remains visible `UNKNOWN`, without re-creating a battle. The new `disposable_settlement_assault_restart` native scenario reached `HOT`, `CURRENT`, `strikeStatus=CONFIRMED` and `settlement_assault_reclaimed` after a graceful restart on one fullscreen `:0` client; its clean frame `build/frontier-v3-scenarios/disposable_settlement_assault_restart-035ca241-8e0c-4bd2-924b-5037ea5b6824-settlement-assault-reclaimed.png` visibly contains the real green Zombie among the defended graybox settlement. The pilot's visible-entity evidence now also requires Minecraft line-of-sight, rather than only camera angle. A cargo GameTest's broad nearby-cart assertion was narrowed to the same exact lease/cargo ownership so a parallel independent scene cannot contaminate the no-duplication proof. Node 26/26, scene GameTests 30/30 and the complete critical gate (`guardrails`, `check`, build/package, 250/250 GameTests) pass.
- Hive territorial pressure now has a real discovery-to-projection path: schema 76/WAL retain at most 32 Scout-local settlement-anchor sightings; only a living Scout inside 64 blocks may create one, and a fresh sighting only steers the existing fresh-belief infection frontier toward that anchor. It grants neither remote structure/actor knowledge nor an attack. Scout patrol is now a bounded, in-bounds seed-nest scouting circuit (100-tick / 16-block steps; the production profile reaches a settlement sensing radius without a fixture teleport), while predecessor-less deployed payloads remain confined to their former 48-block circuit. Focused perception/frontier/patrol tests and the pure module's 303-test suite plus `guardrails` pass. The next slice is the distinct assault/siege operation; it must use this fact rather than treating a route interception as a settlement attack.
- The hive-growth source mismatch is repaired: growth derives its named nest, organ and bioform from the exact local STORE holding biomass and rejects every cross-nest start/consumption/intent. The bounded organ-network transfer is now complete across COLD and naturally loaded endpoints: one named 64-item stack leaves an exact STORE slot only after `HIVE_NUTRIENT_DEPARTURE` is durable-before-effect, advances the retained ≤256-node corridor unchanged, then reaches the target exact STORE slot only through `HIVE_NUTRIENT_ARRIVAL`; only a confirmed arrival may feed local growth. Endpoint ownership, item identity, counts and loaded postconditions are checked on both sides; foreign/missing/changed states fail closed and restart inspects rather than replays. Schema 75/WAL preserve the endpoint intent and receipt. The checked-in `disposable_hive_nutrient_transfer_restart` passes a graceful restart on one fullscreen `:0` client, terminal `hive_nutrient_arrived` trace, exact consumption and one physically visible red grown Heart with live coloured bioforms (`...0a8aaa93-e220-4246-bd4d-bee9ac8833ec-west-grown-heart-after-nutrient-arrival.png`). The pilot now captures Minecraft's own F2 framebuffer rather than an unreliable XWayland backing image; camera visits are outside organ shells and frames occur after their visible assertion. Node 26/26, focused pure tests, economy GameTests 11/11 and the full critical gate (`guardrails`, `check`, build/package, 249/249 GameTests) pass.
- The hive now persists its selected doctrine in snapshot schema 72 and WAL: fresh Scout sightings choose `INTERDICT`, exact hive biomass or absent fresh territory chooses `CONSOLIDATE`, and fresh territorial beliefs otherwise choose `EXPAND`. The planner gates its next task by that posture without global infection scanning; foreign-owner and backdated doctrine events fail closed. Focused freshness/codec/rejection tests and the full critical gate pass: `guardrails`, `check`, build/package and 247/247 GameTests; this uncommitted domain slice has no new live-world/materialization claim.
- Scout-driven interception now has a fresh native end-to-end proof rather than only independent sighting and bomber proofs. A physical HOT Scout sees the exact Minecart cargo anchor, creates the one retained intercept task, the player leaves so the route scene drains and COLD approach/combat advances, then returns to revive the same engagement as HOT; a real owned Bomber TNT blast is reconciled to terminal `CONFIRMED` without a protected zone or replay. `SceneLease` explicitly separates the people hand-off from the cargo carrier anchor, so COLD/HOT agree on what the Scout saw. Opaque lease/intent IDs no longer expand into bounded Minecraft command IDs: the serialized checkpoint revision is the idempotency key and payload/PMV3 trace retain ownership. The new isolated `disposable_hot_scout_intercept` native run `b12edf6d-4af4-4481-acf3-dd7218ac82bc` passed on one fullscreen `:0` client; its clean frame `build/frontier-v3-scenarios/disposable_hot_scout_intercept-b12edf6d-4af4-4481-acf3-dd7218ac82bc-hot-scout-intercept-aftermath.png` visibly shows the explosion crater, live Villagers and coloured hive bodies. Scenario assertions are now action-bound, so a later diagnostic cannot invalidate an earlier causal milestone. Focused pure/Node tests pass, and the final critical gate passes `guardrails`, `check`, build/package and 247/247 GameTests; the gate also corrected one off-template cargo-carrier GameTest fixture without altering production behavior.
- The first Wave-5 territorial-belief boundary is implemented: a bounded durable map (256 cells, 2,400-tick target age) records only nonzero infection sensed by a live Scout within 48 blocks or an operational Heart within 32 blocks, with the exact sensor position retained in snapshot schema 71 and WAL. Each strategic refresh admits at most four deterministic observations. Hive expansion now derives its frontier from fresh beliefs, never the global infection map; only its own Heart root is allowed as the no-belief fallback. Same-tick Scout movement admits only the exact immediately preceding patrol position, preventing both false failures and arbitrary historical-coordinate injection. Focused negative/recovery/codec tests and the full critical gate pass: `guardrails`, `check`, build/package and 247/247 GameTests.
- `d40dc0f` completes the exact COLD perception boundary after `9ebeba7` removed global hive knowledge: only a living unleased Scout inside the bounded local radius can emit/persist `HiveOperationObserved`; a fresh sighting creates an intercept task that pins its exact operation and seen position. Its executor approaches that pinned position and may inspect current operation state only for liveness and whether the named caravan meets it, never to retarget; a legacy task with no seen position blocks rather than acquiring hidden coordinates. Schema 70 retains the field; compatible old WAL task payloads decode with no position and fail closed at execution. `45933a2` extends its autonomous bounded seed-nest patrol across HOT/COLD without a second movement state: canonical actor position is the one shared cursor; COLD records exact predecessor→next step, while a HOT Scout moves to that same leased target and its accepted physical arrival atomically advances the cursor and retargets the lease. Scout physical arrival/drain now uses the one exact feet cell above the owned floor anchor, rather than conflating floor and body height. Predecessor-less deployed WAL remains COLD-only compatibility and cannot move a HOT body. The full gate additionally exposed a parallel-GameTest collision from a fixed fixture `WorldId`; the fixture now derives an isolated deterministic ID from its cell, without changing production identity. Focused pure/scene tests (28/28 GameTests) and the critical gate (guardrails/check, build/package, 246/246 GameTests) pass. On 2026-08-31 08:24 +05, the retained `frontier-v3-live-r41` service deployed this commit's SHA-512 `d35b4375…59772`, reached `Done` and `Frontier v3 development runtime started` on 25565 without a new PMV3 quarantine.
- HOT Scout perception now has a distinct durable `HotScoutOperationObserved` payload: a loaded physical Scout must have its exact `SCOUT_PATROL` lease and observe an exact tagged Minecart carrier inside a HOT non-engagement scene; the pure reducer proves the full Scout → scene lease → operation → cargo-anchor chain before it updates bounded hive knowledge. Forged position and released-lease paths reject. The trace retains the same bounded provenance. Disposable `hot-scout-sighting` has no production effect: it relocates only the named test Scout, suppresses only subsequent COLD progress for its operation, and closes the physical scene before its graceful restart assertion. Its fresh native run `disposable-hot-scout-sighting-restart-1788148277442` proved physical sighting, trace causal keys and retained `(-367,64,-340)` knowledge after restart. Node scenarios pass 25/25; focused pure and Scene GameTests pass 29/29; the full critical gate passes `guardrails`, `check`, build/package and 247/247 GameTests. `7e3c746` deployed on 2026-08-31 08:56 +05 to the retained `frontier-v3-live-r41`: service `far-frontier-v3-live` PID 4175036 listens on 25565, loads Frontier v3 without a new quarantine, and its installed `pale_mirror-hosted.jar` SHA-512 is `1c1eef…fae33d`.
- A verified HOT Scout sighting now atomically records its knowledge and creates exactly one next-tick intercept opportunity. The opportunity has no coordinate authority of its own: it can select only the same retained Scout fact identified by its deterministic schedule ID; a replaced/stale fact cancels, rather than retargeting to a later caravan position or unrelated hive work. The resulting exact task keeps that seen coordinate, while operation diagnostics expose only its live `PENDING`/`ACTIVE` intercept. Pure tests cover duplicate, forged, restart and replacement-sighting paths; the physical Scene GameTest proves the exact Minecart carrier reaches the pinned task. The refreshed native `disposable-hot-scout-sighting-restart-1788150172166` passed on one fullscreen `:0` client: after graceful restart, `operation:supply-1-2` retained the observed carrier and active intercept at `(-367,64,-340)`. Node checks pass 25/25; the final critical gate passes `guardrails`, `check`, build/package and 247/247 GameTests. Two unrelated GameTest fixture races were corrected by keeping their TNT/carrier bodies inside their own templates; no production chunk-loading semantics changed.
- `4b858ab` was published and deployed on 2026-08-31 09:34 +05 to the retained `frontier-v3-live-r41` world: its `pale_mirror-hosted.jar` SHA-512 is `ec9559…5c1ff`, transient service `far-frontier-v3-live` has Java PID 186824 listening on 25565, and fresh logs contain both `Done` and `Frontier v3 development runtime started` with no new PMV3 quarantine.
- `a6889fc` closes the remaining terminal-logistics retention gap: schema 59 persists up to 256 deterministic terminal receipts plus delivered/failed/interrupted aggregates, and a durable bounded review detaches an operation/contract pair only after every cargo, active/unknown intent, scene, engagement and intercept-task claim has closed. v58 recovery starts with empty receipt history. The four-seed full-year audit now passes after this change (41=`3c317304…`, 71=`e611e074…`, 31337=`9fec90ac…`, 9031746258841137206=`8896a6a0…`), as do the midyear snapshot recovery and normal food loops; full `runGameTestServer` is 245/245. This closes the autonomy/retention calibration slice, but does not claim Wave 4 complete: registered companies/institutions, monetary accounts, market clearing and the remaining human operations are still absent.
- `229077f` makes `STARVING` exact residents ineligible only for newly admitted production, farm, patrol and logistics work. Existing durable work retains its worker through its own terminal/failure boundary, preventing an acknowledged COLD/HOT effect from being silently cancelled. `WORKER_UNAVAILABLE` is a typed production refusal; a food receipt restores the same person to eligibility. Focused normal/negative/recovery tests and the full 245-GameTest critical gate pass.
- `4622a36` adds the Wave-4 exact economic-owner and first wage boundary: every production container, cargo and item claim is validated against a bounded persisted account registry rather than an implicit owner string; bootstrap gives each settlement a finite explicit genesis treasury reserve, and every later money change is an internal zero-sum settlement→company→resident transfer. There is deliberately no generic client/physical-executor money-transfer command. A bounded CompanyRegistry now also retains one exact active employment contract per resident and its matching RESIDENT account. A confirmed works job invoices its settlement and pays the named crafter atomically with the exact output confirmation; finance failure blocks before a physical production transformation begins. Active employment reserves that exact worker from migration, while a historical founder may migrate without invalidating a company. Snapshot schema 62 and typed company/employment events retain the full legal/account state, while schema 61 re-enters with no employment and the canonical review repairs it. Focused company/production/economy/migration tests, the full pure Frontier module, economy GameTests 10/10 and the critical gate (`guardrails`, `check`, build/package, 245/245 GameTests) pass. Market clearing, licenses, credit/debt, company insolvency transitions and broader contracts remain later Wave-4 work.
- Finance admission is now an exact production start precondition: an unaffordable prospective works job emits the typed `FINANCE_UNAVAILABLE` block before it creates a `ProductionJob`, so a later planner review cannot occupy the same workshop twice. The reducer binds that pre-start block to the one exact pending task and prospective job, rejecting forged blocks or an actually funded job. Focused production tests and the seed-41 full-year ordinary/recovery audit pass deterministically (revision `1273892`, digest `6b02ae54…`); the critical gate passes with 245/245 GameTests. This fixes the lifecycle crash, but it does not make the finite bootstrap treasury a complete market: the next economy subwave must add the closed demand/revenue, quote/order and credit/insolvency model before making broad production depend on finance.
- `6561a32` replaces a race-prone finance check at production admission with a bounded named `FinancialReservation`: it binds each exact production-job invoice to payer, payee and reason, prevents concurrent transfer/reservation overcommitment, persists in schema 63, and settles the settlement→company leg only when output confirms; the company→worker wage follows atomically. Release is explicit and never mints money. Legacy v62 snapshots decode without reservations, while new snapshots preserve them. Focused economy/company/production tests, economy GameTests 10/10, the seed-41 ordinary/recovery annual audit (revision `1273892`, digest `de95e1d8…`) and the complete critical gate (`guardrails`, `check`, build/package, 245/245 GameTests) pass. This soundly admits funded work but is not yet a closed market/demand/revenue economy.
- WIP market foundation now has a separately typed, bounded `MarketOrderBook` retained with legal institutions but distinct from money/custody: exact buyer demand, deterministic seller quotes, one accepted work order, expiry and terminal outcomes all persist in schema 64; v63 re-enters with an empty book. Focused market/company tests and the full pure Frontier module pass. The next reduction must atomically bind a food-production task to its accepted order and existing financial reservation before physical/COLD work begins; no order is yet emitted by production planning.
- `1cc191b` closes the market-backed exact production lifecycle without a second inventory: `ProductionInputHold.Cold` owns the removed source stack plus its source slot, while `Materialized` retains the physical depot stack. A typed `MarketWorkOrderCancelled` releases an accepted reservation and returns COLD input only before any production intent; a prepared/RUNNING/unknown intent remains unresolved. A pre-intent player/world departure from a materialized depot atomically moves the exact stack to its observed custody, blocks the task, cancels the exact accepted order and releases its reservation. An output cannot enter a held source slot; every producer/receiver uses the same reservation-aware slot boundary. Schema 67 migrates v66 state. Focused negative/recovery/codec tests, the pure Frontier suite, economy GameTests 10/10, the seed-41 ordinary/recovery annual audit and the full critical gate (`guardrails`, `check`, build/package, 245/245 GameTests) pass; the new disposable `production-input-theft` pilot proves a normal chest quick-move ends with the original 64 wheat in player custody and `CANCELLED` order/false job+reservation/`BLOCKED` task (PMV3 run `3e0a97e1-4934-4776-b12a-38a8f8340961`, frame `build/frontier-v3-scenarios/disposable_materialized_production_input_theft-3e0a97e1-4934-4776-b12a-38a8f8340961-withdrawal-cancelled-production.png`).
- A worker death now closes the exact employment boundary without guessing about an already physical effect. The canonical death transaction terminates that resident's contract: COLD or `PREPARED` production atomically removes its unexecuted intent, restores its exact source input and cancels/releases the accepted market work; `RUNNING` production instead retains its terminated historical contract only until the observed result settles once, so payment/output cannot duplicate or disappear. The dedicated read-only `production-worker-death` fixture identifies the actual accepted crafter, then an ordinary nearest-Villager player attack at `-480,64,-480` proves `resident:1-15` becomes `DEAD`, its order is `CANCELLED`, job/reservation are inactive and task `BLOCKED`; after graceful restart the same resident remains dead. Focused domain tests, Node 25/25, economy GameTests 10/10 and the full critical gate (`guardrails`, `check`, build/package, 245/245 GameTests) pass. This closes the death/recovery edge of the committed production boundary; generalized mortality, insolvency and broader employment lifecycle remain Wave-4 work.
- An accepted exact market order is now readable at its own workshop without a second state surface: the pure local board renders the exact item/count, recognized settlement buyer and exact fixed-point price, never an opaque order ID or invented work state. The one-client native `disposable_materialized_production_order_board` run (`c1dc20b3-082c-467e-8135-b9079b9c0b56`) naturally visited Northwatch, proved the durable order/job/reservation/task remained `ACCEPTED`/active and captured `disposable_materialized_production_order_board-c1dc20b3-082c-467e-8135-b9079b9c0b56-workshop-market-order.png`. A candidate split buyer/price grammar was rejected after its real frame showed bad TextDisplay composition; the compact proven grammar remains. Focused plan tests, Node 25/25, economy GameTests 10/10 and the critical gate (`guardrails`, `check`, build/package, 245/245 GameTests) pass.
- Wave 4 now has its first legal-institution lifecycle without conflating it with finance: a bounded persistent `CompanyRegistry` owns each company's identity, settlement, exact crafter founder, purpose and legal status, while `EconomicLedger` owns only its same-ID COMPANY account. One scheduled canonical foundation review emits `CompanyRegistered`, atomically opening the account and record; duplicate, unpaired or foreign-founder records fail closed. Schema 61 retains this across snapshots and WAL replay; v60 enters with an empty registry. Focused company/economy tests, the complete pure module, economy GameTests 10/10, `guardrails`/`check`, build/package verification and the full 245/245 GameTest run pass. Wages, prices, contracts, credit/debt and insolvency transitions remain later Wave-4 work.
- `569ffa9` extends the committed settlement-provision boundary with exact per-resident nutrition: every cycle rotates a bounded deterministic set of named living recipients, every allocation names its recipients, and only a confirmed COLD/physical exact-item consumption changes those named residents to `NOURISHED`; shortage marks only unserved residents `HUNGRY`, while an unresolved physical effect retains its uncertainty rather than fabricating food or a shortage. Snapshot schema 58 and payload migration retain this state with a fail-closed legacy count-only start-event upgrade. The native `disposable_settlement_provision` fixture deliberately begins Northwatch at `0 NOURISHED / 37 HUNGRY`; its ordinary loaded-depot consumption reaches `37 / 0` and persists over a graceful restart. Starvation now blocks admission to new work, but does not yet cause direct canonical death; that future mortality boundary must preserve the exact resident and HOT-body recovery rules. Do not publish the unfinished source worktree or restart the quarantined retained live world.
- WIP calibration now makes normal bootstrap food autonomy explicit: the first 12 provision reviews wait until tick 24,000, after ordinary COLD field harvest and exact bread transformation can complete. The 98,000-tick seed-41 audit proves all 386 residents `NOURISHED`, all 12 provisions `SECURE`, and no starvation at four cycle snapshots. Food emergency preempts only a still-`PENDING` reagent-less decontamination task; bread remains on the strategic lane. A retention index keeps one newest completed bread task per settlement across the objective-before-task reduction boundary. Known route-surface loss now schedules a one-shot next-tick review only for affected settlements; a confirmed patrol schedules the explicit bypass review, so loss → patrol → bypass begins within 600 ticks rather than waiting for the 2,000-tick periodic review. The planner never creates a second recurring review stream. COLD infection now pulses every 600 ticks (one 4×4-cell eighth per 30 seconds) and immutable resource-site plans plus organ cell counts are cached/formula-based. Full pure Frontier tests pass; seed 41 two-run annual audit is deterministic at revision `1254386`, digest `a7b912671dd41878196d28e069cc108fa209bf3e2ef22a0a30c15d5961a6efeb`, in 2:42–2:55 versus the prior 5:45 baseline. A 45-second current JFR is `build/profiles/frontier-v3-annual-seed41-cold600.jfr`; its remaining dominant work is exact population/planner validation, not prior infection geometry. Terminal logistics now has a schema-59 bounded receipt ledger: a durable periodic review emits `TerminalLogisticsCompacted` only after cargo, intents, leases, engagements and intercept tasks no longer claim the terminal operation/contract pair; exact receipts retain 256 newest deterministic identities plus aggregate delivered/failed/interrupted counts. v58 snapshots migrate with empty history. Focused normal/negative/recovery tests, the two-run seed-41 annual audit (revision `1255825`, 25 retained but non-compactable live pairs) and the complete critical gate (245/245 GameTests, build/package) pass; commit pending.
- The annual audit exposed a pure-autonomy scale defect: strict full-world validation and copy-on-write maps were repeatedly scanning/copying history as infection and terminal operations grew. Current WIP removes schedule-queue cloning, duplicate immutable-map freezing, needless strategic-plan copies and infection-frontier sorting; it adds bounded persistent infection/frontier indices, dependency-aware pre-WAL audits, O(1) exact inventory slot lookup and COLD body geometry without a whole render-plan compile. Persistent infection enumeration now composes base plus bounded deltas lazily; compaction alone materializes a snapshot. The audit also exposed a real ownership conflict: one exact guard could be selected by a supply operation while still advancing an active patrol (and equivalent migration overlap was possible). Canonical availability and validation now make operation, patrol and migration mutually exclusive actor claims. Focused infection/decontamination/health/patrol/inventory/corridor tests pass. Final 35-second seed-41 JFR samples show `itemAt` 559→19, full render-plan compile 93→0 and infection snapshot 832→56 versus the preceding profiles. Pre-fix four-seed evidence passed but exposed `21,144` retained contracts on the heavy seed: blocked pre-effect cargo work left `ORDERED` records forever. An explicit durable `SupplyContractAbandoned` event now removes only an ordered contract with no cargo, operation or physical intent, while creation requires the exact available hauler/guard pair; the focused normal/negative tests pass. The post-fix heavy-seed two-run annual audit passes with 45 contracts/45 operations (revision `1320405`, digest `fe010ca5696b82738641f1fcf1f39250466b25fc9cd418544bc15f44a3d56c4b), and the post-fix seed-41 uninterrupted-versus-midyear-snapshot recovery audit passes (revision `1320398`, digest `f2a7f465da66730a882f2bf5ae39324b4d4c3eae388d304242be4ea70ed07b89`). Repeat the full four-seed audit after this lifecycle correction, then run the critical gate; do not update the quarantined live world.
- The annual audit exposed a pure-autonomy scale defect: strict full-world validation and copy-on-write maps were repeatedly scanning/copying history as infection and terminal operations grew. Current WIP removes schedule-queue cloning, duplicate immutable-map freezing, needless strategic-plan copies and infection-frontier sorting; it adds bounded persistent infection/frontier indices, dependency-aware pre-WAL audits, O(1) exact inventory slot lookup and COLD body geometry without a whole render-plan compile. Persistent infection enumeration now composes base plus bounded deltas lazily; compaction alone materializes a snapshot. The audit also exposed a real ownership conflict: one exact guard could be selected by a supply operation while still advancing an active patrol (and equivalent migration overlap was possible). Canonical availability and validation now make operation, patrol and migration mutually exclusive actor claims. Focused infection/decontamination/health/patrol/inventory/corridor tests pass. Final 35-second seed-41 JFR samples show `itemAt` 559→19, full render-plan compile 93→0 and infection snapshot 832→56 versus the preceding profiles. Pre-fix four-seed evidence passed but exposed `21,144` retained contracts on the heavy seed: blocked pre-effect cargo work left `ORDERED` records forever. An explicit durable `SupplyContractAbandoned` event now removes only an ordered contract with no cargo, operation or physical intent, while creation requires the exact available hauler/guard pair; the focused normal/negative tests pass. The post-fix annual four-seed audit now passes (41=`f2a7f465…`, 71=`d59af8ba…`, 31337=`e6e25287…`, 9031746258841137206=`fe010ca5…`); the heavy seed retains 45 contracts/45 operations at revision `1320405`. The post-fix seed-41 uninterrupted-versus-midyear-snapshot recovery audit passes at revision `1320398`, digest `f2a7f465da66730a882f2bf5ae39324b4d4c3eae388d304242be4ea70ed07b89`. The complete critical gate passes after reconciling the Hall-owned public sill test with canonical `HALL/PUBLIC_ACCESS_SURFACE` geometry: guardrails/check, NeoForge build/package and 245/245 GameTests. Commit `a3a8627` records this slice. Do not update the quarantined live world.
- The retained player-test world `frontier-v3-live-r41` is served at `localhost:25565` by the transient user unit `far-frontier-v3-live.service`. It preserves the existing world (no reset) and runs the checksum-pinned `37552f7` artifact (`SHA-512 82ef8104…f561`) with v3 enabled; the previously live JAR is archived by the installer. On 2026-08-31 07:51 +05 the committed artifact was atomically published, installed and restarted; the log reached `Done` and `Frontier v3 development runtime started`, and the service/port remained healthy without a new PMV3 quarantine. This proves deploy/boot only, not simulation or player-comprehension acceptance. The standing delivery rule remains: publish and restart this world only after a committed revision has passed its applicable gate, never from an unfinished working tree.
- Outer pack commit `3da7f56` removes six independent client-only UI/cosmetic mods (JourneyMap, EZ Actions, Simply Tooltips, EMF/ETF and Polytone), archives stale Pale Mirror JARs during updates, and makes the dedicated-server updater resolve Pale Mirror, Visuals and Railway Untold from one checksum-pinned hosted source. Its local bootstrap and clean temporary-server probes pass; users must rerun their current client updater to receive it.
- `7df3ba8` closes the operation-assembly slice: one durable `OperationAssemblyDeferral` records the exact blocked actor, unchanged next cursor and either that cursor or the compiled Hall throat. It freezes the shared assembly, survives snapshot schema 55 and a graceful restart, and clears only when that same actor normally reaches the same cursor after passage is restored. COLD schedules only a bounded recheck and cannot skip the obstruction. The ambient executor refreshes its immutable snapshot between actors and ends the tick after an accepted deferral, preventing a second member from turning the same shared block into a stale-snapshot quarantine. The native `disposable_hot_operation_assembly` pilot passed on one `:0` client: ordinary gray-concrete placement blocked the real throat, exact deferral persisted across restart, ordinary break produced `operation_assembly_advanced`; clean frame `disposable_hot_operation_assembly-34e19ae3-bfb9-4271-ad30-0ec6b12520fb-assembly-throat-obstructed.png`. Node scenarios 25/25, focused tests and the critical gate (guardrails, check, build/package, 243/243 GameTests) pass.
- The active HOT operation slice now advances the one persisted `OperationTravel` only after all exact leased bodies and its cargo carrier physically reach the adjacent next formation. `SceneLease.rebaseHotOperationTravel` rejects any non-identical current formation/cargo or non-adjacent translation, then atomically replaces the lease and travel checkpoint; a revision-qualified physical command ID prevents a later valid checkpoint from being mistaken for a duplicate. Local bounded movement may step a body up 0.125 block only after normal collision movement makes no horizontal progress, allowing a thin visible route surface without changing X/Z or crossing a full wall. Read-only operation diagnostics now expose bounded per-member physical readiness. Fresh isolated native `disposable_hot_operation_assembly` passed on one `:0` client: ordinary block placement → durable deferral → graceful restart → ordinary block break → `EN_ROUTE`, `travelCursor=4` and `operation_travel_advanced` trace. Its departure frame is functional capture evidence only: the current graybox camera/volume does not visibly frame the convoy, so it does not satisfy visual/player-comprehension acceptance.
- `55fc775` records the atomic HOT-travel slice. The current presentation follow-up removes technical actor/cargo IDs from HOT entity nameplates: bodies now show settlement/hive role labels, and the physical cargo carrier shows its exact player-readable batch. Canonical IDs remain exclusively in persistent tags and diagnostics; a deliberately synthetic test actor receives a safe generic role label, while authoritative scene admission/ownership remains fail-closed. The corrected native operation scenario passed again on one `:0` client through assembly, restart and `operation_travel_advanced`; its final frame confirms the real convoy and cargo name, but surrounding ambient residents still occlude it, so this is implementation evidence rather than visual acceptance.
- The current operation-return slice makes cargo a separate exact physical anchor from every convoy member. A complete assembly derives its cargo slot from the adjacent hauler/escort port formation; `OperationAssembly` and `OperationTravel` reject colliding actor/cargo anchors before a lease can exist, and later COLD segments retain that cargo anchor instead of recalculating it from the hauler. `disposable_route_scene_return` passed twice consecutively on one `:0` client with `HOT → COLD → routeIndex 1 → HOT → operation_travel_advanced` and the same two actor identities. The bounded PMV3 trace now records a classified scene-conflict cause and current body/carrier readiness without positions or player identity. Focused pure/NeoForge/Node checks, the scene GameTest slice (21/21), and the complete critical gate (244/244 GameTests, guardrails, build and packaged JAR) pass. The continuation frame is still not visual acceptance.
- The operation-local readability slice adds a deterministic non-owning TextDisplay passenger to each exact HOT cargo cart. It shows two semantic lines (`<AFFILIATION> CARAVAN` and exact visible load), clears the tallest Villager body, follows only that cart, disappears before player release/ordinary world custody, and never has canonical/recovery authority. The pilot has generic bounded read-only operation-relative visit/look and local-entity visibility actions, so its camera follows the continuing operation rather than stale coordinates. Fresh `disposable_route_scene_return` on one fullscreen `:0` client passed `HOT → COLD → routeIndex 1 → HOT → operation_travel_advanced`; the clean frame `build/frontier-v3-scenarios/disposable_route_scene_return-f1cfe20f-e3b0-414d-8a6b-8fe201bd2c99-route-current-continuation.png` visibly shows the label above two villagers, the chest-minecart and the route. Focused Java/Node checks, 21/21 scene GameTests and the full critical gate (244/244 GameTests, guardrails, build/package) pass; the added release-negative test then passed in the focused scene slice. This is narrow operation readability evidence, not whole-world product acceptance.
- Wave 2 now has checked recovery boundaries, restartable kernel state, exact bootstrap ownership, persisted sparse spatial/economic state, a true write-ahead canonical commit boundary, an opt-in v3-only lifecycle bridge, bounded infection propagation, exact production, contract-to-cargo flow, one exact COLD cargo route, a loaded-chunk CARGO_HANDOFF executor, and the first leased exact-Villager HOT/COLD hand-off.
- `37c85d3` closes exact resident Transit through HOT and restart. The next architectural work is to replace a remaining Wave-3 mismatch in logistics: an active HOT caravan has physical local movement but no durable fine-grained operation cursor, so its route cannot visibly advance through a leave/return hand-off without losing continuity. Do not patch this by teleporting or by allowing COLD progression during HOT; introduce one bounded exact operation-travel cursor/formation with one canonical owner before changing the executor.
- `ee2f6f6` makes that logistics invariant contractual. The active slice now attaches the bounded `OperationTravel` (adjacent corridor, cursor, formation and cargo anchor) to `RouteOperation`, persists it in snapshot schema 50 and WAL, and admits typed start/advance events through the sole reducer lane. Scene demand/leases now use the same current travel position rather than a stale strategic waypoint. Focused pure snapshot/WAL/state tests pass; COLD scheduling and HOT observed-arrival execution are deliberately still absent.
- The first integration audit exposed a prior graybox topology defect: a supply operation is created at the Hall anchor, but the Hall has no semantic entrance/exit and its residents start at independent ambient slots. A correct convoy must therefore add an explicit assembly/departure process and reusable public access-port geometry before `SupplyOperationProcess` begins `OperationTravel`; advancing the old strategic 0→1 segment would merely hide a teleport. Do not paper over this by starting a travel through a wall or by spawning a duplicate body.
- The reusable `SettlementAccessPort` now compiles every Hall's real public departure geometry: a two-body-clear wall throat, one Hall-owned support-required assembly sill and the adjacent route-network floor. The sill retains Hall provenance, so an ordinary break is exact Hall damage rather than an anonymous route hole; unsupported projection visibly defers. This is `COMPILED`-phase evidence only, not a settled/reloaded world claim. Focused pure tests and the critical gate pass with 242/242 GameTests. Next is durable operation assembly: named participants must reach this sill through their existing exact actor state before the same formation can start `OperationTravel`.
- The assembly integration is complete in the worktree: `ASSEMBLING -> exact per-member approach -> atomic OperationTravel start -> atomic arrived-segment hand-off` replaces the old Hall-anchor teleport. Snapshot/WAL schema 53 retain exact assembly and travel state; active scene leases retain each member's exact floor anchor and cargo floor anchor. COLD records do not pretend to be physical blockers, while non-CLOSED HOT ambient leases reserve their actual canonical floor and loaded Minecraft admission remains authoritative. One shared executor resolves a thin carpet/overlay floor to the first two-cell-clear feet space in the same column, never shifts a body sideways. Stale pure/runtime fixtures now use the real paused first-caravan profile and wait for terminal/domain results. The isolated visible `disposable_hot_cargo_theft` pilot (run `1b53b6bf-3ef9-4e9d-9773-03467488bbcc`) reached READY then HOT bodies/carrier, transferred the real bread stack to player custody and terminally interrupted the operation; its frame is `build/frontier-v3-scenarios/disposable_hot_cargo_theft-1b53b6bf-3ef9-4e9d-9773-03467488bbcc-stolen-hot-cargo.png`. Focused tests and the critical gate pass: guardrails, check, package verification and 242/242 GameTests.
- HOT/COLD scene departure is now safe against vanilla's earlier natural chunk unload: after the accepted 200-tick/64-block hysteresis, a same-runtime bounded complete HOT observation may close a naturally unloaded intact logistics scene, while its stale serialized projection is discarded on ordinary re-entry before any new claim. Volatile evidence is invalidated before combat/explosions/death; a restart without it remains visibly `DRAINING` until natural inspection. The architecture map and v3 contract state this boundary. Fresh isolated `disposable_route_scene_return` proves `HOT → COLD → routeIndex 1 → HOT` for the same `resident:1-16` UUID (`f786e0b1-0619-31ad-aafa-5993d44220f0`); Node scenarios pass 20/20 and the full critical gate passes 237/237 GameTests, build and package verification. Its single clean continuation frame is causal evidence only, not graybox visual acceptance.
- Exact HOT-Villager death now enters only through the real NeoForge death event for an owned HOT body; it is a durable typed fact with observed position and cause, never an inference from an unloaded/missing entity. It marks that canonical actor dead, drains the lease, captures only survivors, fails the affected route explicitly and changes the visible living population. State schema 11 persists exact vitality, and `frontier.scene_lease_released_v2` captures every surviving position and fixed-point health instead of restoring hidden full health in COLD.
- The first bidirectional item-custody surface observes one already-tagged exact item crossing a naturally loaded owned hive `STORE` chest and a real player inventory. It persists a `DURABLE_BEFORE_EFFECT` `ExactItemCustodyChanged` fact only after exact kind/count/NBT identity and source custody agree.
- Unknown, duplicate or altered inventory evidence is now a bounded (4,096) durable `InventoryConflict`, rather than an adapter quarantine, inventory repair or adoption. State schema 12, WAL payload codecs and the audit projection retain it immutably; the observation bridge records only naturally loaded owned chest/player surfaces and leaves the physical stack unchanged. Focused pure tests cover command acceptance, foreign-surface rejection, idempotency and state-codec recovery; the full critical gate passes with 173 `MATERIALIZED` GameTests, build and packaged-jar validation.
- `HiveColony` is the bounded mutable owner of additions beyond the immutable two-nest bootstrap: exact new organs and bioforms have stable IDs, validated hive/nest/bounds ownership, independent actor locations and schema-13 snapshot recovery. The registry is deliberately not yet an autonomous process: its future growth event must consume exact hive storage before it can add either identity.
- The first shared-hive autonomous process now turns one exact 64-item `minecraft:rotten_flesh` stack in a named nest-local `STORE` into a durable `HiveGrowthJob`; only its later completion publishes one same-nest `HEART` and one exact `GUARD` bioform. Schema 14 retains pending growth across recovery, typed start/complete/blocked facts replay through the payload registry, missing biomass remains visibly scheduled as a durable block fact, and the projection counts the added living bioform. Growth cannot silently create a `STORE` without a separately owned exact container. Focused negative/recovery tests and the full critical gate pass with 173 NeoForge GameTests.
- Exact inventory now carries one bounded canonical `ContainerSurface` for every exact container, with a persisted target and strict lifecycle (`UNMATERIALIZED → PREPARED → ACTIVE`, or visible terminal `CONFLICT`). Snapshot schema 15 round-trips it. A typed durable surface-transition process is the only mutation path, and a missing/foreign/altered chest becomes a visible conflict, never permission to recreate it. Pure command/replay tests plus a materialized removal recovery GameTest pass; the original surface slice passed the full critical gate with 174 GameTests.
- The physical container lifecycle is now a separate bounded loaded-chunk executor: it durably prepares, claims exactly one fresh chest, writes canonical slot identities once and activates every settlement depot/hive store. Recovery of a prepared surface accepts only an exact owned-slot postcondition; missing or altered contents remain visible terminal conflict evidence. CARGO_HANDOFF now waits for an active receiver rather than owning storage creation, and confirms its exact postcondition immediately after its physical write. Inventory observation covers every active exact chest (including settlement depots) before passive drift audit; active surfaces receive a fair one-per-tick non-mutating loaded-chunk inspection. Two new materialized GameTests cover exact depot slots and altered prepared-surface recovery; the full critical gate passes with 176 GameTests.
- `FrontierGrayboxPlan` is the first v3-only semantic projection compiler: it deterministically attributes finite structure cells for every settlement, hive organ and connected route network, while retaining infection as sparse canonical 4×4 cells for later loaded-chunk expansion. It rejects conflicting ownership rather than selecting a winner; tests prove deterministic full-profile compilation and destruction removing only its owning desired structure. The full critical gate passes with 176 GameTests. This is pure-plan evidence, not yet materialized-world evidence.
- The first v3 structural graybox executor now projects at most 64 desired cells per server tick and only into naturally loaded chunks. It maps functional buildings, hive organs and routes to stable colour-coded concrete, retains owner/material/semantic-part provenance in a bounded format-2 SavedData ledger, creates only fresh supported air cells, and records a pre-existing or later-changed block as terminal conflict without overwriting it. Cursor progress survives canonical revision changes when geometry is unchanged and is discarded on runtime stop. Three materialized negative GameTests cover fresh projection, foreign obstruction and player/world drift; the full critical gate passes with 180 GameTests, build and packaged-JAR verification. It deliberately does not yet materialize infection or translate structural conflicts into canonical damage evidence.
- Physical reverse causality now has one v17 bounded (65,536) canonical `PhysicalDelta` index: typed known semantic loss removes the exact desired cell, derives settlement condition through `StructureDamage`, disables a hive organ at one-third loss, and blocks a COLD route at an observed corridor hole; unknown loss persists as a scar. Direct player breaks use the same generic evidence for settlements, hive organs and routes; if the durable command is rejected, Minecraft cancels the break and shows the player an explicit error rather than creating unaccounted reality.
- Ordinary external explosions remain physically unrestricted. Their affected non-air in-bounds cells enter a separate bounded persisted NeoForge observation queue with exact pre-effect `BlockState` and known semantic provenance; only a later naturally loaded postcondition check emits a canonical known-loss or unknown-scar, and restart continues inspection rather than replaying/recreating blocks. Pure codec/rejection tests and a SavedData-reload GameTest cover the boundary; `runGameTestServer` passes 182/182. This is automated evidence, not a live destructive-scene audit.
- The first dynamic v3 infection overlay now compiles one colour-severity marker per canonical 4×4 infection cell and projects it only into fresh naturally loaded air above the observed surface. Its bounded 65,536-entry SavedData ledger records exact marker position/stage; canonical retreat clears only the still-owned carpet back to its air baseline, while a physical delta or foreign replacement is terminal conflict evidence. Player marker breaks first durably become `UNKNOWN_SCAR` before Minecraft mutates. A materialized SavedData-reload/retreat/conflict GameTest passes; the full critical gate passes with 183 GameTests.
- Exact physical custody now admits only `container ↔ player` or `container ↔ WorldCarrier`; a carrier transfer updates its UUID reverse index atomically. The loaded-chunk chest observer binds a nearby exact item-entity UUID into the stack before durable container-to-carrier evidence and accepts a tagged carrier only back into an owned chest. The full critical gate passes with 184 GameTests; hopper-specific surfaces remain next.
- A directly neighbouring naturally loaded hopper now becomes a bounded `WorldCarrier` only when it holds the one exact stack observed missing from an active PM chest. Before durable container-to-carrier evidence, its block-entity NBT and that stack receive the same UUID; repeated observation keeps it stable, while foreign/mixed/duplicate candidates are not adopted. A materialized hopper identity GameTest passes; the full critical gate passes with 185 GameTests.
- Hopper carrier identity now has a separate bounded 4,096-entry persisted `UUID ↔ block position` ledger. A tagged hopper can bind an exact stack only at its one recorded position; a copied NBT UUID, foreign pre-tagged carrier stack, or mismatched position is durable conflict evidence and is left untouched. A GameTest proves ledger reload and duplicate-position refusal; the full critical gate passes with 187 GameTests.
- An active owned chest now has end-to-end GameTest evidence for exact hopper return: the same tagged physical stack moves through a bounded hopper carrier, canonical custody first becomes `WorldCarrier` and then returns to its exact container slot after loaded-chunk observation, with no recreated or rewritten stack. The full critical gate passes with 188 GameTests.
- An active owned chest now supports exact two-way player custody: a player deposit assigns one durable ID only after physical pending-marker recovery, while a physically withdrawn tagged stack moves to that player's UUID custody and its return restores the original slot without duplication or aggregation. Inactive, foreign, occupied, duplicate or altered surfaces remain conflict evidence. Materialized ingress/withdrawal GameTests and the full critical/package gate pass with 197 GameTests.
- The first exact-resource recovery work is now one-cell semantic repair for settlement structures and hive organs. A 200-tick deterministic planner selects a known non-destroyed structure loss or organ loss only when its settlement depot or hive `STORE` has the matching active concrete stack; it creates a durable `STRUCTURAL_REPAIR` intent. A loaded-chunk executor verifies air, exact tagged stack and original claim, places the one cell, decreases that same physical stack by one and only then confirms the canonical receipt, removes that loss and restores the structure condition or organ availability. A changed/foreign block or stack is never overwritten/consumed; a restart inspects block plus remaining stack and otherwise retains `UNKNOWN_AFTER_RESTART`. Snapshot schema 19 persists a typed physical-effect observation union. Pure negative/codec tests for structure and organ plus a materialized exact-stack/foreign-block GameTest pass; the full critical gate passes with 190 GameTests.
- The route network now owns a separate materializable 27-slot exact maintenance depot. A known route-surface loss becomes repair work only after matching gray concrete reaches that active route-owned surface; no settlement depot or player-built gray road grants route authority. The same durable one-cell repair boundary restores only the recorded corridor hole, consumes exactly that stack and returns COLD passability; empty stock and foreign replacement are negative paths. Pure state/codec tests and the materialized route-block conflict GameTest pass; the full critical gate passes with 190 GameTests.
- `b6bc1a1` introduced the pure bounded `RouteTopology` replacement-path contract; the compiler, loss validation, exact repair and operation admission share it, with safe local egress for all twelve settlements and ready-only cutover from a completed non-overlapping bypass.
- Route construction has a bounded canonical candidate/cursor/`BUILDING`/`CONFLICT`/`READY` lifecycle in snapshot schema 22 while active topology remains unchanged. After an observed blocked corridor, the 100-tick route AI deterministically tests a fixed in-bounds spine catalogue against canonical deltas and compiled geometry, starts the first passable all-twelve-settlement bypass and never adopts a player/Minecraft path; an unavoidably blocked endpoint yields no fictitious reroute. Exact gray-concrete construction, conflict/restart evidence and durable ready-only cutover remain as before. Pure repeat/all-settlement/negative and end-to-end scheduler tests plus the full critical gate pass with 191 GameTests.
- The first ambient exact-actor projection now creates only named living actors near a real player: every resident becomes one deterministic owned Villager and every bioform one deterministic owned Zombie, never force-loading or adopting an unknown identity. A real death of only that exact non-leased body emits durable `AmbientActorDied` evidence; duplicate/leased/foreign evidence is rejected. Pure codec/rejection tests and materialized identity/conflict GameTests pass; the full critical gate passes with 186 GameTests.
- Ambient normal body departure now emits a distinct durable `AmbientActorObserved` capture with actual block position and fixed-point health before Minecraft releases that living owned body. It cannot infer death from absence or race a leased scene; COLD reconstruction therefore starts from the last physical state rather than the bootstrap home. Pure capture/codec tests and the full critical gate pass with 186 GameTests.
- Restored ambient bodies now have a bounded 20-tick noncanonical admission bridge across `EntityJoinLevelEvent` and `ServerLevel` UUID indexing. Only a correctly tagged living canonical actor may occupy it; while pending, projection defers instead of duplicating a body. The bridge is cleared on index/expiry/runtime stop and never changes canonical state or force-loads a chunk. The full critical gate passes with 186 GameTests.
- Ambient actors now use a separate bounded per-actor execution lease: durable `PREPARED → HOT → DRAINING → CLOSED` captures exact position/health, revision and a canonical role-derived local goal, while preventing an actor from holding a scene and ambient lease simultaneously. State schema 18, WAL payloads and projection counts retain it; loaded bodies navigate their assigned local goal with normal Minecraft AI. Restart turns active ambient leases into visible `UNKNOWN_AFTER_RESTART`; only an owned loaded-world body can reclaim `HOT`, so missing bodies are never duplicated or inferred dead. Pure lifecycle/codec/negative tests plus the existing materialized exact-body/conflict GameTests cover this boundary; the full critical gate passes with 186 GameTests.
- The ambient reclaim GameTest now supplies the physical half of that recovery path: an already loaded, exact tagged Villager reclaims its restart-`UNKNOWN` lease as `HOT` through the real entity join observer, retaining the sole deterministic UUID rather than creating a replacement. Together with the file-store shutdown/start test this is automated recovery evidence; an ordinary player-visible restart session remains a later product gate. The full critical gate passes with 187 GameTests.
- Exact infection recovery now has its first physical operation: every 200 ticks a nearby active settlement infirmary can reserve one exact `minecraft:glowstone_dust` stack for one infected 4×4 cell, durably transition a `DECONTAMINATION` intent, reduce exactly 250,000 fixed-point intensity and consume that same stack only after an observed marker postcondition. The bounded format-2 overlay ledger retains a cleared-owned marker lease across a crash window; restart either inspects/finishes that exact effect or exposes `UNKNOWN_AFTER_RESTART`, never recreates a foreign block. Snapshot schema 23 and payload codecs retain the exact receipt. Infection metabolism is now a separate bounded process rooted only in operational HEART organs: it reseeds after exact full decontamination, and ends when every root is physically destroyed. Focused pure negative/zero-removal/root-loss tests and a materialized clear/reload/retraction GameTest pass; the full critical gate passes with 192 GameTests.
- Strategic execution now owns the first human/hive effects: a settlement task alone may create an exact reagent-backed decontamination intent, and observed confirmation or restart-unknown outcome atomically completes or blocks that task in the same durable command. Hive infection and exact biomass growth no longer have ownerless timers: each task owns its target/work, exact precondition and active/blocked/completed lifecycle. Terminal task retention compacts deterministically; focused negative/recovery tests and the full critical gate pass with 192 required NeoForge GameTests.
- Settlement bread production and exact hive delivery now form a task-owned causal graph: a completed local production task is recorded as a predecessor only when it actually produced the bread, so player-supplied bread gains no fictitious provenance. Each supply objective expands into exact cargo preparation then delivery/escort; loading completes preparation and activates delivery, while absent cargo/participants terminally blocks both graph nodes. The active delivery owns the COLD route, HOT handoff and physical terminal result; route obstruction, actor death and unknown recovery block it, while confirmed handoff completes it. Graph cycles are rejected and referenced tasks survive bounded compaction. Ownerless bootstrap demand and duplicate supply planners are removed. Focused pure/runtime recovery tests and the full critical gate pass with 192 required NeoForge GameTests.
- A blocked settlement route now triggers one task-owned COLD guard patrol, not immediate construction: one exact living GUARD follows durable waypoints, records the exact observed physical-delta cell, then a dependent construction task creates the one deterministic bypass project. Repeated reviews cannot duplicate either task; a restart-unknown construction effect blocks its owning task visibly. Schema 25/WAL retain patrol evidence across recovery. Existing deployed v2 server/runtime state is unchanged and is not evidence for v3.
- HOT scene bodies now derive their Minecraft type from the canonical actor identity rather than the cargo-route presentation: a leased resident remains a Villager and a leased bioform is an exact owned Zombie through materialization, death observation, release and cleanup. A materialized GameTest proves the bioform mapping; the critical gate passes with 193 required NeoForge GameTests.
- Route interception now owns an exact operation target, 1–3 guard bioforms, bounded integer COLD approach cursors and a monotonic COLD strike epoch in schema 29. It waits only for the named caravan at the intercept, then continues exact alternating fixed-point combat every 20 ticks with durable role-checked strikes, idempotency rejection and explicit hive/settlement/aborted outcome; a hive victory atomically kills the exact residents, fails the operation and completes the hive task. An engagement atomically leases that exact mixed actor set in schema 30: COLD → HOT → COLD release shares one canonical lifecycle; restart reclaims only a complete already-owned living body set or safely drains a recorded casualty. Schema 31 adds the durable two-member `SCENE_STRIKE` intent and exact health receipt; the full critical gate passes with 194 NeoForge GameTests.
- HOT scene bodies now disable native AI and use bounded collision-aware movement: bioforms pursue, guards hold/answer attacks and noncombat residents retreat. Each exact strike remains durable `PREPARED → RUNNING → receipt`; an unresolved intent blocks substitution/replay, and deterministic tie-breakers remove map-order dependence. A recovered scene becomes HOT only from its complete original loaded UUID set; one missing body remains explicit `UNKNOWN_AFTER_RESTART`, never a substitute. Materialized GameTests prove damage, guard response and both recovery branches. The development HOT-strike profile shares reducers but is not lifecycle-wired; production interruption at +20 holds the caravan without duplicating review cadence, and hive victory cancels stale progress. Full critical gate, including 196/196 GameTests and packaged-JAR verification, passes.
- EXPLOSION receipts retain exact actors/items/overlay outcomes before postcondition inspection; actual TNT GameTests and filesystem restart prove reconciliation without blast replay. Scene hand-off retains one UUID between ambient and scene execution; bounded no-force-load boards explain state. The cache cut canonical decode samples 167→4 without stale revisions. Two 120-s JFR/player runs found no quarantine or average tick above 50 ms. A local flat dev profile initially had only 64 layers, placing grass at Y=-1; its corrected 128-layer profile places grass at Y=63 and v3 residents at Y=65. Live TNT exposed two scene-release defects, fixed in `0c5cc58` (fresh canonical state after same-tick deaths) and `934a44a` (COLD preserves released physical positions rather than requiring an obsolete approach endpoint). The complete 207-GameTest/package gate passes. In a fresh local r3 world an ordinary TNT killed exactly residents 1-4/10/16/19/22; a normal restart had no quarantine and recovered all five as canonical `DEAD` at revision 223.
- A HOT scene has one deterministic visible chest-minecart cargo carrier with exactly its canonical tagged stacks. Opening it durably moves the whole batch from `Cargo` to that exact `WorldCarrier`, interrupts its contract/operation/tasks and lets native Minecraft inventory interaction proceed; observed withdrawal/pickup/toss retain the same exact item ID. An external blast or fatal non-explosion Vanilla vehicle hit retains exact carrier evidence before Minecraft resolves it; later loaded inspection attributes exact drop/player/cart/hopper/destruction custody without replay or replacement. The terminal-damage hook never cancels physical damage; legacy release WAL remains readable. Recovery/negative GameTest passes with 213/213 core GameTests.
- Exact whole-stack consumption is reusable beyond hive growth: each settlement reviews one bounded birth permit per source day and creates a resident only after an exact active 64-bread receipt; direct birth, unloaded work and restart-unknown fail closed. Schema 38, pure scheduler/codec/negative recovery coverage and a loaded-chest GameTest passed the full 215-GameTest/package gate. `55d58c1` additionally proves the production-to-cargo contract keeps its exact 64-stack rather than its one-item template.
- `57a5bfd` defines the reusable resource-site contract and `076fb26` implements twelve stable 8×8 fields with named soil/crop slots. Schema 41 gives every initial field one durable preparation intent and 64+64-cell receipt boundary. The executor owns only complete loaded grass/air fields, canonical growth stages and exact harvest receipts; drift/restart/explosions become typed conflict or UNKNOWN, never replay/repair. Mature sites create an exact farmer/depot-bound harvest task, reset 64 slots and yield one tagged 64-wheat stack. Pure target/payload/snapshot/recovery tests and 219/219 materialized GameTests pass.
- Live r1 exposed a preparation-event subject mismatch; `006a207` fixes it and adds a public-boundary regression. Live r2 then exposed COLD route progress treating restart-UNKNOWN scene evidence as active; `6d5532b` defers that route rather than inventing scene state, and makes the graybox Zombie a daylight-safe hive creature in both ambient and HOT paths. Focused tests plus the critical gate (219/219 GameTests, build and packaged JAR) pass.
- Fresh disposable live `r3` ran the exact `6d5532b` JAR. One real-display daylight session proved a living fire-resistant Zombie bioform, a live 8×8 Northwatch wheat field at canonical stage 1, and eight exact resident casualties in canonical state after physical hive attacks, all without v3 quarantine. Screenshots are under `build/visual-audits/`.
- `e0b2506` defers a container's provenance-checked socket until graybox support exists; `7a73a1d` makes harvest receipt commands revision-scoped. Both passed the complete critical gate (221 required GameTests, build and packaged JAR). A fresh r5 harvest run exposed a more fundamental defect: the player was in `pale_mirror:frontier_graybox` but every v3 physical executor and feedback hook used Overworld, so loaded chunks could not materialize canonical sites.
- Frontier v3 now has one V3-owned `FrontierV3PhysicalWorld`: lifecycle execution, restart inspection and all public physical-event bridges use only Graybox; other dimensions are explicit no-ops. Its canonical `WorldId` is `frontier:graybox`, invalidating the faulty r5 state; Graybox's known light-gray-concrete baseline is accepted exactly for field preparation. SourceGraybox delegates to the same physical dimension identity without a V3→legacy dependency. Fresh r6 materialized the Northwatch field and exposed one HOT/COLD route-lease race; `4b68424` made the lease exclusive, deferred COLD start/progress/readiness/combat and excluded a generic route scene during unresolved interception. The critical gate passed with 222 required GameTests.
- Fresh r7 exposed COLD combat changing an actor under unresolved ambient recovery; `1d2de76` makes ambient authority exclusive. r8 then exposed PREPARED restart turning permanent UNKNOWN; `0e7896d` retains PREPARED, but r9 found bootstrap resident anchors inside Hall. `FrontierSettlementActorSlots` now compiles bounded, unique clear street/perimeter anchors for bootstrap and births; r10 then found legacy admission cancelling V3 bodies. Strict canonical UUID/type/lease proof permits only exact V3 bodies. `0e1a8a4` freezes HOT leases at server stop; an initial fresh-r11 graceful recovery retained resident `1-1`'s UUID, but a later ordinary client reconnect exposed a separate duplicate-UUID race: late `EntityLeave` had closed a lease after the old body entered chunk NBT. The r11 world is tainted as recovery evidence. `e0cd261` replaces that late close with fail-closed HOT recovery and the only COLD hand-off with 200-tick absent-demand/64-block-safe durable capture followed by explicit body discard; the contract/map and GameTest negative path match it. Fresh r12 proved normal player leave/rejoin and stop/restart with 20 recovered HOT leases and no duplicate UUID/quarantine, but its field exposed a separate V2/V3 single-writer violation: frozen SourceGraybox could republish into V3 Graybox. The r12 field is tainted. The successor makes launch-mode V3 exclusively own Graybox, pauses/rejects V2 writers even after V3 quarantine, and has focused launch-ownership coverage; without competing client/server load its full critical gate passed 223/223 GameTests plus build/package verification.
### Next
- Close T0.1–T0.5 before adding another movement-bearing scene or transport family: typed spatial values and fresh-world persistence, semantic facility ports, bounded 3D capability topology, one observed HOT/COLD cursor, then non-flat/blocked/restart and Foundry evidence. Complete the already active medical correction only through this reusable port/topology boundary; do not add another coordinate-specific approach.
- Close V3-AUD-022 before another route or transport scene: prove the registered maintenance chain in a materialized normal path, unavailable/foreign source or target, and restart between pickup and target postcondition; then run one native ordinary player break → exact repair → same-edge recovery scenario. The current physical body/scene limits remain fixed; any future increase requires the documented same-seed JFR and terminal causal scenario, not TPS alone.
- The active V3-AUD-022 contract remains `docs/frontier-v3-route-maintenance-contract.md`: `RouteMaintenance` owns one PM route loss, exact team, source pickup/cargo and target receipt. It cannot become generic repair, a bypass substitute, force-loading or player-road adoption. The immediate next implementation slice is physical GameTest and pilot evidence, not new strategic behavior.
- The engineering/recovery slice now has the shared exact tool boundary: every
  fresh 12-settlement bootstrap explicitly stocks four exact iron pickaxes per
  depot; a retained `EngineeringRecoveryTeam` issues/returns one of those
  ordinary tagged stacks via `EQUIPMENT_ISSUE`/`EQUIPMENT_RETURN`, generic
  owner-scoped receipts and the same naturally-loaded Villager executor as
  defence. Fresh native disposable pilot `b07ccad0-69ca-421e-86f8-7f3d06664be5`
  passed: `item:bootstrap-1-engineering-tool-1` moved to `resident:1-32`, its
  bounded `human_equipment_issued` correlation occurred before graceful
  restart, and the same ID still had that actor custody after restart; its
  clean barrier frame is
  `build/frontier-v3-scenarios/disposable_engineering_equipment_restart-b07ccad0-69ca-421e-86f8-7f3d06664be5-engineering-tool-after-restart.png`.
  Schema 85 retains one immutable bounded COLD approach corridor and cursor per
  same retained person to distinct work-site columns; each turn may advance one
  living non-HOT person. Schema 86 additionally persists the accepted ordered
  replacement-cell plan and uses a fixed bounded public-lane catalogue, so
  steady ticks/recovery never recompute world route geometry; missing lanes
  block admission visibly. Full pure suite passes 375 tests. Tool-ready
  construction remains deliberately gated before the former autonomous route
  placer. Schema 87 now adds that exact naturally loaded HOT crew/work-site
  lease: its closed registered cause is `project + current immutable-cell
  cursor`, it retains only the COLD-complete exact crew, and only its HOT
  executor may request one durable physical route-construction intent. The
  executor does not write blocks; the physical executor remains the only
  writer and a confirmed receipt clears the old approach before the same crew
  may approach the next cell. The former independent first-corridor selection
  could deadlock exact people at intersecting lanes. The current bounded joint
  compiler retains only a lane combination whose deterministic COLD schedule
  reaches every distinct work-site column without an overlap/head-on exchange;
  it fails closed if its 256-combination bounded search cannot prove one.
  Fixture and focused route/engineering/diagnostic tests pass. Fresh native
  `disposable_route_repair_cargo_restart-1788266376186` then proved the same
  causal chain across a graceful restart: player route loss, exact cargo and
  crew preparation, naturally loaded repaired `gray_carpet`, and terminal
  `route_construction_cell_built` (`revision 3921`, intent `CONFIRMED`). The
  pilot's disposable `fml.toml` now disables only NeoForge's pre-game early
  loading screen after an XWayland font crash; fullscreen ordinary Minecraft
  rendering and connection remain the evidence path. Node 29/29, the focused
  client-profile preparation and this native scenario pass. The captured
  worksite/crew frame is causal evidence but is visually crowded, so it is not
  yet sufficient player-comprehension evidence for engineering readability.
- `6d8558f` makes the seed territory canonical rather than a visual multiplier: two 3×3 clusters are 18 exact infection cells and materialize as two 12×12 patches. It also gives exact bioforms role-coloured, non-dropping head markers (worker lime, scout cyan, guard purple, bomber red), suppressing vanilla daylight ignition but preserving real fire/explosion observation. Fresh r38 proves no daylight flames, distinct roles, readable organ boards and the 12×12 patch. `804bb16` then derives organ/structure contamination only from semantic-geometry intersection with current canonical infection cells. Same-world r39 correctly exposed a materializer defect: frequent unrelated revisions reset the eight-board cursor at its first board, so later loaded hive boards retained stale long text. `f75ff95` preserves the deterministic cursor position when slots are stable; GameTests prove in-place text update/no duplication and starvation-free revision refresh. `guardrails`, build/package and 229/229 GameTests pass. The SHA-pinned `5cf8d639…` JAR restarted that same r39 world with normal fail-closed UNKNOWN HOT recovery; one visible native pilot loaded the western hive and recorded `visible_hive_west_nest-1e062a79-efd4-4241-b272-6076ba547a5a-hive-west-nest.png`, showing the persistent boards updated to compact `INFECTED / BLOOM|SATURATED` text without overlap or duplicate display. This is narrow visual/restart evidence only; the broader alien-biome/materialization-comprehension gate remains open. The successor admits a two-cell-clear body above a loaded infection-carpet floor without writing the world, exposes its exact read-only admission state, and compiles new hive bioforms into deterministic organ-clear perimeter slots. Pure/NeoForge tests plus full gate pass at 229/229. Same-world r39 proves recovery of the former blocked `west-0` as one HOT UUID; fresh r40 proves the new canonical `west-0` perimeter slot (`-426,64,414`), 12 settlements/48 bioforms/12 sites, and a player-height frame with no zombie on a hive roof. r40 later quarantined on an old harvest observation and is preserved as negative evidence. The successor replaces ambient vanilla navigation (which could route bodies through organ centres) with shared deterministic collision-checked, gravity-enabled role motion around each durable hand-off slot; 230/230 GameTests and Node scenarios pass. Fresh SHA-pinned r41 records `visible_hive_motion_west_nest-235978d1-825a-467d-a333-c2d96eb4573f-*.png`: all visible bioforms remain grounded outside organs and change position over eight seconds, while `bioform:west-0` remains one HOT exact UUID and r41 has no quarantine. r41 is the active disposable world. The r40 quarantine root cause was exact: `ResourceSiteHarvestObservation` existed in the snapshot schema but was omitted from the WAL payload codec, so a confirmed physical harvest failed before acknowledgement; v3 now appends compatible tag 10, pure receipt round-trip coverage and a native harvest scenario that permits blank-ID summary diagnostics. The focused harvest test, 230/230 full critical gate and 8/8 Node tests pass; r41 live observation reset a loaded mature field to `GROWING` stage 0 without a new quarantine. The first capture pair was rejected as visual evidence because its pre-frame was `Loading terrain`; the scenario now adds client render settling and HUD suppression before frames.
- The pilot no longer relies on per-scenario HUD steps. Every declared frame is an exact local `clean` capture barrier by default: it hides generic HUD/chat, closes incidental screens, waits ten client ticks, emits `frame_ready`, and cannot advance until the X11 helper records `frame_captured`. `presentation: player` remains an explicit opt-in for UI assertions. Native abrupt recovery on disposable world `v3-disposable_redwillow_field_abrupt_re-6b9e0c07` passed after this handshake; its clean post-restart field frame is `build/frontier-v3-scenarios/disposable_redwillow_field_abrupt_restart-be29d6be-b5ab-4fb1-a7ab-b1bf3363ce05-after-abrupt-restart-field.png`. A neutral field is now insufficient to recover: only the matching durable active claim permits the exact full-footprint rebuild; current, rebuilt and foreign paths are covered. Node schema tests pass 17/17 and the full critical gate (guardrails, build/package and 231 GameTests) passes.
- A canonical READY→HARVESTING hand-off can precede bounded physical crop projection by one materializer turn. The harvest executor now recognizes a complete exact field at its owned ledger stage as `WAITING_FOR_FIELD_PROJECTION`, rather than falsely recording an irreversible conflict; only the field projector advances it, including while the receipt is HARVESTING. Read-only intent diagnostics expose claimed stage, exact match and decision. Fresh isolated native `live_redwillow_harvest` evidence records the wait, then one `CONFIRMED` exact 64-wheat stack in `container:4-depot` and the reset field at epoch 2/stage 0; Node 17/17 and the critical gate (231 GameTests, build/package) pass.
- Player feedback has one explicitly noncanonical, bounded delivery boundary: stable world facts remain on owned boards, journal/Atlas retain detail, and background simulation cannot write HUD/chat. Successful player actions explain themselves through their physical result, item lore and owned board. A direct refusal now has only a locally heard, three-second-deduplicated denial cue; its reason remains on the object board or the player-requested contextual card. Ordinary gameplay no longer writes action bar, toast stream or system chat; system faults remain visible through their conflict state and opt-in operator diagnostics. `verifyArchitecture` mechanically rejects direct HUD/chat outside the explicit RegionalJournal and operator transcripts. Focused presentation tests plus `guardrails check` pass; this is not a substitute for player-comprehension review.
- Atlas discovery no longer bypasses that boundary with a system-chat line: negotiated clients receive the bounded read-only Atlas snapshot and discover its notice only by opening Atlas, while unsupported clients retain the world-object explanation. `verifyArchitecture` now rejects direct chat/action-bar writes in the Atlas network boundary. Focused presentation tests and `guardrails check` pass.
- The native pilot can now prove real player resource ingress without a test-only mutation path: it opens an owned chest through ordinary Minecraft interaction, quick-moves one exact player stack, waits for the terminal canonical container slot and records bounded read-only container diagnostics plus PMV3 trace. `container` is an explicitly registered one-object diagnostic with lifecycle and occupied exact slots only. Player capture frames preserve the asserted screen but suppress disposable-client chat/tutorial/toast noise. Fresh isolated `disposable_redwillow_player_ingress` passed twice on `DISPLAY=:0`: `minecraft:glowstone_dust ×1` became canonical `item:ingress-*` at `container:4-depot` slot 0 (`ACTIVE`, `occupiedCount: 1`); the second frame is `build/frontier-v3-scenarios/disposable_redwillow_player_ingress-3cbdca17-37e2-4dc6-8a90-75ee472c4572-opened-depot.png`. Node 18/18, focused diagnostics/parser tests and the critical gate (231/231 GameTests, build/package) pass.
- The native pilot now proves the reciprocal ordinary withdrawal path: it opens Northwatch's owned depot and quick-moves the exact canonical `item:bootstrap-1-wheat ×64` into the real player inventory. Fresh isolated `disposable_northwatch_player_withdrawal` retained the same item ID and reported `PLAYER` custody at revision 95 while the active depot reported zero occupied slots; its clean player-UI frame is `build/frontier-v3-scenarios/disposable_northwatch_player_withdrawal-a6bb7ff8-e8cc-477f-b8a6-a8bb0d48f0c3-withdrawn-wheat.png`. The runner now permits 90 seconds for Minecraft's own all-dimensions-saved acknowledgement before deleting a loaded 1024×1024 disposable world; it still never treats timeout as a save. Node 18/18, focused tests and the full critical gate (231/231 GameTests, build/package) pass.
- Player presentation now has a bounded replace-not-stack context-card contract: one negotiated client card contains a title and no more than two detail lines, expires locally, and never becomes canonical state or chat history. Source-graybox briefings and V3 object boards use that card; a V3 click is accepted only for the exact current canonical board plan plus matching claimed TextDisplay UUID/position, never a lookalike tag. Action-bar outcomes are forcibly one line, while critical durable rejections remain deduplicated chat. The architecture map owns the client/server boundary and its presentation-only fallback. Pure negative tests and the full critical gate (232/232 GameTests, build/package) pass. Fresh isolated `disposable_northwatch_board_card` then proved an ordinary near-range board click through the native client on `DISPLAY=:0`; the player-visible result is `build/frontier-v3-scenarios/disposable_northwatch_board_card-734626ea-0ac3-4022-8a0a-b534e88231a4-northwatch-field-card.png`, showing one readable Northwatch/field card over a real loaded crop field and live residents.
- A native Bomber now executes a durable-before-effect owned TNT entity, while the observed Vanilla blast remains unrestricted. Exact impacts reconcile before the scene closes; physical cargo destruction atomically leaves the operation `INTERRUPTED` and engagement `ABORTED`, then drains/closes the scene without fabricating a COLD combat continuation. Strict actor admission requires the exact persisted UUID/type/lease proof. The isolated `disposable_hot_bomber_explosion` pilot passed after an ordinary visit and captured the real crater, drops and live bodies at `build/frontier-v3-scenarios/disposable_hot_bomber_explosion-43ee518c-3843-4911-b49a-2c5628dc7c15-hot-bomber-aftermath.png`; the same PMV3 trace reaches terminal `CONFIRMED`, `CLOSED` and carrier `CONFLICT`. The client pilot accepts both documented semantic-camera target spellings (`position` and `at`). Focused planner/pilot tests, Node 19/19 and the complete critical gate pass: `guardrails`, `check`, 234 GameTests, NeoForge build and packaged-JAR verification.
- A dedicated `disposable_hive_growth` native pilot now proves the complete ordinary loaded-chunk growth flow: the east `STORE` physically materializes and consumes its one exact 64-rotten-flesh stack; the intent confirms; one named east `HEART` and one HOT `GUARD` appear; and the sparse infection field advances from 18 to 19 cells. Graybox scheduling is now round-robin by naturally loaded plan chunk, so an unvisited lexicographically earlier site cannot starve a player's newly loaded support/container. The declarative scenario captures `grown-hive-heart`; its PMV3 trace reaches the terminal hive, intent and actor assertions. Node 19/19, focused tests, guardrails, build/package and 235/235 GameTests pass.
- The reciprocal HOT-caravan theft path now has real-player evidence with no test-only transfer: a bounded native-pilot action may interact only with the nearest locally rendered entity of a declared type, never selects a server UUID, and the normal server interaction remains authoritative. Fresh isolated `disposable_hot_cargo_theft` opened the actual `minecraft:chest_minecart`, quick-moved exact `minecraft:bread ×64`, retained `item:production-1-1-bread` at the same count in that player's custody, and terminally changed `operation:supply-1-2` to `INTERRUPTED` at revision 101. Its player-UI frame and bounded PMV3 trace are under `build/frontier-v3-scenarios/`; Node 20/20, focused parser test, two 237/237 GameTest passes (the first full attempt had one non-reproducing pre-existing scene-admission flake) and the complete critical gate pass.
- A native ordinary-combat pilot now proves player-caused HOT resident death without server target authority: it selects one nearest locally rendered Villager by declared type/range, sends bounded normal client attacks, holds its local runtime target only, and gives the final packet a bounded acknowledgement window. Fresh isolated `disposable_hot_resident_death` records `resident:1-16` as `DEAD` and `operation:supply-1-2` as `FAILED` after the same player action; its causal trace and player-height Northwatch frame are under `build/frontier-v3-scenarios/`. The 20-strike bound accounts for Vanilla spawn/damage immunity without replacing combat with a test mutation. Two previously intermittent GameTests were fixture defects: their 1×1 vanilla template did not load off-template scene/field chunks. They now synchronously load only their own exact fixture chunks without a persistent ticket; extended admission diagnostics retain the precise mismatch. Node 20/20, the focused Java parser test and the complete critical gate now pass: `guardrails`, `check`, build/package verification and 237/237 GameTests.
- Player destruction of a materialized route now has an end-to-end, inspectable v3 proof. The graybox observer accepts the durable physical delta before Minecraft removes the owned route block and records a deterministic `physical-delta:x,y,z` trace. Read-only `PMV3_DIAG physical_delta x y z` exposes only bounded semantic state (kind, owner, part, player/system cause class and trace), never a player UUID. Fresh isolated `disposable_route_obstruction` broke `(-380,64,-304)` through normal client input, recorded `KNOWN_SEMANTIC_LOSS` for `route:frontier-network`, naturally released its HOT scene, and terminally failed `operation:supply-1-2`; the route board visibly changed to `ROUTE DAMAGE · PATROL NEEDED` in its clean semantic frame. Node 20/20, focused Java diagnostics/pilot tests and the complete critical gate pass: `guardrails`, `check`, build/package verification and 237/237 GameTests.
- Route repair now has an exact physical supply boundary rather than implicit stock: a player deposits gray concrete in the active route-maintenance chest; one real source-stack unit is durably observed as one distinct COLD cargo item, and exactly that cargo is consumed only when a naturally loaded target cell is placed. Each cell has a fresh material-load intent ID, so completed audit records cannot collide with later work. The read-only `route_construction` diagnostic, compact route board and PMV3 trace expose cargo/cell state without mutation. Fresh isolated `disposable_route_repair_cargo_restart` used a deliberately small test-only view distance to retain COLD cargo outside a naturally loaded chunk, completed a graceful restart, then visited the target and proved one route block plus terminal `route_construction_cell_built`; no chunks were force-loaded. Node 22/22, focused pure tests and the full critical gate (239/239 GameTests, build and packaged-JAR verification) pass.
- Exact resident health and settlement quarantine are now canonical, versioned state: semantic infection contact causes one ordered resident transition per owner reconsideration, while quarantine blocks new logistics and migration without altering existing bodies or custody. The `health-quarantine` disposable profile and scenario prove a player can naturally visit Northwatch, observe quarantine, restart the same world gracefully and find the same quarantine truth afterward. The active test pilot keeps its system feedback quiet for the whole run, retaining only bounded diagnostic state and `PMV3_PILOT_DIAGNOSTIC` JSONL/client-log evidence; ordinary player chat and production HUD policy are unchanged. The contextual card now uses a responsive safe-zone width. Node tests, focused Java tests and the critical gate pass (239/239 GameTests, build and packaged-JAR verification); the successful restart manifest is `build/frontier-v3-scenarios/disposable-northwatch-quarantine-1788064640162.json`.
- V3 object boards now have a pure semantic scope without changing their canonical identity, physical position or provenance: settlement halls, hive hearts and the route network are approach landmarks; facilities, fields and secondary organs are local facts. The executor renders local boards compactly (at most 45 blocks), physically occluded and smaller; landmarks remain visible within 96 blocks. A native graceful-restart quarantine scenario on one fullscreen `:0` client captured the readable local infirmary board at `build/frontier-v3-scenarios/disposable_northwatch_quarantine-dd7a5858-4c0f-4f1e-8710-ed3cfbd7e1c9-northwatch-quarantine-card.png`. The critical gate exposed an unrelated GameTest fixture race—fresh entity UUID lookup is only visible next tick—so strict scene-admission assertions now wait one tick without relaxing their proof. `guardrails`, build/package verification and 240/240 GameTests pass.
- Exact-person COLD Transit is now canonical and durable: a housing loss admits up to two fair, parallel journeys per source (24 world-wide), each with one bounded adjacent-position route, short deterministic COLD advances and an implicit exact destination-bed reservation. Quarantine, route loss or lost reserved housing block the same journey; household membership changes only on final arrival. State schema 49 and WAL codecs retain routes/cursors beyond byte size, recovery resumes the exact due action, and all 12 profile sources compile within bounds. Focused negative/recovery/codec tests and the critical gate pass: `guardrails`, `check`, NeoForge build/package verification and 240/240 GameTests. This is deliberately COLD-only: the next wave must hand the same journey to a naturally loaded HOT Villager, not substitute a new body or a teleport.
- Exact-person Transit now crosses naturally loaded HOT scenes without a second movement state: the same journey, cursor, lease and deterministic Villager UUID advance through normal collision-authoritative Minecraft movement, while COLD is suspended. The pure corridor compiler reserves visible route surfaces for readability but chooses adjacent clear lanes, excludes structures/organs and every other living actor hand-off, and keeps full block collision authoritative. A disposable native restart scenario (`disposable-resident-transit-restart`, manifest `1788072470906`) proved one resident at route index 4 before restart and index 8 after recovery, with the terminal `resident_transit_advanced` trace; 241/241 GameTests plus the full critical gate, build and packaged-JAR verification pass.
- The exact engineering work-site now has its own `engineering-worksite` read-only fixture and declarative native restart scenario. It begins only from an already COLD-complete real four-person crew, issued exact pickaxes and a one-unit exact gray-concrete cargo; an ordinary visit produces `ENGINEERING_WORKSITE` HOT, physical `route_construction_cell_built`, then `scene_released`. The first live attempt exposed two logistics-only closure assumptions: diagnostics and the canonical release/recovery planner. Engineering ownership now resolves from its retained project even after the confirmed cell advances the cursor, and releases/recovery use that owner without cargo semantics. Fresh fullscreen `:0` disposable world `v3-disposable_engineering_worksite_rest-66519478` passed the full HOT → exact receipt → graceful restart → same `BUILDING`, no-cargo, `confirmedCells: 1` result; its pre-restart clean frame is `build/frontier-v3-scenarios/disposable_engineering_worksite_restart-924ce949-76d4-4bef-9970-bbf19226a419-engineering-worksite-repair.png`. Focused Java/Node checks and the full critical gate (`guardrails`, `check`, build/package, 251/251 GameTests) pass.
- Medical/evacuation is now schema 89's exact COLD receipt foundation, not a role label: each settlement's bounded review may admit one exact local infected patient, one-to-three exact local medical workers, intact infirmary and active-depot `minecraft:honey_bottle` into `MedicalEvacuationOperation`. The same IDs derive exclusive `MEDICAL_EVACUATION`; an ordinary durable physical-executor command must confirm that exact source decrement before the named patient becomes `RECOVERING`. An unknown restart retains the same people. Completed records retain only the most recent 64 receipts; active/unknown work is never compacted. Snapshot/WAL, rejection, engine scheduling, exact receipt and retention tests pass. Adding the new settlement review exposed an unrelated scheduling defect: scout initial timing depended on the size of the whole mixed initial queue; it now depends only on deterministic scout ordinal. This is intentionally not player-facing treatment yet: next is one naturally loaded HOT infirmary lease/executor using those same people, then native causal evidence.
## Open questions
- Future Create integration still requires explicit selection/pinning of compatible Create add-ons, exact rail topology, recipes, energy ratios and industrial balance after graybox validation; road freight and aeronautics are not open scope in the accepted first phase.
- Product decision needed before the next finance slice: should an operational settlement grant its local company a bounded interest-free working-capital credit facility with fixed daily repayment, where a missed payment blocks new work but does not cancel confirmed wages/shipments, and recovery is only exact repayment or recapitalization? No credit/debt implementation is authorized until this rule is accepted or replaced.
- Exact farm growth/yield timing, infection/territory tuning and final HOT actor budgets remain profile calibration work; product validation still needs real-display, clean-room and cooperative-player v3 audits. The accepted per-owner lane rule now permits one strategic objective plus one facility harvest objective. The restart runner has complete graceful and abrupt two-server same-world evidence: both pilots pass the `GROWING` field assertion after durable save or exact-JVM crash/restart; an entirely absent confirmed field is rebuilt only from complete baseline, while partial/foreign state remains visible conflict.
- Context-card player-visible evidence now exists for one V3 field board and the new quarantine card; a local physical-board frame now proves the new compact/occluded grammar. Future UX review should verify other object types (hive organ, damaged structure and route) and judge broader world navigation, information density and landmark placement with unbriefed players.
- The operation-local caption/camera grammar has narrow evidence for one HOT caravan, but broader product validation remains open: unbriefed players must still judge navigation, information density, ambient crowding and the visual grammar for other operation types (combat, hive assaults, damaged structures and field work).
- The defender scenario proves that the derived nameplate is genuinely rendered,
  not merely stored as an entity name. It is still narrow correctness evidence:
  the vanilla-scale label is not yet the accepted large, bright tactical visual
  grammar for unbriefed players. Establish that reusable visual layer before
  claiming broader human-combat comprehension.
## Working set
- `AGENTS.md`, `CONTINUITY.md`, `architecture.yml`, the v3 docs/plan, archived ledger; test-pilot now has a minimal current-source NeoForge profile, unique seeded disposable worlds, terminal domain waits, settlement-work reasons, read-only fixture preconditions, ordinary chunk visits, camera-semantic frames, graceful/abrupt recovery splitting, bounded PMV3 JSONL/client-log diagnostics, ordinary exact player/container ingress and withdrawal, a quiet pilot-only system-message policy, bounded contextual-card presentation, and scope-based physical V3 board grammar.
- The exact issue/return, first managed-death loss/drop/pickup and first
  defender-readiness boundaries are closed at schema 82. The accepted human
  composition's route-unit defect is closed at schema 83: `RoutePatrol` and
  `RouteOperation` each embed the sole immutable exact unit manifest; new
  patrols retain a leader plus scout, while cargo operations retain one crew
  member plus two escorts. Schema-82 snapshots and historical patrol WAL
  preserve only their same understrength legacy people and never fabricate a
  replacement. The next human work extends through engineering/recovery and
  medical/evacuation operation/equipment owners while preserving one canonical
  item claim and accounting for physical results.
- The first engineering/recovery owner begins at schema 84, its exact COLD
  work-site approach at schema 85 and durable replacement-cell plan at schema
  86: every newly
  admitted `RouteConstruction` embeds a stable one-to-four-person exact local
  `EngineeringRecoveryTeam`; the same people derive exclusive
  `ENGINEERING_RECOVERY` assignment and are rejected when another active human
  owner already claims them. Its snapshot/WAL value is durable. Historical
  schema-83 construction remains explicit autonomous legacy work rather than
  silently acquiring invented residents. This is intentionally not yet a
  sapper role or physical crew lease. Exact tool custody, a bounded jointly
  schedulable COLD approach and the schema-87 HOT work-site lease now exist;
  its ordinary COLD approach → HOT repair → receipt/restart causal scenario is
  now complete. The next human capability owner is medical/evacuation, while
  engineering must retain the same exact crew/material semantics for later
  cells and recovery.
